# Decisions log

One paragraph per architectural choice, and why. Newest at the top. Written so that in six months the reason is still here, not only the result.

Started 30 August 2026, backfilled from what is already built and from the Yaadly Technical Notes of 24 August 2026.

---

## 2026-09-02 · A place in the worker portal to see the actual invoices, not only the computed estimate

**Founder's own instruction, direct continuation of the same session's work: "there should be a place in the portal where they can review the money of jobs and what is pending."** `WorkerMoneyPanel` (Stage 5.6, still in place, unchanged) has always shown one computed figure per job, 88% of labour plus materials, held or released off whether the whole job is `complete`. It was never the real document trail, and now that `invoices.worker_email` exists (this session, same day) there was finally something real to show instead of a client-side estimate.

**`WorkerInvoices`, a new section beneath the existing money panel, not a replacement for it.** Lists the worker's own actual invoices, job by job, stage by stage: the real `period_label`, the real `total_pence`, a **Pending** badge on anything `sent` that the worker hasn't yet recorded receiving payment for (`jobs.pay_method`, the existing self-report). Deliberately worded in the panel's own copy that "pending" is not proof money moved, only the worker's own later note against the job says that, the same honesty `CLAUDE.md 9` already requires of the rest of this portal.

**A real display gap found and fixed before it shipped, not after.** The first version looked up a job's title from the same `jobs` array the page already had, scoped to this worker's *live* stake (`jobs.worker_email` or an active quote). An invoice's own `job_id` isn't guaranteed to fall inside that narrower list, an older or differently-seeded row can name a job this worker no longer shows as "live" but was still genuinely invoiced against, and the first live test showed exactly that: "Untitled job" on a real, correctly-scoped invoice. Fixed by looking up any missing titles directly, by id, rather than assuming the page's existing job list was exhaustive.

**A second parallel-session hazard, same class as before, caught before anything was lost this time.** Mid-edit, `git status` showed `On branch main` again, silently, with none of this session's other work in sight, meaning both new files had just been written against `main`'s own base rather than `the-approve-button`'s. Nothing was committed yet, so nothing was actually at risk, caught by checking the branch before committing rather than after, per the runbook entry the last time this happened. Resolved with `git stash`, a clean `checkout` back to `the-approve-button`, and `stash pop`, re-verified with a typecheck and a live reload before writing anything further.

**Verified live**: the panel renders three real invoices across two jobs correctly, two still `draft` (raised before this session's auto-send fix existed, a real and accurate distinction, not a display bug), one `sent` and correctly marked Pending, all under their correct job titles after the lookup fix.

---

## 2026-09-02 · A worker can read their own pay invoice; `invoices` had no column naming them at all

**Founder's own instruction: "a worker needs to know which job they are being paid for."** Checked before building anything, not assumed: `invoices` had `client_email`/`client_user` and nothing naming the worker at all. The worker portal's own money panel already shows a job's title per row, but that figure was always computed client-side from `job_quotes`/`jobs.status`, never tied to the actual invoice document raised in the worker's name; there was no RLS path by which a worker could read that document even if they knew its id.

**`invoices.worker_email`, set only on a `payable_to = 'worker'` row**, null on every other invoice type unchanged. `invoices_worker_read`/`lines_worker_read` mirror the existing client-read policies exactly: never a draft, only their own, matched on the same `lower(email) = lower(auth.jwt() ->> 'email')` shape used everywhere else in this codebase. Both raise functions now write it from the accepted quote's own `worker_email` at the moment they raise. Every worker-pay invoice already on file, raised earlier in this same session, was backfilled in the same migration, so nothing already sent sits invisible to the person it was always for.

**Verified live**: the four already-raised worker-pay invoices all backfilled correctly. A fresh trigger-fired raise, in a rolled-back transaction, wrote `worker_email` on insert without being told to separately. `pg_policies` confirms `invoices_worker_read`'s predicate matches what was written, not assumed from the migration text.

**A parallel-session hazard, caught before it did damage, not after.** Mid-session, `HEAD` in this shared working tree moved to `main` without this session doing it, twice, the second time silently: reading `DECISIONS.md` to add this very entry returned a much older version with none of today's prior entries in it, because `main`'s own copy of the file predates this branch's work entirely. Caught by noticing the missing entries were missing rather than assuming the read was current, confirmed with `git branch --show-current` and `git status`, and resolved by checking out `the-approve-button` again before writing anything, not by editing what was on screen. Nothing was lost: all of this session's commits were made while `HEAD` genuinely was on `the-approve-button` each time, confirmed from each commit's own branch-prefixed output, `main` moving under the working tree never touched them. See `RUNBOOK.md` for what to check if this happens again.

---

## 2026-09-02 · The invoice is sent the instant the client approves, no separate admin click; a real ordering bug caught on the first live test

**Refused first, then resolved, not overridden.** Asked to auto-send, this session refused on the governing rule ("a named human confirms every consequential step") and proposed a bulk-send button instead, since every other invoice send in this codebase runs through `yaad-invoice`'s `isAdmin()` gate and no admin session exists at the moment a trigger fires. The founder's reply resolved it rather than overruling it: "the client is approving the workflow." That is the named human the rule asks for, present, authenticated, taking a real action, it simply is not an admin, and this was never an admin's decision, it is arithmetic on terms already agreed. Confirmed against the actual database rather than assumed: `invoice_status_guard` only calls `is_admin()` on a `sent -> paid` transition, never on `draft -> sent`, so moving straight to `sent` inside the same security-definer chain the client's own `approve_stage()` already runs in needed no service-role bypass of anything, `yaad-invoice`'s own admin gate was never touched. Deliberately narrow: only the per-stage worker-pay invoice works this way. The whole-job fallback and every other invoice type in this codebase still need an admin's own click, unchanged.

**A real bug, caught on the very first live test, not by inspection.** The first version inserted the invoice with `status = 'sent'` directly, then inserted its lines as a second statement, and `invoice_line_price_guard` correctly refused: "a sent invoice's lines are frozen." Postgres rolled the whole function back on the error, so nothing broken reached the table, checked directly by counting every worker-pay invoice's own lines before writing the fix, not assumed from the error message alone. Corrected order: insert as `draft` (lines can only ever be added to a draft), insert the lines, only then update to `sent`, passing through `invoice_status_guard`'s ordinary `draft -> sent` branch exactly like any other invoice, `sent_at` included.

**Verified live, both the fix and the trigger together**: `raise_job_stage_worker_pay_invoice('JOB-TEST-KICKOFF-1', 2)` raised `INV-2026-0016`, J$3,900 (60% of J$6,500 labour), `status = 'sent'`, `sent_at` set, one correctly priced line. The trigger itself, fired by a real `stage_approvals` insert, in a rolled-back transaction: J$1,300 for stage 1 (20%), `status = 'sent'` with no separate step at all.

---

## 2026-09-02 · The worker pay invoice raises itself, per stage, the moment the client approves; a founder-set default when there's no pack to read

**Founder's correction to the same-session feature above: not one invoice at the end, one per stage, and not an admin's errand to remember.** "It should not be us deciding what goes in, it should be the worker and client payment terms that are being raised against," her own words. `raise_job_stage_worker_pay_invoice(job, stage)` reads a stage's own `proportion_percent` from whichever pack governs the job, same document and same priority order as the completion trigger (`20260902h`): an approved Kickoff Pack first, an approved Quote Pack draft second. `trg_raise_worker_pay_on_stage_approval`, `after insert on stage_approvals`, calls it the instant `_do_approve_stage()` inserts the client's own approval, whatever channel that approval came through, portal, WhatsApp, in person. A failure to raise (no pack yet found, some data problem) is logged and swallowed inside the trigger, never allowed to block the approval itself: the client's evidence sign-off is the consequential act this whole feature hangs off, the invoice is bookkeeping that follows it, not a precondition on it.

**Materials are never split by proportion, and never assumed present.** "The material is dependent on the job as some have it, some don't," founder's own words, confirming a design already reached independently: whether a stage's invoice carries materials is read off whether a `kind = 'materials'` evidence row actually exists for that stage, a fact, not a guess.

**A founder-set default for a job with no pack at all: 25% on the first stage, the rest on the second, nothing past that.** Not this session's invention: "if there is not payment term, it is a 25% and the rest split," stated with the same authority as every other split in this codebase. `invoices.notes` says plainly when a stage used the default rather than an agreed document, so nobody mistakes Yaadly's fallback for something the worker and client actually signed.

**Reuses `invoices.stage`, already present and otherwise retired** since the old per-stage agency-fee invoice was dropped (`20260901y`): here it names which stage of the *worker's* pay this invoice is for, distinguished from the whole-job invoice (`20260902i`, `stage` null) by `payable_to` plus a populated `stage`.

**A real gap closed in the same pass, not left for later.** A trigger-raised invoice lands as `draft`, and nothing in the desk's Job Invoices view could actually send a draft that hadn't come through its own "Raise & send" click, it would have sat there forever with no path to being emailed. Added a plain **Send** action for any `draft` invoice, same `fn({action:"send"...})` call the existing raise flow already makes.

**A deliberate limit, stated rather than silently decided: raising is automatic, sending is not.** Every other invoice in this codebase needs a human's click to actually email a client, matching the governing rule. Auto-raising a correctly computed draft the moment the client approves satisfies "raised against the payment terms" without also making this the first thing in the system that emails real money figures to a real client with nobody having looked at it. Flagged to the founder rather than assumed either way.

**Verified live**, all three paths, each via a real function or trigger call, none left standing: `raise_job_stage_worker_pay_invoice('JOB-TEST-WAPAY-3', 1)` and `(..., 2)` against its real Quote Pack (30%/70% of J$8,000 labour) came back J$2,400 and J$5,600, summing to the full labour price. The trigger itself, fired by a real `insert into stage_approvals`, in a rolled-back transaction, correctly raised against both an approved Kickoff Pack (`JOB-TEST-KICKOFF-1`, "Mobilization and assessment," J$1,300) and, separately, the no-pack default (`JOB-TEST-REALPHOTO`, a real job with no pack, J$2,500 then J$7,500 off J$10,000 labour, stage 3 correctly raising nothing). `pg_trigger` confirms it live and enabled on `stage_approvals`.

---

## 2026-09-02 · A second, separate invoice: what the client owes the worker, raised as a record only

**Founder's own instruction, direct: Yaadly raises two invoices per booked job, not one, paid separately, to different parties.** The agency fee invoice (20260901y) already existed: Yaadly's own 15%, billed to and paid by the client, to Yaadly. Nothing anywhere raised the second one, a record of what the client owes the worker, labour plus materials at cost. `20260901v`'s own comment already said the client pays the worker directly; this gives that arrangement paperwork instead of leaving it entirely informal.

**Not payment integration, and checked against CLAUDE.md 9 before building it.** No money moves through Yaadly on this invoice, no provider, no holding structure, no release condition changes: the client still pays the worker exactly as before, off-platform, on their own terms. It reuses the existing `invoices`/`invoice_lines` machinery and its existing guards (`invoice_line_price_guard`, `invoice_status_guard`) unchanged, admin-raised and admin-marked-paid exactly like the agency fee invoice already is. `payable_to` (`'yaadly' | 'worker'`, default `'yaadly'`) is the only new column, so every invoice raised before today keeps its existing meaning.

**Gated on the job actually being `complete`.** Raising a bill for work not yet finished and approved would be inviting a client to pay before the evidence exists to back it; `raise_job_worker_pay_invoice()` refuses otherwise. One per job, same discipline as the agency fee.

**A real mistake caught before it shipped, not after.** The rendered invoice document's footer prints `app_settings.invoice_pay_to`, Yaadly's own bank details, unconditionally, on every invoice, because until today every invoice was Yaadly's own. Left as is, a worker-pay invoice would have told a client to pay a bill for the worker's labour into Yaadly's bank account, the exact confusion this whole feature exists to prevent. `renderInvoice()` in `yaad-invoice` now branches on `payable_to`: a worker-pay invoice's footer says plainly that Yaadly is not the payee and does not hold the money, instead of showing bank details at all. Caught by reading the render function fully before wiring the desk button to it, not by testing the button and noticing the output looked wrong.

**Verified live.** `raise_job_worker_pay_invoice('JOB-TEST-WAPAY-3')` raised `INV-2026-0008`, J$9,500 (labour J$8,000 + materials J$1,500, no fee added), sent, and its rendered footer read back with the corrected worker-pay text, not Yaadly's bank details. `deno check` clean on `yaad-invoice`, deployed from disk with `verify_jwt` read first and left at its existing `true`.

**Not committed: `concierge/concierge.html`.** Already carried substantial unrelated uncommitted work before this session touched it (101 lines changed once combined). The new Job Invoices UI, two rows per job instead of one, is verified live in the local desk; `supabase/functions/yaad-invoice/index.ts` and the new migration are committed, `concierge.html` is left as a working-tree change for whoever owns the rest of it.

---

## 2026-09-02 · The same fix, reapplied on top of a second session's newer work; the portal's own stage display gets it too

**Two sessions on this branch at once, `sync_job_status()` changed under this one mid-task.** Minutes after `20260902g` (below) shipped, the live function had already moved on: a fee-paid-first gate (`if not fee_paid then new.status := 'awaiting_payment'; return new;`, ahead of what that migration's own file on disk shows) and `coalesce(final_stage_count, 1)` in place of `5 - 1`, neither of which this session wrote. The Quote Pack fallback `g` added had been silently dropped, reverted to reading `kickoff_packs` alone. Nobody's change was wrong on its own; they landed on top of each other. `20260902h` re-adds only the fallback, inside the structure actually live at the time (re-read via `pg_get_functiondef`, not assumed from the last file this session wrote), the fee-paid gate and its own default left exactly as found.

**`JOB-TEST-WAPAY-3` correctly went back to `awaiting_payment` after this**, not a regression: its agency fee invoice was never actually marked paid, and the new gate is a real, separate rule that has nothing to do with either pack. Confirmed the two fixes are independent before concluding that.

**The portal's own "Stage X of Y" never read either pack either**, a second, cosmetic instance of the same root cause: `stageCount` in `web/app/portal/(gated)/jobs/[id]/page.tsx` was `Math.max(job.stage, ...evidence stages seen, 1)`, "the highest stage number observed so far," coincidentally readable as a real total but never actually one. `web/lib/portal/journey.ts` gained `quotePackPaymentStages()`, the same shape as `packPaymentStages()` for the other document's `docs.payment_stages`. The page now tries an approved Kickoff Pack first, then an approved Quote Pack draft, same order and same reasoning as the trigger, so the portal's own number agrees with the one that actually decides completion. `currentPackStage`, the client's per-stage amount-due figure, was quietly reading kickoff-only too and is fixed the same way.

**Not committed.** `page.tsx` and `journey.ts` both already carried substantial uncommitted work from the other session before this edit (163 and 21 lines respectively once combined). Committing either now would sweep that work in under this session's name. `web`'s own `tsc --noEmit` is clean and the fix is verified live in the dev server; the two files are left as working-tree changes for whoever owns that other work to fold in or commit themselves.

---

## 2026-09-02 · The completion trigger reads a Quote Pack's stage count too, not only a Kickoff Pack's

**Found live while manually testing the worker journey end to end, join through paid, not caused by that test.** `sync_job_status()`'s completion check (`is_complete := stage > final_stage_count`) only ever looked up `final_stage_count` from an approved `kickoff_packs` row. `20260902c`/`d`, built the same day, added an entirely separate Quote Pack path (`quote_pack_drafts`) as an alternative to the Kickoff Pack, not a variant of it, so a job that goes through Quote Pack confirmation never gets a `kickoff_packs` row at all. The lookup came back null and silently fell back to the generic default of 4 remaining stages, whatever the Quote Pack itself had actually promised the client.

**Confirmed live, on a real job.** `JOB-TEST-WAPAY-3`'s approved Quote Pack promised 2 payment stages (`docs->'payment_stages'`, 30%/70%); the portal's own progress bar, reading a third, independent count, was showing "Stage 3 of 3"; the trigger, by its own math, would not have completed the job until stage 5. All three disagreed with each other, and the client's own evidence approvals on stages 1 and 2 correctly released each stage's money regardless, since stage-level release never depended on the total count. Only the job's final `complete` flip, and with it the worker's own "how were you paid" screen, was stuck behind the wrong number.

**Fix.** `sync_job_status()` now checks `kickoff_packs` first, exactly as before, and only if that comes back null, checks `quote_pack_drafts` for an approved row and reads its stage count from `docs->'payment_stages'` directly (a plain array, not nested under `payment_schedule` the way a Kickoff Pack stores it, the two documents were never the same shape). Since a job can only ever go through one of the two paths (`20260902d`'s own reasoning), the coalesce always lands on whichever document the job actually has, never a guess between them. A one-line `update jobs set updated_at = now()` for every job sitting on an already-approved Quote Pack draft was included in the same migration, so an already-stuck job like `JOB-TEST-WAPAY-3` didn't have to wait for an unrelated write to notice the fix.

**Verified live.** Before the fix: `JOB-TEST-WAPAY-3` at `status = 'in_progress'`, `stage = 3`. Applied `20260902g_completion_trigger_reads_quote_pack_stage_count_too.sql` directly to `leffyisvfvjwzilydlwf`. After: the same job, no further action taken on it, read back as `status = 'complete'`. Continued the manual test through to the worker actually recording `pay_method = 'bank_transfer'` via the real portal form, the one screen this whole path exists to reach.

---

## 2026-09-01 · A worker's text or voice reply, no photo attached, now produces a report the same way a photo does

**The second half of the founder's own instruction on the report pipeline** (the daily prompt is the entry above this one): a worker's reply to it, most days a voice note or a couple of words with nothing to photograph, had nowhere to go. `yaad-inbound` had no lane that read a worker's own freeform update as anything at all: it either matched one of the existing narrow lanes (confirming a drafted report, answering a client's specific comment, a Kickoff Pack code) or fell all the way through into the client job-intake pipeline, read as a stranger describing a brand new problem.

**No new pipeline. A door into the one that already existed.** `composeEvidenceReport` (`yaad-notify-client`) already reads `evidence.label` text for a stage, regardless of whether the row carries a photo, built that way originally so photo captions and voice-note transcripts could share one composer. `schedule_evidence_landed_notify()` already fires on any insert into `evidence`, not only one with `storage_path` set. The gap was never in the composing or the notifying, only in `yaad-inbound` never actually filing a text-only or voice-only update as an `evidence` row in the first place. Filing it is the whole change: a new lane, last in line inside the WhatsApp block (after every more specific worker and client lane has had first claim, before the client-intake fallback), that transcribes a voice note locally if there is one, resolves the phone to a worker with active jobs the same way the comment-reply lane already does, and inserts straight into `evidence` with `label` set to the text and every file-specific column left null, same shape the portal and concierge desk already render as "filed without an image." More than one active job asks for the job's code first, reusing the exact prompt and session shape the evidence-photo lane already uses for the same question.

**A real discrepancy found and resolved while verifying this, not caused by it.** Confirming that a text-only row would actually reach a client meant tracing what sets `jobs.status = 'evidence'`, since `due_evidence_landed_notifies()`'s own `should_notify` check depends on it. The trigger bound to `evidence` inserts, `trg_evidence_marks_awaiting_approval`, currently executes `poke_job_on_evidence_insert()`, a function that only touches `jobs.updated_at`, not status, which read at first like the status flip had quietly stopped working for everything, not only for a text-only row. Reading `20260831g_evidence_status_needed_one_authority.sql` resolved it: this is the documented, intentional fix for an earlier version of this same trigger that raced against `sync_job_status()` and lost. The poke's only job is to touch `jobs.updated_at` so `sync_job_status()`, a `BEFORE UPDATE` trigger on `jobs` itself, recomputes status from scratch, including a check for unapproved evidence against the stage being worked, a check that has never cared whether the evidence row carries a file. Confirmed live, in a rolled-back transaction: a text-only evidence row against a job at `status = 'in_progress'` correctly recomputed it to `status = 'evidence'` in the same statement.

**Verified live** through the layer that can be verified without a real signed WhatsApp message: `deno check` clean across every Edge Function, not only the one touched; the four Deno test files under `yaad-inbound/` (36 tests, including the ones that scan `index.ts`'s own source to confirm it imports `pickJobChoice`, `pickEvidenceItem` and the signature check rather than growing a second copy) all pass unchanged; a text-only `evidence` insert in a rolled-back transaction correctly scheduled an `evidence_landed_pending` timer and correctly flipped `jobs.status` to `evidence`. **Not verified**: `yaad-inbound` actually recognising a real, Twilio-signed WhatsApp message as this lane, which needs `TWILIO_AUTH_TOKEN`'s real signature, held by Twilio and the deployed function only, never by this session. Deployed to `leffyisvfvjwzilydlwf` with `--no-verify-jwt` preserved (`yaad-inbound`'s live setting was read first, not assumed, before deploying). See `RUNBOOK.md` for the one real WhatsApp message that finishes proving this.

---

## 2026-09-01 · A daily prompt asks every worker on a live job for a report, deliberately separate from the silence nudge

**Founder's instruction: every live job with a worker attached should be asked, once a day, for a follow-up report, voice note or a couple of words and pictures, and the report pipeline needs an agent behind it.** Two pieces; this entry is the daily prompt only. Built as its own table (`daily_checkin_log`) and its own cron-driven function (`yaad-daily-checkin`), rather than a shorter clock bolted onto `yaad-job-health`: that function watches for silence and only speaks after three days of it, this one asks every day regardless of silence, because the daily ask is the point, not a symptom of the job having gone quiet. Same reasoning `job_followups` already gave for not inventing a second shape where only the trigger differs.

**Always sends through an approved WhatsApp Content Template, never free text, no exceptions.** A daily, scheduled ping is business-initiated by definition every single time, so it cannot rely on the 24 hour customer-service window the reactive notifications sometimes get to use. `TWILIO_CONTENT_SID_DAILY_CHECKIN` is not set yet: submitting a template for Meta's approval is a Twilio console action, outside what a migration or an agent can do on its own, and outside what should be decided without the founder seeing the exact wording going out under her business's WhatsApp identity. Until that secret exists, the function says so plainly on every run rather than quietly sending nothing and reporting success; see `RUNBOOK.md` for the exact template text proposed and the steps to bring it live.

**A mistake made and caught before it could do any damage.** Verifying the auth path meant temporarily overwriting `app_settings.daily_checkin_cron_secret_sha256` with a known test value's hash to prove the authorised path responds correctly, then restoring it, matching the discipline this file already keeps for test data. The restore used a freshly generated random value instead of reading back the plaintext actually baked into the live `cron.job` command, which meant the hash and the secret the 21:00 UTC cron would actually present no longer matched, the same class of mistake `20260831zz`'s entry describes catching in a different function's secret handling. Caught immediately by re-checking `cron.job.command` directly rather than assuming the fix had worked, and corrected the same way `20260831m` reads an existing secret back out of a live function body: the real plaintext extracted from the cron job's own stored command, rehashed, and set back. Re-verified live afterward, 200 and the correct honest response, before moving on.

**Verified live**, the function deployed from disk and called with a wrong secret (403, `Not authorised`) and with the real one (200, the honest "not configured" response), rather than trusting the deploy output alone. The cron job itself was confirmed scheduled (`0 21 * * *`, active) directly against `cron.job`, not assumed from the migration having applied cleanly.

**What this does not do, on purpose.** A worker's reply to this prompt lands back through `yaad-inbound` exactly like any other WhatsApp message; wiring that reply, text or voice with no photo attached, into the same draft/confirm/relay loop evidence already gets through `composeEvidenceReport` is the next piece, not built here. Until it is, a worker who replies to this daily prompt with words alone has nowhere for that reply to go yet.

---

## 2026-09-01 · Arrival check-in captures one GPS point, checked against the job's parish, not its address

**Founder's instruction: the "I'm on site" tap should geotag, so it can show a worker is actually there.** Weighed directly against a promise not yet published anywhere live but already decided for the worker recruitment one-pager, "no tracking." Resolved as: build it, and when that one-pager copy is written, its wording carries the honest version, a one-time site check on tap, not continuous tracking, rather than the two standing side by side saying opposite things. Grepped first to confirm the promise is not yet in any shipped page or component; nothing published needed rewriting today.

**One reading, not a trail.** `log_arrival()` (`20260901za`) already refuses a second check-in on the same stage the same Jamaica-local day, so the ceiling is one coordinate per stage per day, structurally, not by convention. The browser's geolocation prompt is the consent moment; the check-in itself is never gated on saying yes to it, matching the in-person-confirmation checkbox already on the Approve button, evidence that strengthens a record without becoming a precondition for it.

**Checked against the job's `parish`, not `jobs.addr`.** The address column is free text nobody has ever geocoded, and geocoding it would mean sending a client's address to a mapping API, a new third party touching personal data, which CLAUDE.md reserves as Monique's own call, not a default an agent reaches for mid-build. `parish` is already a column on the job, and Jamaica has 14 of them; their town centroids are public, static geography, no vendor, no API call, no data leaving Postgres. `normalize_parish()` folds the live spelling variants (`St Andrew`, `St. Andrew`, `Saint Catherine`, `Portmore (St. Catherine)`) down to one key first, since `jobs.parish` has never had one canonical form. The flag (`arrival_log.far_from_site`, true past 30km) is a coarse sanity check, not pinpoint verification, and is never shown to the worker or the client, never refuses anything, and nothing reads it automatically: same shape as `worker_stall_history`, a fact for a human to glance at.

**A mistake made and caught in the same session.** The first pass tried to widen `log_arrival(text)` to four parameters with `CREATE OR REPLACE`, which cannot change a function's argument list; Postgres silently created a second, overloaded function instead of replacing the first, leaving the old one-argument version live with its old body. Caught by checking `pg_proc` directly rather than trusting the migration's own comment, fixed in the same session (`20260901zb`) by dropping the stale overload.

**Verified live**, not by reading the migration and assuming: a real job, three coordinate scenarios run through `log_arrival()` inside a transaction and rolled back rather than left as test rows, a point 1km from its parish centroid came back `far_from_site = false`, a point 127km away (Montego Bay coordinates against a St Andrew job) came back `true`, and a same-day, same-stage repeat call with no coordinates was correctly absorbed as `already_logged_today` without a second row. `web/`'s own typecheck and its 47-test suite both pass with `ArrivalCheckIn.tsx` converted to a client component. **Not yet deployed**: `web/` changes were verified locally; per the standing gap this file has already named once, nothing in this session's workflow deploys `web/` on its own, that step is still open.

---

## 2026-09-01 · No agency fee invoice is ever split by milestone, jobs or services

**Founder's instruction, replacing what the entry above logged as undesigned, same session: "It does not take 15% on milestone payments or payment terms, it calculates the full price of each job and service and takes the payment from there."** Applies to both mechanisms that raise Yaadly's own fee, never the worker's own pay in either case.

**Jobs.** `raise_job_stage_invoice()`, per-stage, evidence-gated, is dropped outright. `raise_job_agency_fee_invoice(p_job)` replaces it: one invoice per job, 15% of the whole accepted quote's labour price, raisable as soon as the job has an accepted quote (which, per `choose_worker()`, is also the same moment its Kickoff Pack is approved and the job starts), gated on nothing else. Identified in `invoices` by `job_id is not null and stage is null`. The Job Invoices desk view and the Agency Fees "ready to raise" list were both rewritten to one row per job instead of one row per stage. This is Yaadly's own cut only; the client's own payment schedule to the worker, stage by stage against evidence, in the job's Kickoff Pack, is untouched, a separate arrangement this decision has no opinion on.

**Services.** `raise_service_invoice()`'s deposit/balance split for anything £200 and over is retired; every one-off service now raises exactly one invoice for its full published price, before work starts, whatever the price. The monthly retainer branch was already one invoice and needed no change. The public payment terms page (`docs/payments.html`) and the concierge desk's own hint text next to the raise button, which named the £200 rule directly, were both updated to match, since the desk explicitly promises to follow what that page says. **The `docs/payments.html` change is not live yet**, GitHub Pages serves what's on `main`, and this session made no commit.

---

## 2026-09-01 · A new Agency Fees view, one place both kinds of Yaadly-only invoice live

**Founder's instruction: a channel for Yaadly's own invoices and fees, taken from every job and every service, so nothing owed to it is ever sitting uncollected on a screen she didn't happen to open.** Every row in `invoices` already meets that description; the worker's own pay never runs through this table on any path. What was missing was one place both kinds sat together: a job's per-stage fee (Job Invoices) and a drafted service or retainer (Invoices) were only ever visible on their own separate screen.

**Built as a new "Agency Fees" view, not a new invoice type or a new table.** Two parts: a flat "ready to raise, not yet raised" list across every booked job, the same eligibility check Job Invoices already runs per job, just pooled into one list instead of needing to open each job's own card to notice one was sitting ready; and a single list of every invoice ever raised, jobs and services mixed, source column naming which, same Preview / Mark as paid / Void actions as the other two views, same underlying `invoices.status` writes.

**Kept the upfront, paid-before-work-starts invoice type as its own separate piece, not folded into this.** The founder chose to build this completeness view first; the upfront invoice type from the entry above is unrelated in mechanism and still not designed.

---

## 2026-09-01 · An invoice gets paid to Yaadly before work starts, not designed yet

**Founder's instruction, the one thing kept from a pasted third-party proposal otherwise rejected outright** (see the same day's entry below): work should not begin until Yaadly has been paid first. Nothing else from that proposal, not the client-pays-worker-direct structure, not the VAT/GCT rules stated as fact, not the Airtable/Make.com stack, is adopted; the held-funds model in the YaadlyHub project files stands as written.

**Not built. Recorded here so it is not lost before payment integration lands** (still out of scope per §9 of this file's CLAUDE.md, no processor is wired up yet). One concrete tension already visible in the schema: `raise_job_stage_invoice()` requires `stage_approvals` for the stage being invoiced, evidence already approved, before it will raise anything, including stage 1, "Mobilisation." An invoice due before work starts has no evidence to approve yet, so stage 1 cannot be the same mechanism as stages 2 onward without a rethink, either a distinct upfront invoice type, or dropping the evidence gate specifically for stage 1. Which shape it takes is a design and payment-structure decision, not made here.

---

## 2026-09-01 · Job Invoices gets Preview, Mark as paid and Void; invoices gain a client company and PO number field

**Preview, Mark as paid and Void, matching the general Invoices view exactly.** A raised job-stage invoice previously showed status and nothing else, no way to see what the client was sent or to record it paid, without switching to the other view and finding it by id. Same mechanism throughout: Preview calls `yaad-invoice`'s existing `render` action into an iframe, nothing is sent; Mark as paid and Void write straight to `invoices.status` under RLS, same as the general view, no new function needed since the founder herself is the one clicking.

**`client_company` and `po_number` added to `invoices`, both optional and both never invented by the agent**, same rule already governing `client_name`/`client_email`: left blank unless the founder or the model's own prompt was actually given the value, no field to guess into. Job-stage invoices leave both blank; they only matter for a manually-drafted service invoice.

**Bank account details on the invoice document itself: open, waiting on the real numbers.** Founder's instruction, 1 Sep 2026: print account name, account number, sort code and IBAN on the invoice rather than only referencing "the account named in your engagement email," her reasoning being that a sort code and account number alone cannot move money out, only in. Not yet built: needs the real values, which do not belong typed into a committed migration, and a decision on where they live (`app_settings`, matching `invoice_issuer_*`, is the obvious fit but not yet done).

---

## 2026-09-01 · A worker's own quote became an AI-drafted, worker-edited pack, not four bare numbers

Founder's own correction, live: what she'd built to fire the moment a client accepts a quote was drafting the big 12-section Kickoff Pack, which was wrong. Her actual model: the moment a job goes live, a small agent drafts a short overview of the job (scope, what's included and excluded, a rough timeline, payment-stage structure), before any worker has looked at it. A worker considering the job opens the quote form to that draft instead of a blank one, edits it to their own terms, drops their price in, and what they send IS their quote, scope, timeline, stages and all, not just a labour figure. The big Kickoff Pack is untouched: it still fires at `kickoff_requested`, against whichever quote(s) the client has accepted by then, unchanged.

**Same two absolute rules as the big pack, carried over rather than re-derived:** never a price, cost or amount in anything the agent writes (Monique sells oversight judgement, not pricing, CLAUDE.md §5), never the word escrow (CLAUDE.md §6, §8). `yaad-quote-pack`'s SYSTEM prompt states both directly, and the same `guardrails.scan()` the big pack runs is run here before a draft is ever marked `ready`; a dirty draft simply never reaches `usableDraft()` in `QuotePanel.tsx`, which falls back to blank fields rather than showing a worker a document that leaked a price into a live quote.

**One draft per JOB, not per worker.** The scope and timeline come from the job intake, which does not change depending on who reads it. `quote_pack_drafts` (`20260901l`) mirrors `kickoff_drafts`' shape deliberately, much smaller: no evidence checklist, no risk register, none of the big pack's twelve sections. `yaad-quote-pack-check` (`20260901m`, cron, once a minute) polls `jobs` for exactly `open_jobs`' own definition of live (`open = true`, unassigned, `stage = 0`) and requests one draft per job with no draft yet.

**Founder's own follow-up correction, same session, after reading the first live draft:** "high level of what is included and excluded needs to be added to this, so each is stating exactly what they are doing and not doing." `included` and `excluded` (short bullet lists, high level, 2 to 5 items each) added to the SYSTEM prompt and the JSON shape (`20260901o` for storage, `20260901p` for `quotes_for_code()`), rendered in `QuotePanel.tsx` as two more editable text fields alongside scope, timeline and payment stages, and shown to the client on both the code-bearer quotes page and the signed-in portal job page. Every one of these fields is plain text, one item per line where it's a list: the founder's own description throughout was "editable text", never a structured form, so nothing here is stored as jsonb the worker would need a form to edit.

**Verified live, twice** (once before the included/excluded addition, once after): a real test job inserted, the cron caught it within its own one-minute interval, a draft came back `ready` in under 30 seconds both times, guardrail-clean, payment stage percentages summing to 100, included/excluded distinct from each other and concrete to the test job rather than generic. Test job and draft deleted immediately after each check; nothing fake stayed in the database.

**Left for later, not decided here:** no admin-desk view onto `quote_pack_drafts` yet, unlike the big pack's own Kickoff Drafts view. A dirty or stuck draft is currently only visible by querying the table directly (see RUNBOOK); a worker just sees blank fields instead of a starting point, which degrades gracefully but silently.

---

## 2026-09-01 · Job Invoices: Yaadly's own 15% fee, per stage, raised straight off the job's own Kickoff Pack

Follow-on to the mockup the founder reviewed the same evening, approved, and asked to be built for real. **Not the worker's cut.** Asked directly how the worker's own 12% gets collected, given the client pays the worker direct and Yaadly holds no money: the honest answer was that nothing collects it today, and would need either payment holding (out of scope until legal and insurance clear it) or a Founder Pro waiver. The founder chose the waiver, live: workers are not charged their fee until full launch, told so directly, closing that question without touching payment infrastructure. Only the client-side 15% Guarantee & Support fee is built here.

**Two schema changes to the existing `invoices` table, both from `20260826_invoicing.sql`, not a new system.** `currency` gains `'JMD'` alongside `'GBP'`: a marketplace job's own price is JMD, and converting it to GBP at raise time would mean inventing an FX rate and a rate policy this was not the place to decide. `invoices` gains `stage`, set only on a job-stage fee invoice, so "has this stage already been raised" is an exact check rather than a guess from free text.

**`raise_job_stage_invoice(job, stage)`** reads the job's own approved Kickoff Pack for that stage's name and `proportion_percent`, and the accepted quote's `labour_jmd`; the fee is `15% × stage% × labour_jmd`, `price_source = 'manual'` for the same reason `raise_service_invoice()`'s split halves used it - a policy-computed amount, not the model's. Refuses outright if the stage has not actually been approved (`stage_approvals`), matching the founder's own words: "it would be actioned once the evidence is received." Refuses again if that stage already has a live invoice, so raising twice cannot double-bill. `yaad-invoice`'s `money()` and `renderInvoice()` learned to read `invoices.currency` rather than assuming GBP everywhere, the same fix `raise_service_invoice` never needed since it never left GBP.

**The concierge desk's new Job Invoices view** mirrors the mockup card for card: one job, one strip of raised/ready/waiting totals, one row per stage with its release condition, its computed fee, and a "Raise & send" button that only lights up once that stage is genuinely approved. One click raises it, renders it, emails it, and marks it sent - the same one-click shape `raise_service_invoice` already had, extended to jobs.

Verified against real data, not assumed: `JOB-TEST-REAL-WA`'s own pack (30%/70%, labour J$5,000) raised stage 1 at exactly J$225 (15% of 30% of J$5,000); stage 2 correctly refused as not yet approved; raising stage 1 a second time correctly refused as already raised. Deployed to `concierge.yaadly.co.uk` the same evening, `concierge-deploy`'s own copy-then-deploy step, not skipped.

---

## 2026-09-01 · Raising a service invoice is one click: read the terms, price it, email it, mark it sent

Founder's own correction to the same evening's `raise_service_invoice()`: "if there is a hundred jobs, I have to manually process a hundred invoices, no." The RPC already read the correct price and term shape off the catalogue; what it still needed was to actually reach the client, so raising and sending stayed two separate manual steps.

**A third term shape, found the same conversation.** The published terms name three: under £200 in full, £200 and over split 50/50, and "the Oversight Retainer is billed monthly, and you can cancel with 30 days notice." `raise_service_invoice()` only knew the first two - a retainer would have gone through the £200 split branch and come out as a one-off deposit and balance, wrong for a recurring service. `service_catalogue.recurring` already says which items this applies to, so the fix asks nothing new of an admin: a recurring item now raises exactly one invoice for the period (current month by default), never split, its notes naming the cancellation term instead of the £200 rule.

**`yaad-invoice` gained a `send` action.** Renders the exact document the client will read (the same `renderInvoice()` the manual "render" action already used), emails it via Resend, and only on a successful send moves the invoice from `draft` to `sent` - a failed send leaves it a draft rather than claiming something went out that did not. Deliberately refused for an `ai`-drafted invoice: those still need a human to read the model's proposed lines first, the same gate the free-text drafting flow's own separate "mark sent" click already enforces. A policy-raised invoice carries no model-written content at all, only a catalogue name, a policy-computed amount, and a fixed sentence naming which published rule it follows, so there is nothing left for a human to check that raising it did not already guarantee.

**Concierge's "Raise, per your own published terms" became "Raise & send."** One click now: raise (one or two invoices, per the term shape) and send each by email, no separate step. Verified the raising half again against real catalogue prices after the retainer fix; the send half is built and deployed but not yet exercised end to end - it needs a real admin browser session with `RESEND_API_KEY` configured, which is the founder's to run, not something a database-level test can stand in for.

**A client-facing invoice list also reached the portal**, on the same `/portal/services/[id]` page as the payment terms themselves: every non-draft invoice for that service, its status, its notes stating which term applies, and its amount. RLS (`invoices_client_read`, already in the schema since `20260826_invoicing.sql`) already permitted a client to read their own non-draft invoices; nothing in the portal had ever queried the table before tonight.

---

## 2026-09-01 · The published payment terms reached the portal, and invoices finally follow the £200 split they promise

Founder's own instruction, live: "the payment terms should be on the portal once it is agreed and the invoices should be raised to align with this." The terms in question already exist, word for word, on `docs/payments.html` - bank transfer only, GBP/USD/CAD, account details never change mid-job, reports under £200 paid in full before work starts, £200 and over is half before and half on delivery. Two real gaps: a client who had actually booked a service could not read any of this inside their own portal room, and nothing in `yaad-invoice`'s drafting flow knew the £200 rule existed - an admin types free text, the AI proposes catalogue lines, and the split only happens if the admin remembers to type two invoices.

**Portal**: `/portal/services/[id]/page.tsx` gained the same section verbatim, with a comment noting it has to be kept in sync with the marketing page by hand - there is no shared source between the static site and this app to drift-check automatically.

**Invoicing**: `raise_service_invoice()` (`20260901t`), a second door onto the existing `invoices`/`invoice_lines` tables from `20260826_invoicing.sql`, not a new system. Reads the real price from `service_catalogue`, never a typed number, and raises one invoice under £200 (full amount, `catalogue_full`, due before work starts) or two at £200 and over (50/50, `price_source = 'manual'` for the halves - a policy-computed amount is genuinely a human decision, not the model's, the same reasoning the schema already uses that word for). Each invoice's own `notes` states which published rule it follows. Wired into the concierge desk's Invoices view as "Raise, per your own published terms," a service dropdown priced straight from the catalogue, sitting beside the existing free-text drafting card rather than replacing it.

Verified against real catalogue prices, not assumed: Eyes On It (£125, under £200) raised one invoice at the full catalogue price; Deposit Protection Check (£249) raised two, £124.50 each, summing back to the original £249 exactly.

---

## 2026-09-01 · The Quote Kickoff Pack (small pack) gained the review step it was built without

Founder's own question, live, after the WhatsApp test: "I never saw when the small pack was issued for review." True, and by design as first built: a guardrail-clean `quote_pack_drafts` row at `status = 'ready'` went straight into a worker's own quote form (`QuotePanel.tsx`) the moment it finished drafting, no human step anywhere - unlike the big Kickoff Pack, which has exactly this review step (`yaad-kickoff-check`'s own Phase 2, `link_kickoff_draft_to_job()` as the manual door). Founder's own instruction: build the same review step here.

Mirrored the big pack's shape closely (`20260901r`). `quote_pack_drafts` gains `approved`, `approved_by`, `approved_at`. `yaad-quote-pack-check` gains a Phase 2: a finished, guardrail-clean draft is approved automatically, system-attributed, same wording as the big pack; a dirty one is left at `ready`, visible in a new concierge desk view, Quote Pack Drafts, for a human to clear or replace. `approve_quote_pack_draft()` is the manual door, admin only, refuses outright on any guardrail flag exactly like `link_kickoff_draft_to_job()` - no override, ever - and attributes the approval to the actual signed-in admin's email rather than a generic string.

**RLS is the real gate, not the page.** The read policy that let a worker see a draft at all previously had no status filter beyond the job qualifying; it now requires `status = 'approved'`, so an unreviewed draft is invisible to a worker's own query regardless of what `QuotePanel.tsx`'s client-side `usableDraft()` checks - which was updated to match, but is a courtesy now, not the boundary, the same "RLS is the real protection" principle CLAUDE.md states everywhere else. `web/app/jobs/page.tsx`'s own draft query and per-job dedup preference updated to prefer `approved` over `ready` for the same reason.

Verified live: manually triggered `yaad-quote-pack-check` against the real backlog of drafts sitting from before this migration - two approved cleanly, correctly attributed to the system, zero held, zero failures.

---

## 2026-09-01 · The first genuinely real WhatsApp test of the night found three more bugs no amount of simulated testing had caught

Everything above this point in tonight's Kickoff Pack rework was verified by calling the real database functions directly - real logic, real data, but never a message actually relayed by Twilio. The founder gave her own real WhatsApp number and ran the client and worker journeys for real. Three real bugs surfaced in the first four messages, all in `yaad-inbound`, none of them visible from the database side.

**1. Block order.** The new Kickoff Pack confirm block sat AFTER the existing booking block, and both recognise the identical reply (a bare job code). Her first real message, confirming her side for the first time, got intercepted by the booking block instead and correctly refused ("choose unlocks once this worker's Kickoff Pack is confirmed by both sides") - her own confirmation was never recorded. Fixed by moving the confirm block first.

**2. An ORM footgun, not a logic bug.** The candidate query used PostgREST's embedded-resource filter (`.select("...,jobs!inner(...)")`.`.not("jobs.client_phone", "is", null)`), which resolved in a shape the code's own `.map()` did not expect and silently produced zero matches. Nothing errored; the reply just fell through to the booking block again, with the exact same wrong refusal. Rewritten as plain queries joined by hand in JS, the same style already used successfully everywhere else in this file - this was the only block that had reached for an embed.

**3. Already-confirmed packs were still "candidates."** Once both sides had genuinely confirmed, the identical reply kept matching the confirm block anyway (it never checked whether there was anything left to confirm), so `agree_kickoff_pack_via_whatsapp()` correctly refused a third time ("No Kickoff Pack on this job is waiting on your confirmation") and the reply still never reached booking. Fixed by excluding, per side, any pack this phone has already confirmed at its current revision from the candidate list.

**Verified for real after each fix**, not assumed: her actual replies moved through confirm (client), book, and confirm (worker, on a second test job, same physical phone, correctly attributed as a different party) - all three read back from the database as genuinely recorded, not simulated.

**A fourth issue, copy not logic, caught by the founder mid-test:** the "both sides have signed off" message told the client nothing had gone wrong but never said what to do next - choosing is a second, separate reply, not automatic. Fixed: the client's copy of that message now says so directly; the worker's does not, since choosing is the client's action alone.

---

## 2026-09-01 · kickoff_packs.quote_id made unique, after a manual trigger raced pg_cron and produced two packs for one quote

Setting up a real test for the founder to run over her own WhatsApp number, manually invoking `yaad-kickoff-check` to speed a draft along landed in the same window as pg_cron's own minute-tick. Phase 2 reads every `ready`, unlinked draft once per invocation and inserts a pack for each; two invocations close enough together both read before either writes, so the same quote got two `kickoff_packs` rows with two different confirm codes. A real risk beyond testing, since the concierge desk can also trigger this by hand. Fixed with `unique (quote_id)` (`20260901q`): one quote, one pack, ever - revisions update `rev` in place on the same row, they never insert a second one, so the constraint costs nothing real and turns the race into an already-handled insert failure instead of a silent duplicate.

---

## 2026-09-01 · The WhatsApp booking door itself was broken by the Kickoff Pack rework, found running the exact test it was built for

Founder's own instruction: test the client and worker journey for real, over WhatsApp, not simulated through the portal. Doing that surfaced two more real, load-bearing defects, both direct fallout from the quote/pack rework earlier the same night (`20260901f`), neither caught by that migration's own testing because that testing used the portal's RPCs, never the WhatsApp doors.

**`sync_job_status()` didn't know `kickoff_requested` existed.** Its own "does this job have a live quote" check only ever looked for `status = 'submitted'`; a quote that had moved to `kickoff_requested` made the job read `open_for_quotes` again, as if nothing had happened. Not just a wrong label: `choose_worker_via_whatsapp()`'s own candidate query in `yaad-inbound` filters `jobs.status = 'quoted'` directly, so every job actually mid-flow through the new process - which is every job with a requested Kickoff Pack - was invisible to the WhatsApp booking door. Fixed (`20260901l`) to count `kickoff_requested` alongside `submitted`.

**`choose_worker_via_whatsapp()` itself was left checking two things that can no longer both be true at once.** It only searched for a quote at `status = 'submitted'` (the same fault as above, in its own query this time) and its own worker-readiness pre-check still read and wrote `scope_agreements`, the mechanism `_do_choose_worker()` stopped consulting the moment its gate became the Kickoff Pack's `both_confirmed_at`. The prior migration's own comment predicted a stale pre-check here would "fail open... caught one call further in"; live testing showed it does not - it returns `PENDING_WORKER_SCOPE` and never reaches `_do_choose_worker()` at all, a confident wrong answer rather than an honest refusal. Rebuilt (`20260901m`) to search `submitted` or `kickoff_requested` and defer entirely to `_do_choose_worker()` for whether booking is allowed, dropping the dead pre-check and the `PENDING_WORKER_SCOPE` branch in `yaad-inbound` along with it.

**Verified afterward against a real booking, start to finish, through the WhatsApp-native doors specifically** (`choose_worker_via_whatsapp`, `approve_stage_via_whatsapp`), not their portal siblings tested earlier: quote requested, Kickoff Pack drafted and confirmed by both sides, booked by phone-matched reply, arrival logged, evidence filed, stage approved by phone-matched reply. All correct. The one link still not exercised through a literal signed Twilio webhook, same caveat as the Kickoff Pack confirm work above.

---

## 2026-09-01 · Confirming a Kickoff Pack moved off the portal onto WhatsApp, for the worker

Founder's own correction, live, testing the journey end to end after the quote/pack rework below: "this need to go through whatsapp, it should not be done through the portal." CLAUDE.md already said so, written before that same night: the worker's whole surface is WhatsApp by design, and the web is only for onboarding and file upload. The Kickoff Pack rework a few hours earlier gave the worker a "Confirm as the worker" portal button anyway, exactly the surface that rule exists to prevent.

`agree_kickoff_pack()`'s core (record the agreement, check for both, stamp `both_confirmed_at`) moved into `_do_agree_kickoff_pack(pack_id, side, email)`, shared by the portal path and a new one, `agree_kickoff_pack_via_whatsapp(p_job, p_phone)` (`20260901i`, corrected twice more the same night, below). `yaad-inbound` gained a new job-code-reply lane, same family as `choose_worker_via_whatsapp` and `approve_stage_via_whatsapp`: either side's phone, matched against every job with an approved pack still waiting on that side, replies with the code to confirm theirs. `yaad-notify-client`'s `kickoff_pack_ready` message now says so directly instead of only linking to the portal. The worker-facing "Confirm as the worker" button is gone from both `/pack` and the job room's own early worker view; both now read "reply to Yaadly's WhatsApp message with `<job id>`" instead. The client keeps their portal button, since the client's primary channel was never in question.

**Two real bugs in the new function, both caught live testing it, neither from guessing:**

1. `agree_kickoff_pack_via_whatsapp(p_phone)`'s own `RETURNS TABLE(..., job_id text, pack_id text)` column names silently became variables inside the function body, PL/pgSQL's own well-known trap, and collided with a CTE returning columns of the identical bare names - "column reference pack_id is ambiguous" the first time it actually ran. Fixed (`20260901j`) by qualifying every CTE column reference.

2. Worse, and only found because the same worker genuinely had two Kickoff Packs pending at once by the time this got tested (one real, left over from before tonight's rework; one from the test itself): the function took only a phone number and searched globally for "the one pending pack", so replying with the EXACT correct code for one job still refused with "more than one is waiting", discarding the one piece of information - which job the reply actually named - that `yaad-inbound` had already worked out via `matchApprovingJob()` one line earlier. Every other WhatsApp door in this codebase takes the job it already matched as a parameter; this one tried to re-derive it from the phone alone. Rebuilt (`20260901k`) to the same shape as `choose_worker_via_whatsapp(p_job, p_phone)`: takes the job, checks only whether THIS job has a pending confirmation for this phone, refuses only then. Verified afterward against the exact failing case: a worker with two genuinely pending packs, confirming the named one correctly while the other stays untouched.

**Not verified: a real, Twilio-signature-signed webhook hitting this in production.** Every check above went through the underlying database function directly, with the real test accounts, and a careful re-read of the new `yaad-inbound` matching code. The Twilio signature check (`checkTwilioSignature`, reading `TWILIO_AUTH_TOKEN`) is real and cannot be faked without that secret, which correctly never leaves Supabase's own environment. Confirming this from an actual WhatsApp message needs a phone number really on file for a test account, or the founder testing it from her own number against a real job.

---

## 2026-09-01 · Accepting a quote no longer books it. A client can accept more than one, compare Kickoff Packs, and choose

Found live, testing the client journey end to end: pressing Accept on a quote set `jobs.worker_email` and lit the job up as `in_progress` immediately, before any scope of work or payment terms existed. The Kickoff Pack that carries both was drafted afterward, tied only to the job, with nothing pointing a client at it and nothing gating anything on it. Founder's own correction, live: "Accepting the job should not accept the job because there is no scope of work agreed and there's no payment terms agreed so it never should have gone on the portal," followed by the actual model wanted: "a client can accept many quotes and let the worker showcase their split with the kickoff pack and the client will then have the final say on what quote they will choose as they now have documents against a quote."

**The unit that gets confirmed moved from the job to the quote**, because more than one quote can now be live for the same job at once. `job_quotes` gained a status between `submitted` and `accepted`: `kickoff_requested`. `kickoff_drafts` and `kickoff_packs` gained `quote_id`, so a job can carry more than one draft or pack in flight, one per worker whose price the client wants to see terms against (`20260901f`).

**`accept_quote_as_me()` is retired**, replaced by `request_kickoff_as_me(p_quote)`: it flips a submitted quote to `kickoff_requested` and touches nothing on `jobs`. It can be called again for a different quote on the same job, on purpose. `yaad-kickoff-check`'s first phase now polls `job_quotes.status = 'kickoff_requested'` instead of "a job with a chosen worker," one request per quote, and `yaad-kickoff` accepts an optional `quoteId` to stamp the draft.

**`_do_choose_worker()`, the one real booking path under `choose_worker()` and `choose_worker_via_whatsapp()`, is regated onto the Kickoff Pack instead of `scope_agreements`.** `scope_agreements` was already known not to fit more than one live quote, since it carries one row per side per job, not per quote; `choose_worker_via_whatsapp()` already refuses outright the moment more than one quote is open, for exactly that reason. The Kickoff Pack can tell two workers' agreements apart, because it is drafted and confirmed per `quote_id`. The gate is now: this quote's Kickoff Pack has `both_confirmed_at` set. `scope_agreements` and `agreeScope()` are left in the schema, unread by the gate; the portal's separate scope tick-box UI is fully retired in this pass (`/portal/jobs/[id]` no longer renders it), rather than left standing next to a mechanism it no longer feeds.

**Four downstream places assumed booking happened before or alongside a Kickoff Pack, and broke the moment that stopped being true (`20260901g`):** `agree_kickoff_pack()` matched the worker side of a confirmation against `jobs.worker_email`, blank pre-booking, so a worker could never confirm their own pack; fixed to check the pack's own `quote_id` first. RLS on `jobs` ("workers can read their own jobs"), `kickoff_packs` ("parties read approved packs") and `kickoff_pack_agreements` all keyed off `jobs.worker_email` too, so an unbooked quoting worker could not even load the job row their own portal pages depend on; all three extended to also match via `job_quotes.worker_user`. `job_quotes`' own client-read policy only allowed `submitted` and `accepted`, so a client's own requested quote would have vanished from their own portal view the moment it moved to `kickoff_requested`. `notify_worker_kickoff_pack_ready()` and `yaad-notify-client`'s `kickoff_pack_ready` handling resolved the worker and picked "whichever pack last updated" by job id alone, both wrong once two packs can be in flight on one job; both now carry and read `quoteId`.

**Portal UI**: `/portal/jobs/[id]` gained an early, separate render for a worker with a live quote but no booking, since the rest of that page assumes a booked worker everywhere it says `role === "worker"` (arrival log, materials store) and none of that applies yet; rather than audit every section for a state that could not previously exist, an unbooked quoting worker gets a small focused view of their own quote and their own Kickoff Pack instead. The client's own quote cards gained a "Get a Kickoff Pack for this price" button per submitted quote, pack status once requested, and "Choose X for the job" once that pack is confirmed by both sides. `/pack` and `/pack/[doc]` take an optional `?quote=` and, failing that, match the confirm code from the link, rather than assuming the job's latest-updated pack is the right one. `AcceptPanel.tsx` and `/portal/join` updated to call `request_kickoff_as_me` and describe what it actually does.

**Separately, the same live test caught why a kickoff draft can fail at all**: `yaad-kickoff`'s `draftPart()` had no diagnostic logging on a parse failure, same class of silent failure fixed in `yaad-inbound` two entries below. `console.error` added, logging `finish_reason` and the first 1200 characters of the raw model response. The specific draft that prompted this (`JOB-WA-1788258939807`, part A1, "did not return usable JSON") had already self-healed on `yaad-kickoff-check`'s own retry three minutes later, so the exact cause of that one instance is still unconfirmed, but the next failure will say why.

**Caught running the actual test this was all built for, same evening:** `request_kickoff_as_me()` reported success and returned the job id on every call, but `job_quotes.status` never actually moved to `kickoff_requested`. `job_quotes_touch()` (a `BEFORE UPDATE` trigger) exists specifically so a non-admin caller can only move a quote into an admin-only status through a transaction-local flag, `yaadly.choosing`, that `choose_worker()` and the retired `accept_quote_as_me()` both set around their own updates. `request_kickoff_as_me()`, written fresh in `20260901f`, never set it, so every call silently reverted itself. Live proof this class of bug is real, not theoretical: the RUNBOOK already documented the identical failure shape happening to `accept_quote_as_me()` before 31 Aug 2026. Fixed in `20260901h`, which also resets any quote caught mid-flight by the bug. Verified afterward end to end against production, live, no shortcuts: two real quotes on a fresh test job, both accepted, both packs drafted in parallel and auto-issued clean within about ninety seconds, both confirmed by the correct worker on each side (proving the pre-booking worker-match fix above), one chosen, the other auto-declined, the job booked correctly.

**Closed the same evening, founder asked directly:** a worker with a live, unbooked quote now appears on their own worker portal job list. `/portal/worker` and the door page both widen their worker filter with a direct `job_quotes` lookup (`worker_user = auth.uid()`) rather than `jobs.worker_email` alone, so the WhatsApp link is a shortcut into the job, not the only way in. The existing `WORKER_STATUS['quoted']` copy, "You have quoted, waiting on the client," needed no change; it was already honest about this exact state.

---

## 2026-09-01 · Police record check and the trial job dropped from worker vetting, founder correction

Founder read the marketing copy on `docs/marketplace.html` describing worker vetting and said plainly: "remove trial jobs, i dont do that" and "i no longer ask for [the police check]... like i remove that from my site and live site doesnt ask for that." Neither the JCF police record check nor an independent reviewer attending a worker's first job at Yaadly's cost is something the business actually does, even though both were built into the applicant form, the Worker Guidelines, the AI vetting review and the public marketing pages.

**Removed everywhere it was asked for or claimed.** The `police_check` document type and its upload path, the `policeStatus` field and the "8 · Trial job" step are gone from `JoinFlow.tsx`; the ON_THE_BOARD top tier text now reads "until your telephoned references are done" rather than naming a police check. `yaad-vetting-upload` no longer accepts a `police_check` document or writes `police_status`. `yaad-vetting-review` dropped `police_check` from `CORE_DOCS` and `DOC_LABEL`, the police check age computation, and the line telling the desk an applicant said they had no check yet. The concierge desk's vetting queue lost its "Police check" column, synced to `concierge-deploy`. The public worker profile no longer shows a "no current JCF police record check" warning. `docs/marketplace.html`, `docs/faq.html`, `preview/index.html`, the specs and `web/lib/hubspotConfig.ts`'s certification dropdown all had the same claim removed.

**The £500, occupied home, and keys threshold stays, gated on references alone.** That threshold is a real, database enforced gate (`enforce_vetted_worker_on_quote`, 20260831d), not marketing copy, and the founder did not ask to remove it, only the police check half of what unlocks it. A Probation worker is still refused those jobs until the three referees are telephoned; the trigger itself never actually checked `police_status`, only the desk's own decision to flip `vetting_state` to `verified` referenced it, in RUNBOOK.md and in the worker's own copy of what was expected of them.

**Worker Guidelines moved to v1.4** because the removed text was a signed legal document, not just a web page. `legal-copy.json`'s section 2 lost the police check callout and the trial job paragraph, gained a v1.4 amendment note saying plainly that both are removed because neither is part of how Yaadly actually vets a worker. `app_settings.worker_guidelines_version` bumped to match in a new migration (`20260901i`), because `current_doc_version('worker_guidelines')` reads that table, not the JSON file, and the job matching gate in `yaad_match` compares a worker's signed version against it exactly: leaving it at '1.3' would have failed every worker who signed the corrected text and passed every worker who signed the one just withdrawn.

**Not yet deployed.** The two edited edge functions (`yaad-vetting-upload`, `yaad-vetting-review`) need `supabase functions deploy` from disk, and the new migration needs applying, before any of this is live. Nothing in this pass touched Persona identity verification or the referee phone calls, those were not part of what the founder said was wrong.

---

## 2026-09-01 · "Book" renamed to "Accept this price", both places a client chooses a quote

Founder's own point, live, looking at the actual quotes page: "Book Test Worker (SEED)" reads as final agreement, and at that point in the journey there is no written scope of work on screen for either side to have actually read, only a price. Accepting a quote settles the price and books the worker provisionally; the Kickoff Pack that follows, confirmed separately by both sides, is where the real scope and payment terms are agreed. The button read as the whole thing when it is only the first half.

Changed in both places a client can do this: `AcceptPanel.tsx` (the no-account `/jobs/[id]/quotes` page) and the portal's own quote list (`/portal/jobs/[id]`). Button text changed from "Book X" to "Accept X's price"; a new line under each button states plainly that this is provisional and the Kickoff Pack is the actual agreement. No RPC or database behaviour changed, copy only: `accept_quote_as_me()` and `choose_worker()` already do exactly what the new copy describes, the old copy was the only thing overstating it.

---

## 2026-09-01 · A client got two different kinds of silence on WhatsApp, both real, found live against Twilio's own logs

Two separate real messages from the founder's phone got no useful reply. Neither was diagnosable from this project's own logs at the time, which is itself the first finding.

**Confirmed via Twilio's own message log (Monitor → Logs → Messaging), not assumed:** the first failure has an inbound message received with no outbound reply anywhere near it, ever, where every other exchange that day shows a reply within seconds. `yaad-inbound`'s outermost catch block returned a JSON 500 on any uncaught exception, and only ever called `root.recordError(e)`, which lands on the trace span and nowhere in `function_logs`. A client whose message hit that branch got silence, and nothing in this project said why. `console.error` added alongside the trace record; the actual exception for this specific incident was never captured, since the logging did not exist yet when it happened, but the next one will be.

**The second failure, on a fresh message minutes later, was root-caused directly from the new logging.** `readTheJob()`'s call to MiniMax carried `max_tokens: 1100`, the smallest budget of any model call in this codebase (every sibling call is 1200 or higher; `yaad-completion` is 8000). This model reasons inside a `<think>` block before writing its JSON answer, and that reasoning counts against the same token budget. Caught live: `readTheJob: no JSON object in model content: <think>\nThe client needs a plumber... I have enough information here:\n- What the work is:\n` — the response was cut off mid-thought, no closing `</think>` tag ever written, so `stripNoise()`'s regex could not strip it and `extractJson()` found no `{` to start from. `readTheJob()` correctly returned `null` rather than throwing, so the client did not get silence this time, they got the hardcoded generic opener ("Tell me what needs doing, which parish...") instead of an answer to what they had already said twice. Raised `max_tokens` to 2500.

Both fixes deployed and verified against the actual deployed source before either was trusted (`deno check`, then `supabase functions deploy yaad-inbound`, then re-read the live logs).

---

## 2026-09-01 · The Kickoff Pack gains client responsibilities, an itemised cost breakdown and a legal placeholders section

Founder supplied a reference quote template from another platform and asked for the useful shape adopted, explicitly not the whole thing: it used the word "escrow" repeatedly, a hard-banned term in this codebase (`guardrails.ts`/`guardrails.py`, "never say escrow, say held safely with a licensed payment provider"), and several sections read as real contract terms (warranty length, dispute process, who pulls permits) that belong to the founder and her solicitor, not to an automated document. Declined those two pieces explicitly before building anything, confirmed with her, then built the parts that were genuinely missing.

**`client_responsibilities` added to the model's own `scope_of_works` schema** in `yaad-kickoff`'s `SYSTEM` prompt, same quality bar as the existing included/excluded items: concrete to the property, never "cooperate with the contractor". Model-generated on purpose, since what a client needs to do differs job to job the same way the scope itself does.

**`cost_breakdown` and `terms_placeholders` are never asked of the model, added in code after the four-part draft merges.** `cost_breakdown` is built directly from `scope_of_works.included`, one row per scope item, materials/labour/total all blank: this keeps the row names identical to the scope the client is actually agreeing to and never risks a second prompt call answering rule 1 ("never price") differently under load. `terms_placeholders` is fixed text naming exactly what is still open (warranty, permits, dispute process, what happens if a hidden defect is found), each line explicit that it is not decided by Yaadly and needs the founder's solicitor before it becomes standard wording, same "question for an adviser, not an asserted answer" pattern this repository already uses for compliance items elsewhere.

**Portal rendering**: `packDocs.tsx` gained a "What you need to do" field on the scope page, a cost breakdown table on the payment page, and a new tenth pack page, "Terms to confirm". `concierge.html`'s `KICK_DOCS` list, which this file's own header comment says must name the same things the same way as the client view, updated to match. Both `yaad-kickoff` and the web app redeployed; `npm run typecheck` and `npm test` (47/47) both clean before either deploy.

---

## 2026-09-01 · Continuing the WhatsApp test: two real bugs, a real gap, and one wrong feature built then correctly reverted within the hour

Same founder-directed session as the entry below, continued: two full real conversations sent from her own phone, second one deliberately overlapping her first job's 12-hour thread window to test the boundary.

**A second, unrelated job sent within 12 hours of the first read back the first job's summary and never started gathering the second at all.** `const continuing` in `yaad-inbound/index.ts` matched purely on `channel + from_addr` within the window, with no regard for whether the prior thread had already finished. A reply about an already-done job (approving a stage, answering a comment) is intercepted by its own lane well before this check runs, so nothing legitimate needed the old behaviour. Fixed by also requiring `prior.stage !== "done"`; a done thread now starts fresh rather than swallowing whatever comes next. Found live because the founder's second real message, about an electrician, came back as a stale plumber-job summary.

**Deploying that fix broke every continuing reply outright, caught and fixed within minutes of shipping.** The same pass added `access_contact` to the row `yaad-inbound` writes (see entry below), written as `s(card?.access_note) || null`. `jobs.access_contact` is `NOT NULL DEFAULT ''`; the `|| null` sent an actual NULL over the wire the moment `card?.access_note` was empty, and every `UPDATE` on a continuing thread failed with Postgres `23502`, silently from the client's side: no reply at all. Fixed to `s(card?.access_note)`, which already returns `""`. Worth naming plainly: this was this session's own bug, in code deployed minutes earlier, caught by the founder saying "got no response back" and confirmed from `edge_logs` (`response.headers.proxy_status: "PostgREST; error=23502"`), not by any test suite.

**The model's own confirmation read missed an unambiguous "yes" once, live, and there was nothing under it to catch that.** `card?.confirmed` is one model call's classification of one message; the founder's actual final message, "Monique Sewell-Bennett, [email]. Yes, that's everything, go ahead.", came back `confirmed: false`. The job row still wrote (the row write is unconditional every turn), so it looked finished in the database while the conversation logic still thought it was waiting, and the client got a bare thank-you with no portal link, no explanation. Added a deterministic word-match backstop (`yes`, `go ahead`, `correct`, `confirmed`, and similar, tested against that turn's own raw text only) that can only ever add a `true` the model missed, never override one it read correctly, same shape `wantsOut()` already uses for "cancel". Separately, and independently worth doing: the "confirming" stage reply used to just ask an open question and hope the answer was recognisable. Founder's own point: a client should be told what to do, not left to guess the right words. The reply now always ends with an explicit "Reply yes if that is right, or just tell me what to change," appended in code so it is consistent every time rather than however the model happened to phrase the read-back.

**Also found and fixed while diagnosing the above: `readTheJob()`'s failure paths were invisible.** Every one of its `return null` branches (HTTP error, unparseable body, no JSON object in the model's content, malformed JSON) only ever called `sp.recordError()`, which lands on the trace span and does not appear in `function_logs`. A client who fell through to the hardcoded generic opener left no console trail at all explaining why. `console.error` added alongside the span record on every one of those paths. The generic fallback's own wording was also tightened at the founder's request, from "whereabouts the property is" to "which parish the property is in", matching the model prompt's own field name rather than a vaguer phrase, since parish is the literal column matching runs on.

**`client_go_live()` never opened a returning client's later job, and the existing recovery button couldn't reach it either, same root cause.** Full account: `sync_job_status()` sets a not-yet-open job to `'draft'`, not `'awaiting_client_setup'`, the moment the client is already cleared (signed guidelines on file from an earlier job). `client_go_live()`'s `WHERE` clause only ever matched `'awaiting_client_setup'`. A returning client's second job therefore reached `open = false, status = 'draft', client_email` correctly bound, and stayed there permanently: correct by every individual rule, invisible to every worker, and the founder's own "stuck job" recovery button (`web/app/portal/golive-actions.ts`, built 28 Aug for a different stuck case, materials store nominated out of order) checks the same `'awaiting_client_setup'` status so it could not reach this one either. Fixed by matching both statuses a not-yet-open, cleared client's job can honestly carry (`20260901b_client_go_live_missed_a_returning_clients_second_job.sql`). Found live: the founder's own second job, on a number that had already completed the whole flow once earlier the same session.

**A client-side "edit your description" feature was built, shipped, and correctly reverted within the hour, by the founder, on sight.** Asked for a way for a client to correct what a worker will read before the job goes live (`BoardPreview.tsx` already showed it, read only, with nothing to fix it if it was wrong), built as `edit_job_description()` in Postgres plus a server action and a component, same shape as `nominate_materials_store()`. Wrong the moment it shipped: `jobs.descr` is not a draft, it is the record, the raw transcript, "In their own words," and the system's own working notes on the job (whether an email is proven yet). The founder caught it immediately, on the live page, seeing the edit form pre-filled with exactly that transcript: "dont allow them to edit this, it is the actual log we need." Reverted within the hour: `edit_job_description()`'s grants revoked from every client-facing role (`postgres` and `service_role` only, confirmed directly against `information_schema.routine_privileges` rather than assumed), the component and server action deleted, the page reverted, redeployed. The function definition itself is kept, not dropped, so a later reader of the migrations in order sees what was tried and why it stopped, rather than the attempt vanishing. If an editable summary is wanted later, it is a new column and a new function, never this one re-granted.

Test data added this round, same conventions as the entry below: job `JOB-WA-1788224177580` (electrician, St Andrew), same founder phone number and email throughout.

---

## 2026-09-01 · A real end-to-end test of the client and worker journeys, three findings fixed

Run at the founder's request: post a job through the web wizard, and through a simulated WhatsApp conversation, and follow each as far as the system's real scope goes (no live payment integration exists yet, so that stopped at the record-keeping fields, not a transfer).

**The web wizard's guest flow never told the client how to finish.** `web/app/jobs/new/PostJob.tsx`'s "Send my job" step calls `yaad-enquiry`, built for the marketing site's plain contact form, which writes to the `enquiries` table with no `job_id` column at all. The job itself stays at `awaiting_client_setup` with `client_name`/`client_email`/`client_phone` blank, by design (`yaad-post-job`'s draft mode writes an anonymous row on purpose, so that possession of an email is never mistaken for proof of it). The completion mechanism, `/portal/join?job=…&code=…`, already existed and already worked, verified directly: `claim_code_as_me()` binds `client_email` to the job the moment the client verifies. What was missing was simpler than it looked: the confirmation screen never showed the client that link or their `portal_code`, which the draft response already returns and always has. Fixed by surfacing both on the "Job received" screen and folding the link into the enquiry message, so it also reaches the receipt email and the desk copy. No account-creation logic changed; this is presentation of what already existed.

**The concierge desk's own safety net for this exact situation was reading a dead column, or so it appeared at the time.** `concierge/concierge.html`'s "Built a job, never signed" (Intake) panel filtered on `client_user is null`. Grepped clean across every migration and function in the repository at the time: nothing wrote it. That was true of the repository and false of the live database, corrected further down this same entry once the reason surfaced. Switched the filter (three call sites, plus the badge count and the overview health check) to `status = 'awaiting_client_setup'`, which is the column `client_go_live()` itself checks before it will open a job, and which turned out to be the better choice anyway: it is versioned, actively maintained, and authoritative, where `client_user` is set by a trigger this repository did not know it had. The panel change stands.

**The WhatsApp testing above was against the wrong function, corrected same session.** `yaad-whatsapp-webhook` speaks to Meta's Cloud API directly and has not received real traffic since `0e654f6`, 31 Aug 2026, "Move WhatsApp evidence intake from Meta to Twilio, same day", founder's own words there: "remove meta from this moving forward." That commit ported evidence intake to `yaad-inbound` and explicitly reverted `yaad-whatsapp-webhook` to its pre-evidence-intake state rather than leave it "dead code nobody will ever configure" — but that earlier state still included a full guided, one-question-at-a-time client intake (`finalizeIntake()`, built 29 Aug per its own comments), which the revert left standing. Untouched since, still deployed, still reachable only by a Meta send that will never arrive. This session found it, tested it as if it were live, and fixed two real bugs in it (`trade` never written to the column `yaad-match` routes on; no admin notification on a completed job) before realising the mistake. Corrected here rather than pretended away: those fixes were real, but the file itself was never the live path, so they fixed nothing anybody would hit.

**The actual live path, `yaad-inbound`, was already correct on both counts this session initially flagged as missing.** Its own new-job intake (thread-based across `intake_threads`, the model deciding `gathering` → `confirming` → `done` rather than a fixed seven-question script) already writes `trade`/`trade_source` on the insert. Its `notifyAdmin()` already fires on the first message of a new thread and again the moment a job is confirmed, which is a stronger guarantee than what was added to the dead Meta file: the founder is told a lead exists before it is even a real job, not only once it becomes one. No code changed here; this is what was actually found on rereading `yaad-inbound` properly after the correction above.

**What is still genuinely unverified, stated plainly.** `yaad-inbound` requires a valid `X-Twilio-Signature`, computed with `TWILIO_AUTH_TOKEN`, a secret this session does not hold and should not try to reproduce. Every piece of logic that does not need a real signed request was traced directly against the source and the live database; the first genuinely signed message from a real device is the proof that remains, same limitation every prior WhatsApp-via-Twilio entry in this log has stated honestly rather than assumed past.

**`yaad-whatsapp-webhook` was deleted outright, founder's instruction, same session: "strip it," then "remove it from the dead file and delete the dead file afterwards."** Not reverted to an earlier state this time, the way the evidence half was on 31 Aug: removed entirely, source and deployed function both (`supabase functions delete yaad-whatsapp-webhook --project-ref leffyisvfvjwzilydlwf`). The worker-signup-via-WhatsApp lane living in the same file went with it, unreviewed and untested this session, flagged to the founder as a separate, smaller instance of the identical problem before she gave the instruction to remove the whole file rather than pick it apart. Every doc that named the function as a live thing (`CLAUDE.md`, `web/README.md`, `concierge/README.md`, `supabase/functions/README.md`, `specs/ARCHITECTURE.md`) was corrected in the same pass to point at `yaad-inbound` instead, or to say plainly that the pointer needs rechecking where this session could not confirm the replacement (`specs/PORTALS-BUILD-SPEC.md` §5.4, `specs/PORTAL-SPEC.md`, on which function carries a client's portal chat message out to a worker's WhatsApp: the inbound half of that relay is confirmed live in `yaad-inbound`, the outbound half was not reverified before this entry was written). Nothing about this removal touched `yaad-inbound` itself, which was already correct and is the only WhatsApp path this repository has now.

Test data left in the live database, all under names beginning `TEST QA`, all pointed at the founder's own email as contact: jobs `JOB-WEB-1788219235030`, `JOB-WEB-1788220315236`, `JOB-WA-1788220553320`, `JOB-WA-1788220705262`, `JOB-WA-1788220885058`; enquiries `732aa3d1-…`, `b434f8b3-…`; application `APP-35885A`. Left in place rather than deleted; nobody was asked to remove them.

**The real, live, signed test the founder then insisted on, correctly, proved the loop end to end.** Everything above was code tracing and unsigned synthetic payloads. Asked to prove it for real, the founder sent an actual WhatsApp message from her own phone (+447767171858) through a clean, fresh conversation: opener, access and photo question, name and email and confirmation, portal link. She then completed the account herself, on her own phone, against the real production site (`app.yaadly.co.uk`, not this session's local dev server), and signed the current Client Guidelines (`doc_signatures`, v1.3, signer "MONI", 00:37:18). The job (`JOB-WA-1788222609649`) went to `status = 'open_for_quotes', open = true`. Trade, parish, name, phone and description were all correct in the database at every step, checked live, not assumed. This is the first message in this repository's history proven signed, received, and carried through to an open job, on a real device, start to finish.

**Checking exactly how it opened surfaced two things worth more than the success itself.** First, `jobs.client_user` came back populated on that job, immediately contradicting what this entry earlier said nothing in the repository had ever written. The write is real: `bind_job_identities()`, a trigger on `jobs` that looks up `auth.users` by `client_email` and fills `client_user`/`worker_user` the moment a match exists. It has never had a migration. `list_migrations` and a full-repo grep both came back empty for it; it was applied to the live database directly, at some point, by someone or some session, and never checked in. Given `scripts/backup-db.sh` is the only backup path on the free plan, a rebuild from migrations alone would have dropped this trigger silently, and the first symptom would have been `client_user` and `worker_user` simply never populating again, with nothing in the schema to explain why. Adopted into the repository as `20260901a_bind_job_identities_had_no_migration.sql`, applied through `apply_migration` so it is now in Supabase's own migration history too, not just the CREATE OR REPLACE re-run: verified live afterward that `trg_bind_job_identities` still exists and the function body is byte-for-byte what was read back before writing the migration. Behaviour unchanged; only its existence is now recorded anywhere but production.

**Second, and more serious: `yaad-inbound` binds a job to whatever email address someone types into WhatsApp, with no proof they own it.** `client_email` is written directly from the model's read of the conversation at the moment `finalizeIntake`'s equivalent completes, not deferred until a link is clicked the way `yaad-post-job`'s draft mode and the now-deleted Meta webhook both deliberately did (see that file's own comment, still true in spirit even though the file is gone: "a single fat finger would lock the real client out of their own job with no way back"). Combined with `bind_job_identities()` matching on email against ANY `auth.users` row, confirmed or not, the realistic failure is: a typo, or someone typing in a real stranger's known email on purpose, and the job silently becomes that stranger's the next time they sign in and sign the current Client Guidelines for any reason of their own, anywhere in the product. The real client is then locked out of their own job with nothing to click their way back in with. Not fixed this session: it is a trust-model decision (defer the bind until a click proves the mailbox, matching the pattern already used everywhere else in this codebase, versus accepting the current risk for the sake of the one-message-and-done WhatsApp experience) and belongs to the founder, flagged rather than guessed at.

Also noted, not yet acted on: `jobs.access_contact` stayed empty on the WhatsApp-created test job even though the AI's read of "who can let them in" is right there in `descr`'s free text; the structured column just never gets written on this path. `jobs.urgency` was `null` for the same job. Neither blocks anything today; both mean a worker or the desk reading the structured fields instead of the free text would see less than what was actually said.

## 2026-08-31 · The concierge desk learns the report_confirm lane a second, correct time

A separate session had already built and shipped the real mechanism for holding an AI-drafted evidence report for confirmation before it reaches a client (`relay_confirmed_report()`, the worker confirms, not the founder) while this one was independently designing and briefly deploying a different, competing version of the same idea from a single leftover test row, unaware the real thing already existed a few commits back on `main`. The duplicate (`confirm_evidence_report()`, `report_confirm_phone`) was live in production for a few minutes before the collision was caught, reverted by redeploying the correct code, and dropped from the database. Full account in this session's own record; the only thing worth keeping from it is here.

What survives: the concierge desk's Mid-chat view (`wa_intake_sessions`) did not recognise `report_confirm` as a lane at all before either session touched it, and the real shipped answers shape (`job_id`, `stage`, `draft_text`, `ai_summary`) is exactly what this view now reads. Small, additive, and correct against the actual mechanism, so it stayed.

## 2026-08-31 · The Kickoff Pack approves with the quote, and drives the real stage rail

`kickoff_packs.status` could be `approved`, the desk's own copy said "approving is what lets it reach a client," and RLS already correctly refused to show an unapproved pack to a client (`parties read approved packs`, status = 'approved' and the job matches their email). Nothing anywhere ever set that column. Every pack, forever, was invisible to every client: a fully built gate with no handle on the door. Founder's instruction, 31 Aug 2026: the pack approves at the exact moment the client accepts a quote, not as a separate admin action, so `choose_worker()` now looks up the job's pack, refuses the whole choice if no pack has been drafted (`docs is null`), and sets `status = 'approved', approved_by = <the client>, approved_at = now()` in the same transaction that assigns the worker and starts the job. One decision, one commit.

That made the pack's own `payment_schedule.stages` a real, usable source of truth, so the portal's stage rail now reads from it instead of a generic fixed list once work starts. `approve_stage()` was already stage-count-agnostic (a bare integer that climbs with no ceiling), so nothing there needed to change; only the display layer did. `jobStages()` in `web/lib/portal/journey.ts` returns the original fixed rail unchanged for any job with no approved pack (legacy jobs, or a choose that somehow raced ahead of a draft), and for everything else, the six generic pre-work stops ("Job live" through "Kickoff issued") followed by the pack's own stage names, followed by "Closed & paid" and "Reviews." A client approving "Stage 2" is now approving the thing the pack itself called stage two, not a label invented separately from it. The amount due at that stage is computed on the page, `proportion_percent` of the accepted quote's `labour_jmd`, never stored, so it can never drift from either figure.

The client-facing `/pack` page also stopped being one long scroll and became one page per document (`web/app/portal/(gated)/jobs/[id]/pack/[doc]/page.tsx`, table of contents at `/pack`), and gained the three sections it never had at all: the document pack, the risk register and the open questions. `human_review_notes` is deliberately excluded from every client-facing page: its own schema description is "specific things the project manager must personally verify or correct before issuing this to the client," which is Monique's checklist against her own draft, not something a client should ever read. It stays visible only in the concierge desk.

## 2026-08-30 · The portal answers on one hostname

`web/wrangler.jsonc` set a custom domain but left `workers_dev` and `preview_urls` at their defaults, which are both true. So the portal was reachable at `app.yaadly.co.uk` and also at `yaadly-app.<account>.workers.dev`, plus a per-version preview URL for every upload, all running the same code against the same Supabase project. Both are now explicitly false. Right now the cost of that was two unwatched addresses instead of one, and previews pointing at the production database. The reason it mattered enough to fix today is what happens next: Cloudflare Access attaches to a hostname, so once the operations console ships behind Zero Trust, a workers.dev URL walks straight past it. The Technical Notes list this as the most common way people believe they are protected and are not. Set in the committed config rather than the dashboard so it survives the next deploy.

## 2026-08-30 · The banned-language screen reaches the live reply path

The engine screened every line it emitted and the thing actually talking to clients did not. `yaad-inbound` composes the WhatsApp and SMS reply a client reads, strips forward-looking promises, strips dashes, and sent it. Nothing checked it for the words this company has standing rules never to use. A model explaining how payment works reaches for "escrow" as the obvious word, and it would have gone out under Yaadly's name.

Found by checking whether step 4 of the Technical Notes sequencing was worth doing. It is not, and this was.

`supabase/functions/_shared/guardrails.ts` is a direct port of `BANNED_TERMS` from `yaad/guardrails.py`. It goes in `twiml()`, which is the single place anything in that function reaches a person, so the fixed strings are screened alongside the model-written ones: it costs nothing and a careless edit to a fixed string cannot walk past the rule either.

On a hit the behaviour differs from the engine on purpose. The engine raises, which is right for a batch job and wrong here, because raising means somebody who messaged about their roof gets silence and silence is indistinguishable from the message vanishing. Instead the client gets a short holding reply, the draft goes to the function log, which is private to the project rather than to a third party, and a phone alert says a reply was held back. The alert carries the guidance strings only, never the draft, because ntfy is a public service and the draft is model prose about somebody's property. That is the governing rule doing what it is for: the machine refuses to send it and hands it to a person.

`yaad-completion` and `yaad-kickoff` now flag the same list rather than blocking it, because a human reads those before anything is issued. The human gate was already there; what was missing was telling them. `yaad-whatsapp-webhook` needed nothing: its replies are fixed templates and a deterministic status read-back, and its model call only fills a job card.

Ten Deno tests, wired into CI. Seven assert the screen, using the same five phrases the Python suite uses so a loosened pattern turns one of the two red. Three assert the gate is still wired into `yaad-inbound` and the two draft producers, because the realistic failure here is not a broken regex, it is somebody removing the call in good faith while tidying the reply path.

## 2026-08-30 · MiniMax stays for now, and the switch is triggered by real data

Amends the entry below, same day. The destination is settled and the plumbing is built; the timing is not now. Founder decision: keep running on MiniMax while the data flowing through these functions is synthetic. That is consistent with the Technical Notes, which said MiniMax is fine for the buildathon with synthetic data and that the move belongs before the pilot. The trigger is therefore not a date but a fact: **real client and worker data, which arrives with the December pilot in Kingston and Portmore.** On cost, for the record, because it was asked and answered rather than assumed: Mistral Large is about 1.7 times MiniMax per token, which at pilot volume is roughly one pound a month, so cost is not what is deciding this either way.

One thing changed in the code as a result. The MiniMax branch was written as a loud temporary fallback that logged a warning on every single call saying the migration was incomplete. Now that running on MiniMax is the deliberate current state rather than an unfinished job, that warning would fire thousands of times against a decision somebody already took, and an alarm people are trained to ignore has stopped being an alarm. It is gone. What remains is the honest signal: `yaadly.model.region` rides on every model span, silent when nobody is asking and conclusive when they are. The day the answer has to be `eu`, one query proves it.

## 2026-08-30 · The text model moves to the EU, and the provider stops being eight constants

Founder decision: move off MiniMax to an EU endpoint before the pilot carries real data, and Mistral is the endpoint. The reason is jurisdictional rather than technical. MiniMax is hosted in China, it was hard-coded into eight live Edge Functions as a pair of constants each, and everything a client typed and everything a worker said over WhatsApp passed through it. China has no UK adequacy decision, and Jamaica's Data Protection Act 2020 restricts transfers where the destination lacks adequate protection. Mistral because it is EU hosted, it speaks the OpenAI chat completions shape so the eight call sites barely changed, and it offers a signed DPA.

The shape matters as much as the destination. The provider now lives in `supabase/functions/_shared/textmodel.ts` and nowhere else, so it is a decision in one file rather than a property of eight, and a future move is a secret change rather than a deploy. Four `TEXT_MODEL_*` secrets override everything, which is the escape hatch that stops the next migration being another eight-file edit. CI enforces it two ways: the shared-copy check now loops over every file in `_shared` rather than naming `otel.ts`, and a new job fails the build if any function hard-codes a model endpoint again.

The MiniMax branch is still in the resolver, deliberately and temporarily, so that deploying this could not take the intake flow down before the Mistral secret exists. It is a noisy fallback rather than a quiet one: it logs a warning every time it runs, and `yaadly.model.region` travels on every model span so the question "is this still going to China" is answered from telemetry rather than from memory. `RUNBOOK.md` step 9 is the three-step completion, ending in deleting that branch. A fallback to China that nobody notices is exactly how a migration gets declared finished and is not.

## 2026-08-30 · The buildathon repo stays public until judging closes, then goes private

Founder decision. The organisers' requirement is public for judging, so this repository stays public and MIT licensed until judging closes, and is made private that day. Nothing to weigh any more, it is a dated action waiting on a date. Two things to hold on to when it happens. Making a public MIT repository private stops future access, it does not recall what is already out, and anything forked before the switch stays licensed forever, so the switch is worth making on the day rather than the month after. And the product work does not have to wait for it: anything that is not part of the submission can go into a private repository whenever it suits, with this one frozen as the buildathon reference. Recorded here rather than in a task list because it is the kind of item that gets discovered in December.

## 2026-08-30 · Identity documents are withheld from the model entirely

Supersedes the entry below it, same day. Founder decision: stop sending the government photo ID, the live selfie and the face video to NVIDIA, keep the machine read for everything else. Persona already does the identity step with real document-authenticity and liveness checks, so the second copy of a passport in a US model vendor's request logs was adding a transfer of the most sensitive thing on the file without adding a check anybody relied on. The consent gate made it lawful to ask for. It did not make it worth asking for. Implemented as `IDENTITY_DOCS` in `yaad-vetting-review`, tested before the mime check and before the download so the file is never fetched from the bucket on that path, with no override flag and no query parameter. The face-match pass went with it. Where Persona has passed, that comparison happened somewhere stronger; where Persona has not, the review now states in plain words that nobody has compared the two faces, which is a better outcome than a model guessing. Withheld documents stay on the skipped list with the reason, because a document that vanishes from the review reads as a document that does not exist, and the count goes to telemetry so it is provable after the fact rather than promised. Applicant-facing copy in `JoinFlow.tsx` updated and `AI_CONSENT_VERSION` bumped to `ai-review-v2`; v2 is strictly narrower than v1, so existing consents still cover what is done, but the sentence changed and a consent is tied to its sentence.

## 2026-08-30 · Identity documents go to a model, with consent, to one vendor

Recorded because the first draft of `CLAUDE.md` carried a flat rule that ID documents never reach a model API, which is not what is built and would have had a future session delete a working feature. What is actually built: `yaad-vetting-review` sends identity documents to NVIDIA's hosted vision model, one image per call, so the desk has a machine reading before a person opens the file. It never decides; there is no code path in it that approves or declines. It runs only when `ai_review_consent` is `granted`, NULL counts as declined, and the gate sits inside the review function rather than at the caller so the desk's re-run button cannot override the applicant's refusal. That is the right shape. The open item is not the consent gate, it is the destination: NVIDIA hosted means a transfer of identity documents to a third country, which is exactly the processing a DPIA has to cover and exactly the reason the OIC registration matters. Same class of question as the engine's model provider, and it should be answered at the same time.

## 2026-08-30 · One repository, not three

The Technical Notes recommend three repositories: a private Python engine, a private TypeScript product monorepo, and a public static landing page. Today this is one repository with the same separation inside it: `yaad/` and `tests/` are the engine, `web/` is the Next.js app, `supabase/` holds the Edge Functions and migrations, `docs/` is the static marketing site on GitHub Pages. The separations the notes were actually protecting are already in force. The marketing site has no build step and deploys independently, so a broken app build cannot take `yaadly.co.uk` down during a launch window, which was the main point. Splitting the repositories now would cost a week and buy visibility control, which is a separate decision (see below). Revisit if repository visibility forces it, or if a second engineer joins.

## 2026-08-30 · Repository visibility is unresolved and it is a deadline, not a preference

This repository is public and MIT licensed, as a submission to the Global Open-Source Agentic AI Buildathon. The codified operating logic of the business, the guardrails, the evidence gates, the benchmark structure, is therefore visible to anyone. What is held back is the right stuff: no payment integration, no real data, no cost research, no fee structure. But the architecture of the guardrails is part of the moat and it is on display. The thing that makes this urgent rather than tidy: making a public MIT repository private stops future access, it does not recall what is already out, and anything forked before the switch stays licensed. So the decision is worth less every week it is not made. The blocking question is for the buildathon organisers, not for anyone here: must the submission repository stay public, and for how long. Public for judging only means make it private the day judging closes. Public indefinitely as a prize condition means freeze this at the submission commit, develop in a private copy, and say so in the README. No requirement means private now. Monique's call.

## 2026-08-30 · The engine stays Python

The Technical Notes reversed an earlier suggestion to port the engine to TypeScript, and the reversal is right. The tests are the asset: they are what proves the AI never releases money, rules a dispute or moves a Yaad Score. A rewrite risks losing coverage exactly where coverage matters and gains nothing a judge, a client or an investor can see. Python is also the better home for what is on the roadmap, EXIF parsing and image checks on evidence photos, and the hiring pool for LLM orchestration and WhatsApp Business API work skews Python anyway. Treat a rewrite as a 2027 decision, to be made only if a TypeScript-first engineer joins and the split becomes a real burden, and then with the tests as the specification.

## 2026-08-30 · The engine stays stateless

Job Card in, structured result out. No database access, no auth, no session state. Everything stateful lives in Postgres. This is the boundary that makes a two-language codebase cheap instead of painful, and the agents were already written this way, so it costs nothing to hold. It is also what would let the engine be wrapped in a thin API and deployed separately later without disturbing the agents or the tests.

## 2026-08-30 · Pricing is a lookup and a model call must never be added to that path

Recorded here as well as in `CLAUDE.md` because it is the single most likely thing for a future agent to helpfully break. The founding premise is that a client in London pays what a client in Portmore pays. A hallucinated price band breaks exactly the thing the business exists to fix, so pricing reads from researched benchmarks in `yaad/benchmarks.py` and returns "no public price exists in Jamaica for this work" rather than a guess. That answer is correct and complete, not a gap. Widening coverage means adding a sourced benchmark row.

## 2026-08-30 · The worker web surface stays thin

The diaspora client is motivated, on a laptop, and will tolerate friction. The worker in Portmore is on a phone, mid-job, and will abandon anything that is not WhatsApp. So the worker portal exists for the two things WhatsApp does badly, structured onboarding with credentials and file upload, and for nothing else. Building a full worker dashboard for December would be wasted effort because it will not be opened. The consequence worth watching: if the worker journey is WhatsApp-first, ID documents land in Meta's infrastructure before they reach ours, which is why identity verification links out to a vendor flow rather than accepting documents over the chat.

## 2026-08-30 · Model provider is a configuration value, and the current one has to change before real data

The engine speaks the OpenAI chat completions API, so any compatible endpoint works from two environment variables. That was a day-one call made because the buildathon gateway key had not been issued when the build started, and it turns out to be worth much more than the convenience. MiniMax is a Chinese provider and is fine for the buildathon with synthetic data, which is what synthetic data is for. Once real job data flows, worker names, property addresses, job details, voice transcripts, that becomes a transfer of personal data to a third country, and two regimes bite: UK GDPR, which needs adequacy or appropriate safeguards and has neither straightforwardly available for China, and Jamaica's Data Protection Act 2020, which restricts transfers where the destination lacks adequate protection. For the pilot, move to a provider with a UK or EU region and a signed DPA. Because of the day-one design that is a config change, not a rewrite.

## 2026-08-30 · Data protection registration is the item most likely to stop the launch

Not a technical decision, recorded here because it constrains the technical ones. Jamaica's Office of the Information Commissioner requires registration before processing personal data about people in Jamaica, and the workers put a UK company in scope regardless of where it is incorporated. Operating unregistered is a criminal offence; enforcement has not been switched on, which is a grace period rather than an exemption. Separately, UK GDPR requires a DPIA for high-risk processing, and collecting ID documents about individuals across two jurisdictions is high risk by any reading. Neither is difficult, both take longer than expected, and both tend to be discovered in November. This also needs a qualified solicitor in both jurisdictions before any real payment is taken. Timing is Monique's.

## 2026-08-30 · Guardrails live in code and in tests, not in prompts

`yaad/guardrails.py` screens outbound text for banned language and refuses any money or reputation action that does not carry a named human decider. The tests prove it. The reason this is in code rather than in a system prompt is that a model can be talked past a prompt and cannot be talked past a raise. The layer still missing is the third one the Technical Notes recommend: a database-level constraint that refuses a payment release without a named human recorded. Application code is what an agent writes; a database rule is set up once and then left alone, and it holds even when the code above it is wrong. Worth adding before any payment path exists, which means before the December pilot takes money.

## 2026-08-26 · DNS on Cloudflare, app and site on separate hostnames

The `yaadly.co.uk` zone went active on Cloudflare on 26 August 2026. The marketing site serves from GitHub Pages at the apex, and the Next.js app deploys as a Cloudflare Worker at `app.yaadly.co.uk` via OpenNext. Two hostnames, two deployment paths, one of which has no build step. When the operations console arrives it gets its own hostname for the same reason plus one more: a separate hostname is what allows Cloudflare Zero Trust to sit in front of it, and the console holds ID documents and payment controls. That subdomain must be proxied, orange cloud rather than grey. A DNS-only record routes traffic straight past Cloudflare and Zero Trust does nothing at all, which is the most common way people believe they are protected and are not.

## The client funnel lives in the app, and takes no account (Stage 2, 30 Aug 2026)

Stage 1 deleted the post-a-job funnel from `docs/index.html` so the marketing site could be short. The plan said to leave the old path working until the app funnel existed. It did not: the app's `/jobs` is a BOARD, a list of other people's jobs, and nothing in `web/` ever created one. So for a window there was no way to post a job anywhere, and the app's own "Post a job" button pointed at `yaadly.co.uk/#post`, which redirected to the board. A client went in a circle.

`web/app/jobs/new` is the funnel, and it is now the only place a job is created.

**No account, and no password on the page.** Founder decision, 30 August: no account to get quotes, an account once a job is booked. The account is worth something at booking, where it approves evidence, holds the invoice and carries the property record between jobs. In front of a quote it is a toll gate on the way in.

**Two backends, both already hardened, neither changed.** The work goes to `yaad-post-job` in draft mode, which writes a job at `stage 0, open false` and deliberately stores no personal data. The contact details go to `yaad-enquiry`, which has the per-recipient throttle, the receipt tracking and an honest answer for somebody who gave a phone number rather than an email. The enquiry carries the job reference so the desk reads them as one thing. No new table.

**Three behaviours carried over on purpose**, because a rewrite is the easiest place to lose them: the job saves as a draft before a single personal detail is asked; the contact field still says "Hidden from workers until you start a chat with one. You control when it's shared."; and the reply promise is one working day.

**The end state names what happens next.** "Your job is saved" tells somebody who has just described water running down their mother's bedroom wall nothing at all. It now says a person reads it within one working day, then an itemised quote with labour split from materials, then a written scope, then evidence at every stage with nobody paid for a stage until it is approved, and that no account is needed until they book.

`TRADES` and `PARISHES` moved to `web/lib/taxonomy.ts`. They were declared inside `JoinFlow.tsx` and the funnel needs the same two lists. Two copies drift, and the day they drift a client posts a job in a trade no worker profile can carry.

## Quotes without an account, and the booking that needs one (Stage 2.2, 31 Aug 2026)

The decision was "no account to get quotes, an account once a job is booked." Half of it was not true: `job_quotes` was only ever rendered inside the portal, which is behind auth, so a client had to make an account before seeing a single price.

**Looking is open, choosing is not.** `quotes_for_code` and `job_for_code` are security-definer functions taking the job id and its `portal_code`, which is the bearer token the WhatsApp link and the portal claim already ride on. They return what a client needs in order to choose and nothing else: no client contact details, no address. `/jobs/[id]/quotes?code=` renders them for an anonymous visitor, labour split from materials, materials marked at cost.

**Accepting is the booking, and the booking is where the account appears.** `accept_quote_as_me` is authenticated only and refuses any caller who is not that job's `client_email`. So holding the link is enough to look at prices and never enough to book one. Pressing Book with no account routes to `/portal/join` carrying the job, the code and the chosen quote; the account is made there and the quote is accepted on the way back.

**A job that already has a worker is not re-bookable through it.** Changing a worker mid-job is a desk decision with a conversation behind it, not a button.

**Found while testing:** `revoke all ... from public` did not take execute away from `anon`, because `anon` is a role with its own grant rather than a member of PUBLIC for this purpose. `accept_quote_as_me` was therefore callable by anonymous visitors. The body already refused them, having no email in the JWT, so nothing could be booked, but a booking function anonymous callers may call is one bug away from one they may use. `anon` is now revoked by name.

**Not built:** nothing tells the client a quote has landed. The page is honest when empty and the link keeps working, but the client has to come back and look. Notifying them is its own piece of work and needs the WhatsApp credentials that are still unset.


## Clients sign in with a code, not a password (31 Aug 2026)

The audience is diaspora clients, often older, often on a phone in another country, who have already explained the whole job once. "Choose a password, at least 8 characters" is where those people stop, and it bought nothing: the job code was always the real gate and a password was a second secret to lose on top of it.

`yaad-portal-code` issues a six digit code and delivers it by email through Resend, and by WhatsApp or SMS through Twilio when the job carries a number. It uses `generateLink`, which returns both a link and a six digit `email_otp` for the same token, and takes the digits. That was chosen over Supabase's own magic-link mail for three reasons: a link opened in a different browser than the one it started in is the classic way magic links strand somebody, digits travel over WhatsApp as easily as email, and GoTrue's templates stay untouched.

The function issues and delivers and nothing else. It never sees the code come back and never mints a session; verification happens in the browser against Supabase directly, so the code goes to the mailbox or the phone and nowhere else, which is the only reason holding it proves anything.

**The gate is unchanged.** A new client still presents a job code and `pend_portal_code` still rules on it, rate limited on both the email and the code. A returning client is never asked for one, because locking somebody out of their own history over a code from months ago is the wrong trade. `email_has_account` decides which case it is, and it is granted to nobody: callable only with the service role, because "is this person a Yaadly client" is the question an account enumeration attack asks.

**`verifyOtp` takes type `"email"`, and this is measured rather than assumed.** The founder signed in through the page on 30 August and the auth log shows a single `POST /verify` returning 200 with `login_method: "otp"`, `provider: "email"`. Write that down, because the storage says otherwise: `generateLink` files the token in GoTrue's `recovery_token` slot and logs the event as `user_recovery_requested`. It still verifies as `email`. It also cannot be rediscovered by probing, since GoTrue returns the identical "Token has expired or is invalid" for all four types when the token does not match, so a wrong type looks exactly like a wrong code.

Existing accounts keep their passwords. Nothing was removed from them; they simply have a way in that needs nothing remembered.

## One door in (Stage 3, 31 Aug 2026)

The plan said to retire `yaad-post-job`. That would have broken the funnel, because `/jobs/new` calls it in draft mode, which is the right thing to call: it is rate limited, it writes `stage 0, open false`, and it deliberately stores no personal data. What actually needed retiring was narrower and more important.

**`yaad-post-job`'s `golive` mode is gone.** It created an account from a password, captured the Client Guidelines signature and opened the job, and it was step 6 of the five-step funnel deleted in Stage 1. Nothing had called it since. Removing it was not tidying: it was a **second account-creation path that took a password**, on a journey that moved to a six digit code with no password anywhere. A dead endpoint that can still mint password accounts is exactly the kind of thing that quietly outlives the decision to stop doing it. `verifyPassword`, `signedInUser` and `MIN_PASSWORD` went with it, forty-seven lines of password handling. It answers 410 and names where each part went.

**`yaad-website-intake` is retired to a 410 stub.** It served the public "Tell us what needs doing" form deleted in Stage 1, took no authentication by design, and created jobs. The only reference left anywhere was one line in `web/README.md`. It is a stub rather than a deletion because an unreferenced public endpoint that writes rows is precisely what survives three refactors unnoticed, and a 410 that says what happened beats a 404 that says nothing. If something out there still calls it, that is now visible rather than silent.

**One stale link found and fixed:** the portal's `ServiceNext` still sent clients to `yaadly.co.uk/#post`, the deleted funnel, and to `#services`, which moved to its own page in Stage 1.

**What can create a job now:** `/jobs/new` through `yaad-post-job` draft mode, plus the WhatsApp webhook and `yaad-inbound` for email and SMS. Those are channels rather than rival funnels: they land in the same tables with the same portal code model, which is what the plan meant by one code path plus WhatsApp intake.

## The ID check must not exclude the people it is for (31 Aug 2026)

Persona's template decides which government documents it accepts, and **most Jamaican tradespeople do not hold a passport**. A passport-only template would silently exclude the supply side this business is built on, and would do it in a way that looks like ordinary rejection.

**The Persona API does not expose enabled ID classes.** Checked directly: the template config it returns carries Persona's generic field schema, not the document types the template accepts. So this cannot be verified, changed or monitored from code. It is a dashboard setting and the runbook says which documents to enable.

**So the product does not depend on it being right.** A worker can press "It would not take my ID. Let me send it another way." on the ID step, which switches to the in-page capture and upload for a person to check by hand. The existing fallback only fired when Persona failed to LOAD, so somebody whose voter card was refused saw Persona work perfectly and turn them away with nowhere to go. The escape is theirs to take rather than something they must ask for, the copy names the voter card and the driver's licence, and it says plainly that it does not count against them.

**The thirty second introduction** is Phase 2's, optional, and the only thing on that page a client ever sees. It is held to the same rule as the identity captures: a face and a voice is the same category of thing as a selfie whatever it is called, so `intro_video` is on `IDENTITY_DOCS` in `yaad-vetting-review` and is never sent to a model.

## The approve button, and the trigger that had to be found first (Stage 5.1/5.2, 31 Aug 2026)

The evidence ledger has said "Money moves when you approve them" since 30 Aug. It was wired to `jobs.status = 'evidence'`, a value the `jobs_status_check` constraint has never allowed, so `awaitingApproval` was always false and the sentence had described nothing since the day it was written.

**Two pieces were needed for the sentence to become real, not one.** Something has to put the job into a state where there is genuinely something to approve, and something has to let the client say yes.

`'evidence'` joins the allowed status values. `sync_job_status()`, the existing trigger that recomputes `jobs.status` from scratch on every write, gained one more fact: when a job has a worker and the stage currently being worked has evidence filed against it with no `stage_approvals` row yet, status is `evidence` rather than `in_progress`.

**The first version of this used a second trigger to set the status directly, and it silently did nothing.** `sync_job_status` already owned that column and ran after mine on the same statement, recomputing it from its own rules and overwriting what I had just set. Two triggers deciding one column is exactly the shape that reads correctly and does nothing, and it was only found by testing the actual database rather than reading the code. Fixed by giving the one existing authority the missing fact rather than adding a second one beside it. The evidence-side trigger now only pokes the job row so the recompute fires; it does not decide anything itself.

`approve_stage(p_job)` is authenticated only, same access rule as `client_go_live`: `revoke ... from public, anon` named explicitly, because Supabase grants `anon` execute on every function in an exposed schema and revoking from `public` alone leaves that standing, the same trap this repository has hit twice before (20260828b, 20260828f). It refuses a caller who is not the job's client, refuses while a dispute is open (`state <> 'resolved'`, the same predicate the dispute RLS policies already use), refuses when nothing has been filed for the stage, and on success snapshots every evidence item's id, sha256 and label into `stage_approvals` before advancing `jobs.stage`.

It does not decide a job is complete. Proving one stage is a smaller claim than finishing the job, and nothing currently marks a job `complete` at all; that is a separate, later piece of work.

**Exposed once**, in `EvidenceLedger`, as a one-tap button for the client only, beside a real link to the dispute form (`?tab=job#dispute`) rather than a second copy of it. Stage 6's WhatsApp approval reply calls the same `approve_stage()` function; nothing about this design assumed a browser.

**A second bug found by clicking rather than reading:** the ledger's headline said "Nothing filed yet" in the same breath it listed two filed items and showed the Approve button, because `currentStage === 0` was checked before `awaitingApproval`. Whether something is waiting on a human is the more urgent fact. Fixed by reordering the two checks.

Verified against the live database with real auth tokens, not reasoning alone: the right client approves and the stage advances with a fingerprinted record; the wrong user is refused; an open dispute refuses and a resolved one un-blocks; a stage with nothing filed is refused; an anonymous caller is refused at the grant level, before the function body runs; `stage_approvals` is readable by both parties to the job and by nobody else. Then walked through the real browser UI with a forged but genuine Supabase session: the button rendered, the tap advanced the stage rail from "Stage 1 on site" to "Stage 1 released," and the button correctly disappeared once there was nothing left to approve.

## The materials store gate, loosened to when it actually applies (Stage 5.4, 31 Aug 2026)

Founder's own wording: "say where materials are kept" becomes a go-live condition only when the accepted quote includes materials; small jobs default to worker supplies. Before this, `enforce_store_before_open()` blocked every job from opening until the client named a materials store, whatever the job needed: a two hour call-out with nothing to buy was held up by a question that did not apply to it.

`materials_store_nominated(p_job)` now returns true outright when the job has no *accepted* quote with `materials_jmd > 0`, and only falls back to the original rule (a real place named, or the explicit "none available" answer) when there is one. One function decides this, and both callers, the `enforce_store_before_open` trigger and `client_go_live`'s own `WHERE` clause, inherit the change for free.

**Reading `client_go_live()` closely turned up something worth recording:** it opens a job only when `worker_email` is empty, meaning a job always goes live *before* any quote is accepted. No job can have an accepted quote at the moment this gate is checked. So the loosened rule does not merely soften the block for small jobs, it removes it from the go-live moment entirely; the question genuinely belongs later, once a quote naming materials has been chosen, which is a state that only exists on the job room page after the fact.

**The client-facing checklist (`jobGates`) had to move with it, or it would show a question the database no longer requires an answer to.** It now takes `hasAcceptedMaterials` and only lists "Say where materials are kept" when that is true. On the pre-go-live "still waiting" list it is always `false`, correctly, since no job on that list can have an accepted quote by construction. On the job room page it is computed from the real quotes already loaded on that page: `status === 'accepted' && materials_jmd > 0`, the same test the database uses.

The separate trigger that blocks *filing materials evidence* without a named store is untouched: once materials are actually involved, that check still applies at the point it always applied, and my change does not reach it.

Verified directly against `materials_store_nominated()`: a job with no accepted quote passes with no store named; a job with an accepted quote carrying materials is refused until one is named; naming a lockable store with a description clears it. Test rows deleted.

## The client hears about every state change, not just the quote (Stage 5.3, 31 Aug 2026)

Before this, exactly one event ever reached a client: a price landing, fired from `web/app/jobs/actions.ts` right after `submitQuote` inserted the quote. Evidence landing, a stage being released, a dispute being raised, none of those said anything. The founder's own instruction was to fire from the same functions that change state, never from the UI, and to build the notification system beyond the "worker on site today" case, which has no schema signal to hang off (see below).

**One edge function, one shared secret, four kinds.** `yaad-notify-client` replaces the single-purpose `yaad-quote-landed`. The caller sends `{secret, jobId, kind}` and never a message body: all wording is composed inside the function from a fixed `kind` enum (`quote_arrived`, `evidence_landed`, `dispute_raised`, `stage_released`). A leaked secret can trigger a real notification about a real job state; it cannot make the system say arbitrary things to a real phone number. The same shared-secret-hash pattern already used for the deletion-clock cron job (`20260827f_the_deletion_clock_actually_runs.sql`) is reused here: a random secret generated once, only its SHA-256 hash kept in `app_settings`, the plaintext baked into the trigger function bodies.

**Three triggers, not one function called from three places.** `notify_client_quote_arrived()` on `job_quotes` AFTER INSERT WHEN `status = 'submitted'`. `notify_client_on_job_change()` on `jobs` AFTER UPDATE, covering both `evidence_landed` (status flips to `evidence`) and `stage_released` (stage advances) in one function, because both are facts about the same row changing on the same statement. `notify_client_dispute_raised()` on `disputes` AFTER INSERT, unconditional: every dispute notifies the client, framed as a receipt rather than an accusation.

**`submitQuote` no longer calls a notifier at all.** The UI-fired call was removed outright rather than left alongside the trigger, because a state change firing twice from two places is exactly the kind of drift this plan exists to close off. `yaad-quote-landed` is retired to a 410 stub, same shape as `yaad-website-intake`: it read a client's contact details on the caller's bearer token, so an unreferenced version of it should say what happened rather than 404 silently.

**A bug the plan's own honesty requirement caught: the very first approval on any job never notified.** `notify_client_on_job_change()`'s `stage_released` condition originally checked `stage_approvals.stage = old.stage`. `approve_stage()` clamps a job's very first stage to 1 internally (`greatest(jobs.stage, 1)`) while `jobs.stage` itself is still sitting at 0 going in, so the approved row lands as `stage_approvals.stage = 1` while `old.stage` was `0`. They never matched, so the first stage release on every job, the exact moment the product is named after, stayed silent. Confirmed live before fixing: `approve_stage` on a test job moved `jobs.stage` 1→2 and inserted `stage_approvals.stage = 1`, and `net._http_response` recorded no third call. Fixed in `20260831j` by keying off `new.stage - 1`, which is always the stage that was just approved regardless of where `jobs.stage` started, rather than `old.stage`. Re-tested live: fires correctly now, on the first stage and on later ones.

**Honest answer on "worker on site today," not a hedge.** There is no Arrival Log table and no column anywhere that records a worker checking in. Nothing in this migration invents one. This notification stays unbuilt, not deferred behind a vague "would need to check": it needs a real arrival event to fire from, which is its own piece of schema work, not a wording change to this file.

**No spam confirmed live, not assumed.** A second evidence insert against a stage that has already flipped `jobs.status` to `evidence` does not fire a second `evidence_landed`, because the trigger's `WHEN` clause is `old.status IS DISTINCT FROM 'evidence' AND new.status = 'evidence'`, a transition, not a state. Verified by inserting two evidence rows for the same stage and checking `net._http_response` stayed at one new row, not two.

**Verified against the live database with real HTTP calls, not migration success alone.** All four kinds fired and returned `{"ok":true,"told":true}` from `net._http_response`, including the `stage_released` fix, exercised through a real signed-in session (password-grant login, not a service-role call) calling `approve_stage` for real. Test job, test users and test identities deleted after.

## Worker video evidence, and an offline queue with nothing to reuse (Stage 5.5, 31 Aug 2026)

Photo evidence has worked since 27 Aug, but only photos: `evidence-actions.ts` runs as a Next.js Server Action, and a video is too large to ride inside one. The plan called for reusing the resumable-token pattern from the application flow (`yaad-vetting-upload`). What that meant in practice needed adapting, not copying: vetting invented a token because an applicant has no account yet. A worker filing evidence already has a real account and a real session, and `public.evidence` plus the evidence storage bucket already carry RLS that lets a job's own worker or client read and write exactly their own job's folder (20260827a, 20260830b). Inventing a second credential on top of that would have been a parallel authorisation system answering a question RLS already answers.

**`yaad-evidence-video` runs entirely as the caller, never as service role.** Every storage and database call inside it uses a Supabase client built from the caller's own bearer token, so Postgres RLS is the only authorisation check that exists. A worker who is not on a job gets refused by the same policies that would refuse them calling supabase-js from a browser console; the function repeats none of that logic itself. This is the actual reuse the plan called for: the vetting flow's start-PUT-finish shape (the server picks the path, mints a one-path signed URL, the browser PUTs straight to Storage, the server verifies and hashes what arrived before writing the row), carried over with the credential swapped for the one that already exists here. The row-write step is the one invariant that could not be relaxed: `evidence-actions.ts`'s own rule, "computed HERE, on the server, from the exact bytes stored," applies to a video exactly as it does to a photo, so `finish` downloads what Storage actually holds and hashes that, never a client-reported figure.

**80MB, not the bucket's 500MB.** The function has to hold the whole file in memory to hash it, on the same free-plan edge runtime `yaad-vetting-upload` already uses for its own 50MB video cap. 80MB is a few minutes of ordinary phone video at a reasonable bitrate, comfortably clear of the bucket's ceiling, and it leaves headroom for the runtime itself rather than chasing the largest number the bucket happens to allow.

**The offline queue is new, not reused.** Nothing in this repository did anything like it before this. IndexedDB holds each queued video as a Blob with its label, stage and kind, because it is the only browser storage that can hold something video-sized without choking, and a queued item survives a dropped connection, a closed tab, or a phone that locks mid-shoot. Upload is sequential, one item at a time, deliberately: a worker on a weak signal filing two stage videos should not have them competing for the same thin pipe. Retry is whole-file, not byte-range resume; nothing here does chunked resume, Supabase Storage's own tus support included, and adding it for a pilot of a handful of jobs a week was not this stage's job. A failed item retries itself automatically on reconnect up to five times, then waits for a worker to tap Try again, so a video that is failing for a real reason (the materials store question, most likely) does not silently hammer a weak connection forever.

**The materials-store gate needed nothing extra.** The finish step inserts into `public.evidence` exactly as `evidence-actions.ts` does, so `evidence_materials_needs_store()` (20260828c) fires the same way and with the same message whether the evidence arrived as a photo or a video. One gate, one trigger, both upload paths.

**A bug caught before it was ever live: `functions.invoke` hides a refusal's real message.** A non-2xx response from an edge function arrives from `supabase-js` as an `error` object with the JSON body sitting on `.context`, not on `data`. The first draft read `data?.error`, which is `undefined` on every refusal, since every refusal in `yaad-evidence-video` is a non-2xx status. That would have shown a generic "Could not start the upload" for every real reason a worker needed to hear, including "You may not be on this job" and the materials-store message. `JoinFlow.tsx`'s own `call()` helper for `yaad-vetting-upload` already carries the fix, written for the identical problem; `video-upload.ts` copies it rather than rediscovering it.

**Video-only, worker-only, by design.** The photo form stays exactly as it was, open to both client and worker, unchanged: that question was never Stage 5.5's to answer. The video form renders only for `role === "worker"`, matching the plan's own wording ("worker video evidence upload") even though RLS itself would not object to a client using it.

Verified against the live database and a real browser session, not curl alone: `start` refuses a worker who is not on the job, refuses no bearer token at all, refuses an oversized file, refuses a non-video mime; `finish` writes a row whose `sha256` was independently reproduced from the same bytes with a plain `shasum`, matching exactly. Then driven through the actual portal UI as a signed-in worker: chosen video queued, uploaded, the evidence ledger showed it, and `jobs.status` flipped to `evidence`, the same transition Stage 5.3's notifier fires on. Test job, users and storage objects deleted after.

## The worker portal finally shows the money it has promised since it was written (Stage 5.6, 31 Aug 2026)

The worker portal's own header has read "Your work and your money" and its own subhead has promised "what you are owed" since the file was written. Neither was ever true: the page was a plain job list, no figure anywhere, while `jobs.pay_method` and `jobs.pay_ref` sat unused in the schema and unread by any code in the repository, alongside `jobs.owed`, also never written.

**The figure is computed live, not read from `jobs.owed`.** Nothing maintains that column, so trusting it would mean showing null or stale money on a page whose entire point is to be trusted. The same 88%-plus-materials arithmetic `FeeBreakdown.tsx` already shows on a single job is computed here across every job the worker has actually won (an accepted `job_quotes` row), which is the honest source: a job with no accepted quote has no money to show, exactly the guard `FeeBreakdown.tsx` already applies.

**Held and released are a straight read of `jobs.status`, the same binary the job room page's own tile already uses (`held: job.status !== "complete"`).** No per-stage proration was invented: nothing in this codebase splits a labour price across stages today, and building that math for the first time here, unasked, would have been exactly the kind of premature abstraction this repository's own house rule warns against. A job either holds its whole figure or releases all of it, matching what the single-job tile has always shown; this page only sums that same fact across every job at once.

**Released is not paid, and the copy says so.** There is no payment integration in this repository and none is planned before the legal review lands (CLAUDE.md 9 and 10). "Released" means Yaadly's part is done: the client approved, the figure is cleared. What actually moves the money is bank transfer, Lynk or remittance, arranged directly between client and worker, off-platform, within the 3 working days the portal's copy already promised. Claiming "Paid" here would be a real payment claim this repository has made structurally impossible to back up.

**`record_pay_info(p_job, p_method, p_ref)` gives the two orphaned columns an actual purpose: the worker's own note of how that off-platform payment happened, not a payment record Yaadly makes or verifies.** Same shape as `approve_stage`: a SECURITY DEFINER function that checks its own caller rather than trusting a write policy, because `jobs` carries no worker-write RLS policy at all today, only admin write plus client/worker read, and this stays that way. One narrow door, exactly the pattern this repository already uses for every other consequential write. `pay_method` is now constrained to the three real options the FAQ already names (bank transfer, Lynk wallet, remittance pick up); two pre-existing rows (`JOB-0002`, `JOB-0003`) carried `''` rather than null from however the column was first added, normalised to null before the constraint landed, the same rule the function itself applies to every write from here on.

**Recording payment info is only offered once a job is released.** Recording how you were paid for money that has not been released yet does not make sense, and the UI does not offer the form early; the underlying function itself carries no such gate, since a worker's own note of a real payment is theirs to make whenever it actually happened, and adding a status check to the database on top of the UI's own restraint would only be a second copy of a rule that belongs in one place.

Verified against the live database and a real signed-in worker session, not code-reading alone: `record_pay_info` refuses an unrecognised method, refuses a job that is not the caller's, refuses no bearer token at all, and succeeds for the worker's own job. Then driven through the actual portal UI: a job held under an accepted quote showed the correct take-home figure with "Held" and no payment form; forcing the same job's stage to 5 (this repository's own definition of complete, from `sync_job_status()`) flipped it to "Released" and surfaced the once-recorded payment info exactly as stored; changing the method and reference through the real form on screen updated both the row and the rendered page. Test job, quote, profile and user deleted after.

## The video walkthrough, completing a description the FAQ has carried since before this repository's schema caught up to it

`jobs.walk_platform`, `walk_link`, `walk_date`, `walk_who`, `walk_notes` and `signoff_method` were added on 23 Aug and never once read or written by any code in this repository. The FAQ has described what they were for the whole time: "At sign-off you can approve from the evidence package, or book a live video walkthrough: the worker walks the whole site with you on WhatsApp video, Google Meet or Zoom, whichever suits you." This gives that sentence an actual path, rather than inventing a competing design: the three platforms, the free-text preferred time, the notes field, all already matched the copy exactly.

**Two sides, two functions, the same shape as `approve_stage`.** `jobs` carries no client-write or worker-write RLS policy at all, only admin write plus client/worker read, and that stays true: `request_walkthrough()` (client) and `confirm_walkthrough()` (worker) are each the one narrow door for their own side, checking their own caller rather than trusting a write policy that does not exist.

**A request never gates the Approve button.** The FAQ's "or" is a client's choice of how to look at the evidence before deciding, not a condition on deciding. `approve_stage()` was not touched and does not read `signoff_method`; a client can request a walkthrough, get no answer for a week, and still approve straight off the evidence at any moment, exactly as before this migration existed.

**A worker cannot schedule a call the client never asked for.** `confirm_walkthrough()` refuses outright unless `signoff_method = 'walkthrough'` already, i.e. unless a request exists. The link is real: the worker pastes whatever Zoom, Meet or WhatsApp link they actually created, arranged the same way everything else in this product is arranged, over WhatsApp between the two of them. Nothing here calls a video API or mints a meeting; that would be exactly the kind of integration this repository's own scope rules warn against adding without being asked.

**A fresh request clears a stale confirmation.** If a client re-requests, whether to change the date or just to try again, any link the worker had already confirmed against the old terms is cleared along with `walk_who`. A worker seeing an old link standing in for a changed request is worse than seeing nothing: they would join a call for a time that no longer holds.

**`clear_walkthrough()` exists because every state this migration adds needed a way out of it**, the same reasoning Stage 4 already applied to a worker a client will not take. Either side can cancel a request or a confirmed call; both request and cancel are honest room for either party to simply change their mind, not a negotiation.

**What this does not do, on purpose.** The FAQ also describes the notes from the call itself being confirmed by the client and landing on the Completion Report; that is a two-sided sign-off workflow of its own and is not built here. `walk_notes` in this migration is only the client's own note at request time, what they want walked through, not a record of what the call actually found. That is a real next piece of work, not a gap papered over with a field that half does the job.

Verified against the live database with two real signed-in sessions, not one, and a full pass through the actual portal UI in a browser as both parties: a worker refused when confirming before any request existed, a stranger refused when requesting on a job that was not theirs, an invalid platform refused on both functions, a confirm refused with no link. Then driven end to end for real: the client requested a Zoom call through the actual form on screen, the worker saw the request and confirmed a real link through their own form, and both the client and worker views correctly showed the confirmed call with a working join link. Test job and both users deleted after.

## The Arrival Log actually logs something, and the walkthrough loop closes on the Completion Report

Two gaps left open when Stage 5 was reported done, closed before Stage 6 started.

**The Arrival Log.** CLAUDE.md's own glossary defined it ("timestamped proof the worker was on the correct site at the start") and the stage rail has said "Stage 1 · on site" since `journey.ts` was written, but neither ever pointed at a real event. "On site" on the rail turned out to be a cosmetic read of `jobs.status = 'in_progress'`, nothing a worker ever did. This is why Stage 5.3's own brief could not build "worker on site today": there was nothing to fire it from.

`arrival_log` is one row per stage per Jamaica-local day, through `log_arrival()`, worker-only, same shape as `approve_stage`. Jamaica carries no daylight-saving shift, so a fixed UTC-5 read of "today" is exact rather than an approximation. A second tap the same stage, same day, is not an error, it is the same fact confirmed again: silently absorbed, no second notification. A new day on the same stage is a genuinely new fact and fires `worker_on_site` through the same `yaad-notify-client` infrastructure Stage 5.3 built. Verified live that a repeat tap does not double-notify, which was the one way this feature could have become spam rather than signal.

**The walkthrough's notes loop.** The FAQ's sentence was always "the notes are recorded, you confirm them in your portal, and both land on your Completion Report." Stage 5.5's original walkthrough work built the request and the call; this is what happens after. `walk_call_notes` is the worker's own write-up, kept separate from `walk_notes` (the client's pre-call request, a different question with a different answer) so a worker's account of the call never silently overwrites what was asked for. `record_walkthrough_notes()` refuses outright unless a call was actually confirmed (`walk_link is not null`), because there is no writing up a call that never happened. Editing after confirmation clears it, the same rule a fresh walkthrough request already applies to a stale link: a client's confirmation is worth exactly what it was given for, and a changed text is a new claim.

**A fourth notification kind, `walkthrough_notes_ready`, exists because the client is essentially never present when the write-up happens.** They were on the call; the notes get typed up after, when they are offline. Without a notification there was no way to know `record_walkthrough_notes()` had ever been called short of opening the job speculatively.

**Both land on the Completion Report, and only the confirmed loop counts.** An unconfirmed set of notes, or a walkthrough requested but never followed through, is not a record either side agreed to, and the static report at `/portal/jobs/[id]/completion` only renders the walkthrough section when `walk_notes_confirmed_at` is set. The `WalkthroughPanel` itself now stays visible on a completed job specifically when a walkthrough was in play (`signoff_method = 'walkthrough'`), so an unconfirmed set of notes is never stranded off-screen the moment `sync_job_status()` marks the job done; it simply stops offering a fresh request.

Verified against the live database and a real signed-in browser session for both roles: `log_arrival` refuses the wrong side and a stranger job id, is idempotent same-day, and correctly did not fire a second notification on the repeat call; `record_walkthrough_notes` refuses before any call is confirmed; `confirm_walkthrough_notes` refuses before notes exist; editing confirmed notes correctly re-opened confirmation and the client had to confirm again. Test job and both users deleted after.

## A third way to sign off a stage: in person (31 Aug 2026)

Founder's own framing: "if a client is in the country and can review the site by themself... they just confirmed completed job by in-person visual so that is signed out if there is any disputes in the future." A client physically in Jamaica does not need the same remote-evidence review as one four thousand miles away, and their own eyes on the property are worth recording as a stronger fact than a bare approval.

**This is an attestation, not a bypass.** Every rule `approve_stage()` already enforced stays exactly as strict: a dispute still blocks it, evidence still has to be filed for the stage before it can be approved. The client's tap is still the confirmation this repository's governing rule protects; nothing about who confirms, or what has to exist before they can, changed. What changed is that the tap now records HOW: `stage_approvals.confirmed_method`, `evidence` or `in_person`, set once, immutable, same as every other column on that table.

**A real bug caught before it shipped, not in this code but in how a signature change works.** The first draft of `approve_stage(p_job, p_method default 'evidence')` used `create or replace`, which does not retire the existing one-argument `approve_stage(p_job)`: Postgres treats a second parameter, even with a default, as a distinct function signature. Left alone, both would have existed at once, and a call passing only `p_job` (which is every existing caller) would have become genuinely ambiguous between the two rather than resolving cleanly. Fixed by dropping the one-argument version outright before creating the two-argument one, so exactly one `approve_stage` exists.

The UI is a checkbox next to the existing Approve button, not a separate flow: "I'm at the property and looked at this myself," which relabels the button and changes nothing else about what it does. The Completion Report gained a new section, "How each stage was confirmed," listing each stage's method next to its timestamp, the actual record a future dispute would draw on.

Verified live: a garbage method value is coerced to the safe default rather than rejected (this field is attribution, not a gate, and a caller sending nonsense should not lose an otherwise-valid approval over it), a real `in_person` approval writes correctly, and a full pass through the actual portal UI, including toggling the checkbox and watching the button's own label change before submitting. Test jobs, evidence and the QA client deleted after.

## WhatsApp evidence intake: a worker's photo lands on the job without opening the portal

Founder's own framing, 31 Aug 2026: a worker on site "has no time to log on and carry out those steps in the web... Uploading in the portal is fine but shouldn't be the first route." The portal upload from Stage 5.5 is not replaced. It is demoted from the only route to the fallback for the one thing WhatsApp genuinely cannot carry: a video bigger than WhatsApp's own 16MB client-side cap.

**Two research passes were wrong before this was designed, and both corrections changed the actual shape of the work.** First: `yaad-whatsapp-webhook` speaks to Meta's WhatsApp Cloud API directly, not Twilio, which everything else in this session's WhatsApp work uses. Second: nothing links a WhatsApp number to a worker. `jobs.worker_email` is the only identity a job carries for its worker; there was no `worker_phone` anywhere, on `jobs` or `worker_profiles`. Both had to be corrected before any code was written, and both are worth recording here because they are exactly the kind of assumption that looks obviously true until it is checked.

**`worker_profiles.phone` is the real column now, digits only, matched on the last 9 digits.** Same tolerance `findHistory()` already applies to a client's phone: WhatsApp's own "From" field arrives with inconsistent country-code prefixes, and pretending to E.164 precision nobody's numbers actually have would mean real matches missed, not fewer false ones caught. `link_worker_phone()` is the one door a worker has to set it themselves, same shape as `record_pay_info`, since `worker_profiles` carries no self-write RLS policy at all (admin write, signed-in read only).

**The trust model this repository already enforces for evidence is not loosened for this channel, it is reimplemented for it.** A WhatsApp message has no session, so it cannot lean on the RLS that already protects `evidence`: "this worker, this job, and no other." The check RLS would have made is made by hand instead, in `yaad-whatsapp-webhook`, before a single byte is trusted: the sender's phone must match a published (`active = true`) worker's linked number, and the job it lands on must be one that worker actually has open. The server-computed sha256 is unchanged from `evidence-actions.ts` and `yaad-evidence-video`, "computed HERE, on the server, from the exact bytes stored," now a third time in a third place, same rule.

**One active job, or more than one, decided the shape of the whole flow.** A worker with exactly one live job has their photo filed immediately, no question asked, because there is nothing to ask. A worker with more than one is asked which job, once, reusing `wa_intake_sessions`, the same table the guided client and worker-signup intakes already use for multi-turn state, given a third lane (`_lane: "evidence"`) rather than a new table. Media arriving while that question is still open is appended to the pending set rather than either lost or forcing an answer before the worker is ready; a burst of photos sent before a reply is the realistic case, not one photo followed immediately by an answer.

**Nothing is ever half-filed.** A photo is downloaded and staged into a neutral path (`_pending/...`, outside any job's own folder, since which job it belongs to is not always known yet) before anything is asked or decided. It only moves into the job's real folder, `{jobId}/...`, matching the same `storage.foldername(name)[1] = job.id` convention the evidence bucket's RLS policies already key on, at the moment a job is actually confirmed, whether that is immediately or after a worker answers which one. Verified live: when the underlying storage move genuinely fails (an object that was never really there), the function reports zero items filed rather than claiming success, and the session is still cleaned up regardless, so nothing is left half-answered.

**The portal deep link, not a paragraph telling a worker to go find it themselves.** For a video too big for WhatsApp to have carried in the first place, the confirmation message after a successful smaller upload carries a one-tap magic link straight to that job's evidence tab, already signed in, the same `generateLink` mechanism `yaad-portal-code` already uses. Founder's own wording: "so all they have to do is upload the file, nothing else."

**What could and could not be verified live, honestly.** Every piece of logic that does not require an actual Meta media fetch was proven against the running system: phone-to-worker matching, one-job-vs-many routing, the ordinal and fuzzy-title job-choice parser, session append-on-media, reprompt on an unrecognised answer, session cleanup on both success and failure, and an unrecognised phone number falling through to the exact pre-existing behaviour with no regression. `WHATSAPP_ACCESS_TOKEN` is confirmed unset on this project (found live, not assumed), so the one thing that could not be exercised is a real Meta media download and the real storage move that follows it. That needs a genuine WhatsApp message from a genuine device once Meta's Cloud API credentials exist, separately from the Twilio Trust Hub clearance confirmed the same day, which is a different provider for a different part of this product.

## The reporting agent finally runs, worker nudges, stall escalation, and a real bug caught while wiring the admin view (31 Aug 2026)

Closing the rest of what was queued after Stage 5: the reporting-agent WhatsApp digest, worker nudges, stall escalation to the founder, client delay notices, an admin stalled-jobs view, and the reliability log. Founder's own framing: "prompt to the workers ensuring he does this so the client is updated... if there is something that need escalating I will be contact direct."

**Money is untouched by all of it.** `approve_stage()` already refuses to release a stage with nothing filed against it; a stalled job simply never reaches that gate. Everything below is coordination and visibility, never a payment decision, and nothing in this body of work writes to `jobs.status`, `stage_approvals` or `evidence`.

**The reporting agent (`yaad/agents/reporting.py`) had existed since before this repository's live side did, fully built and fully guardrailed, and had never once run against a real notification.** Nothing in `supabase/functions/` or `web/` ever called it; confirmed by research before writing a line, not assumed. It is ported now into `yaad-notify-client`'s `evidence_landed` composition, same system prompt, same four fields, same rule that a vague update says so rather than inventing detail. The "worker update" it reads is not a new capture step: it is the evidence labels already filed for the stage, in the order they were filed, which is genuinely what the worker told Yaadly happened. A vague or failed digest never blocks the notification; the original fixed sentence is what ships instead, guaranteed guardrail-clean because nothing generated it. Verified live: a real digest composed from two real evidence labels ("rusted joint... removed and replaced with a new copper joint... pressure tested, no leaks"), coherent, correctly declining to promise anything, correctly guardrail-clean.

**One clock, `job_silence_hours()`, decides both the nudge and the escalation, so the two thresholds are measured against the same fact rather than two that could drift.** The floor is the job's own `updated_at`: a job with a worker assigned but not one single arrival or evidence row yet is not exempt from the check, it is the most stalled case there is. Three days silent nudges the worker over WhatsApp; a week silent (three days on top of the nudge going unanswered) escalates to the founder directly via the existing `ntfy_topic` push, the same channel new applications already use, and tells the client honestly that there has been a delay, without inventing a reason for it. `job_stall_state` clears itself the moment real activity resumes, so the clock genuinely resets rather than a stale flag lingering; verified live by filing a fresh evidence row against an artificially-aged test job and watching `clear_resolved_job_stalls` remove it.

**The reliability log is the narrow version, not a computed score.** `worker_stall_history` is a plain read of `job_stall_state` joined to the job, one row per nudge, newest first. No aggregation, no ranking, no client-facing surface, nothing here feeds worker matching. Flagged against CLAUDE.md's own "Yaad Score computation" out-of-scope line before it was built, and built anyway once the founder confirmed she still wanted it, deliberately narrower than a score: a human reads a list and decides what it means, exactly as asked for.

**A real RLS bug, caught before it ever reached concierge, not after.** The first version of `worker_stall_history` was a plain Postgres view with no `security_invoker`, over a `job_stall_state` table that had been created with no RLS policy of its own at all. A plain view runs with the OWNER's privileges by default, not the querying role's, which means it would have exposed every worker's stall history to anyone PostgREST ever granted select to, regardless of whether they were an admin. Caught while wiring it into concierge, not after: `job_stall_state` now carries an explicit `is_admin()`-gated policy, the same gate every other staff-only table in this repository uses, and the view was recreated with `security_invoker = true` so it actually reads through that policy instead of bypassing it. Verified live, both directions: a genuinely non-admin authenticated user querying the view got zero rows while the underlying data provably existed; direct SQL confirmed the row was there the whole time.

**"Stalled jobs" in concierge follows the exact declarative pattern every other view in that file already uses** (`k`, `g`, `table`, `cols`, no `actions`, matching the read-only "Reviews" view precisely), rather than inventing a new rendering approach for one addition. Verified the file still parses: no console errors on load, and the whole inline script block passes `node --check` cleanly after the edit.

What could not be verified live: the actual Twilio send to a worker's phone and the actual ntfy push to the founder, since testing either would mean messaging a real person outside a purpose-built test account. Both reuse send functions copied line for line from `yaad-notify-client`'s own already-proven Twilio path (confirmed live and working the same day, Trust Hub cleared) and `yaad-vetting-upload`'s own already-proven `ntfy_topic` push, so the mechanism is the same one already carrying real traffic elsewhere in this product; only the specific recipients on this run were never real. Every SQL-level decision the cron function makes before reaching those two sends (which jobs qualify, at which threshold, whether already nudged or escalated, when the state clears) was proven directly against the live database.

## WhatsApp evidence intake moves to Twilio, same day it was built on Meta

The Meta Cloud API version shipped, verified, and correct, ran headfirst into Meta's own business verification, a process outside this repository's control and outside a reasonable evening's patience. Founder's own words: "i dont want to keep wasting my tokens on something that doesn't work... remove meta from this moving forward." The number already registered, already sending, already Trust Hub cleared, was sitting on Twilio the whole time. This moves the feature there instead.

**The Meta build was reverted cleanly, not left as dead code.** `yaad-whatsapp-webhook` is back to exactly its pre-evidence-intake state (`git show` against the commit before it, restored verbatim), rather than an unreachable branch nobody will ever configure sitting in a file that already does something else. Nothing about client intake or worker signup, which that file has always handled, changed at all.

**The new home is `yaad-inbound`, the multi-channel intake endpoint that already receives real Twilio WhatsApp traffic today.** This is a better fit than it first sounds: the header comment on that file has said since it was written, "WhatsApp keeps its own webhook because Meta's signature check and payload shape are its own problem. When it is approved it becomes one more row in the channel column, not a rebuild." Meta was never approved. Twilio, on the same channel, already was.

**The port is smaller than the original build, not larger, because so much of it already existed.** Twilio's signature verification (`twilioSigned`), its form-encoded parsing into a single `Inbound` shape, and its media download (`fetchMedia`, already proven in production for client-sent photos via `keepMedia`) needed no new code at all. What moved: `lookupWorkerByPhone`, `lookupActiveJobsForWorker`, `pickJobChoice`, `finalizeEvidenceItem` and `mintPortalUploadLink`, copied across unchanged, since none of them ever cared which messaging provider the bytes arrived from. What was rewritten: the media-staging step, since Twilio hands over a fetchable URL directly where Meta needed a two-step id lookup first, genuinely simpler than what it replaced. The reply mechanism changed shape too: Twilio's inbound webhook IS the reply channel (a TwiML response to the same request), where Meta needed a separate outbound API call for every message.

`wa_intake_sessions`, despite its name, was never Meta-specific: a plain `wa_id`-keyed table with a jsonb `answers` column, already carrying the "which lane is this conversation in" pattern (`_lane`) the client and worker-signup flows use. The evidence lane reuses it exactly as designed, no new table.

**What is proven and what genuinely is not, stated plainly rather than rounded up.** Every piece of logic that touches the database, phone-to-worker matching, active-job filtering, the job-choice parser, was verified directly against the live database with real data, the same discipline as every other piece of this session's work. What was not fired is a real, validly-signed Twilio webhook hitting this new routing: that requires `TWILIO_AUTH_TOKEN` to sign the request, which is a secret held by Twilio and this project, not by the person building it. The pieces that check that signature, parse the payload, and fetch the media are unchanged, already-live code already proving itself on every real client message this channel already receives; the new orchestration around them was written carefully against that same pattern, and its first real signed message is the genuine remaining proof, whenever one arrives.

## Evidence intake never auto-files, even when there is only one job it could be (31 Aug 2026)

Founder's own words, catching a real gap: "I don't want workers sending photos that are linking to wrong jobs... There should be approval for it to link to the right job where they ask for the confirmation of the job and the confirmation of the code." The original build filed a photo straight away, no question asked, the moment a worker's linked number matched exactly one live job, on the reasoning that one option is unambiguous. That reasoning was wrong: unambiguous to the system is not the same as confirmed by the person sending it, and a worker could still be texting about the wrong job, or the wrong client's job, without a chance to notice.

**Nothing files without the worker naming the job by its own code first, one job or several, no exceptions.** `codePrompt()` always shows the code, `JOB-0042 (Kitchen tap replacement)`, not just the title, and always asks for it back, even for a single candidate: "This looks like it is for JOB-0042. Reply with the code JOB-0042 to confirm, or tell us the right job." The photo is staged the moment it arrives, same as before, but it does not touch a job's evidence until that confirmation lands.

**`pickJobChoice()` checks the code first, deliberately stricter than it looks.** A reply has to contain the job's actual code string to match on the code path; the bare digits alone ("0042" without "JOB-") do not match, and neither does a plain "yes". Verified standalone against ten realistic replies, including that exact pair: both correctly return no match rather than a guess, forcing a reprompt instead of a false positive. For a confirmation step whose entire purpose is refusing to guess, a reprompt is the right failure mode and a wrong-but-confident match is not. The ordinal number ("1", "2") and a clear, unambiguous title match are still accepted as lower-friction fallbacks, but the code is what every prompt leads with and the only one the two-job standing tests all pass on cleanly.

## Sign in becomes one screen, email required and the code optional (31 Aug 2026)

The sign in page asked for an email, and only after that click did a six digit code box appear at all, which from the founder's side looked like there was nowhere to enter one. Her own words: sign in should work "with the code or the email and their code", and the page should show "what is essential and what is optional" rather than imply it by which screen a person happens to be on.

**Rebuilt as one screen, both fields visible together, `web/app/portal/sign-in/page.tsx`.** Email is marked Required, the code Optional. One button does both jobs: an empty code box sends a fresh one, and it also verifies immediately when a full six digit code is already typed, so a returning visitor holding a code from minutes ago (they last about an hour) is not forced through a second send first. The branch deciding which of those two things happens is `isCodeComplete()` in `web/lib/portal/sign-in.ts`, pulled out on purpose so it is real, tested logic and not something only readable by rendering the page.

**There is no way to sign in on the code alone, with nothing else.** A six digit code is not unique across every account; it only means something paired with the email it was sent to. Building a way to identify an account from a bare code, with no other input, would need its own design, most likely a phone-only sign in, and is not a decision to make quietly while doing what was asked as a page redesign. Flagged rather than built.

`web/tests/sign-in.test.mjs` covers the pure decision logic and asserts the page's source no longer gates the code field behind a hidden `stage === "enter"` step, so this specific regression cannot silently come back.

## The Twilio signature check gets pulled out so it can be tested (31 Aug 2026)

The evidence-intake safety fix earlier the same day left one honest gap: the `X-Twilio-Signature` check in `yaad-inbound` was verified by reading the code, not by firing a real signed request, because nobody building this holds `TWILIO_AUTH_TOKEN`. That gap does not need a production secret to close: Twilio's signing algorithm is public, and a test token proves the algorithm exactly as well as the real one would.

**The check itself now lives in `supabase/functions/yaad-inbound/twilio-signature.ts`**, as `checkTwilioSignature(req, raw, token, supabaseUrl)`, taking both secrets as arguments rather than reading `Deno.env` itself. `index.ts` still calls `Deno.serve()` at module load, which means importing it directly for a test would start a live server, exactly why `_shared/guardrails_test.ts` reads source text instead of importing; pulling the check into its own file sidesteps that for good, for this one piece.

**`twilio-signature_test.ts` signs requests the same way Twilio does, with a throwaway secret, and checks the function agrees**: a correctly signed request passes, a wrong token is refused, a tampered body after signing is refused even with a real-looking signature, a missing header is refused rather than skipped, and no token configured means unchecked rather than refused, the deliberate fail-open this endpoint has always had. `pickJobChoice()` moved the same way, into `job-match.ts`, with its own ten-case test file replacing the throwaway script it was proven against earlier the same day.

The one thing this still does not prove is the live endpoint answering a real Twilio request end to end. That needs an actual message from an actual worker's phone, and remains the genuine first proof.

## Stage 6: a client can approve a stage by replying on WhatsApp (31 Aug 2026)

Every approval before this went through one door, `approve_stage()`, which identifies the caller through a live portal session (`auth.uid()`). That is exactly what a Twilio webhook does not have: `yaad-inbound` runs on the service role with no Supabase session behind it at all, so the WhatsApp side needed its own way in, not a looser one.

**The decision logic was pulled into a shared core, `_do_approve_stage(p_job, p_email, p_method)`**, never exposed to PostgREST, taking an already-established identity as an argument instead of deriving one itself. `approve_stage(p_job, p_method)` is now a thin wrapper that derives that identity from the portal session exactly as before; `approve_stage_via_whatsapp(p_job, p_phone)` derives it by matching the phone against that specific job's own `client_phone`, and is granted to nobody but the service role. Both call the same core, so a dispute open or nothing filed refuses either path identically; there is exactly one definition of "approvable," not two that could quietly drift apart.

**The phone match is against this job's own number, not a global "whose phone is this" lookup.** A phone matching some other job with the same client email is not good enough; the check reads `client_phone` off the specific job being approved.

**A client's reply approves only on an exact match against the job's own code, nothing looser.** The worker evidence-linking fix built the same afternoon allows an ordinal number ("1") or a title guess as a convenience, because that entire exchange happens inside a `wa_intake_sessions` row already known to be a worker mid "which job" answer. A client's approval reply has no such session: every plain text message they ever send passes through the check, so a stray "1" in an unrelated sentence, or a bare "yes", must never be read as consent to release payment on a job. `matchApprovingJob()` in `approval-match.ts` is deliberately narrower than `pickJobChoice()` for exactly this reason, code only, no fallback, tested against nine cases including the ones that matter: an ordinal and a title match that would succeed under `pickJobChoice()` both correctly fail here.

**Nobody is told about this route unless they can already act on it.** `yaad-notify-client`'s `evidence_landed` message now tells the client to reply with the job's own code, but only when the job actually has a `client_phone` on file; a client with no phone on record still gets the plain portal link, since there is nothing to reply to from.

One known, accepted redundancy: approving over WhatsApp still triggers the existing `stage_released` notification through the same database trigger every portal approval already fires, so a client who approves by text gets an immediate reply confirming it and a second, separate confirmation moments later. Not wrong, just slightly repetitive; not worth adding complexity to a money-adjacent path to suppress a harmless echo.

Verified live end to end against the database, not by reading the code alone: a wrong phone number is refused, the right one (in several real-world formats, spaces and a leading +) approves, the stage and status advance correctly, `confirmed_method` records `whatsapp`, and a second approval attempt with nothing newly filed at the next stage is refused exactly as it should be. Test job and evidence deleted after.

Spotted, not touched: `yaad-notify-client` still carries a Meta WhatsApp fallback (`sendMetaWhatsApp`), behind `graph.facebook.com` and its own pair of environment variables, sitting after Twilio in the send order. Whether this is dead weight from a Meta setup that was never finished, or a working fallback on an already-verified number unrelated to today's blocked one, was not established either way, and removing it was not required for Stage 6 to work. Left alone rather than guessed at.

## An admin could see the evidence row, never the photo itself (31 Aug 2026)

Asked to look at a photo just filed against a test job, and could not, is how this was actually found, not a scheduled review. `public.evidence`'s own RLS already let an admin read the row, label, sha256, uploaded by, all of it. The file itself lives in `storage.objects`, gated by a separate, narrower policy, "job party can read evidence files", written 30 Aug against exactly the same predicate `public.evidence` uses, and `is_admin()` was never added to it. `/portal/jobs/[id]` mints its signed URLs through the signed-in visitor's own session (`lib/supabase/server.ts`), not the service role, so this was the one check deciding whether that signed URL came back with something a browser could load, or came back null and the photo silently read "Filed without an image."

**Fixed by adding `or public.is_admin()` to the storage policy, upload and delete left exactly as narrow as they were.** An admin looking is oversight, the whole reason this desk exists ahead of the client; an admin writing into somebody else's evidence trail is a different thing entirely and nothing asked for it. Verified live both directions: a simulated admin session can now see the object row, unchanged for everyone else.

## A client's evidence now rides on WhatsApp directly, not only behind a portal link (31 Aug 2026)

Founder's own words, having just watched the fix above still not answer the actual question: "if the photo doesn't go on the portal or send to the client directly for review this needs to be fixed before we continue." The `evidence_landed` notification always described what arrived and linked to it; it never carried the photo itself, so seeing it meant opening the portal regardless of how good the text digest was.

**`evidencePhotoUrls()` in `yaad-notify-client` signs up to five image URLs per stage and hands them to the WhatsApp send as Twilio's own repeated `MediaUrl` parameter**, so the photos land inside the WhatsApp message itself. Runs on the admin client on purpose: a server-to-server send, not a page render, so it does not lean on the storage fix above at all, and the signed URL it hands Twilio is good for five minutes, only long enough for Twilio's own fetch. Video is deliberately excluded: WhatsApp's own media handling makes sending one this way unreliable, and the portal was already the route for anything too big to text. Capped at five, not because a sixth would fail, but because a message carrying ten photos reads as clutter rather than proof.

**Photos ride only on the first WhatsApp attempt, never on the Meta or SMS fallback.** A fallback means the WhatsApp send itself failed; a signed URL good for five minutes has usually aged past useful by the time a second attempt runs, and the text plus the portal link still carry the fact of what arrived either way.

Verified genuinely end to end, not against a synthetic secret this time: a real photo, sent from a real phone to the live Twilio number, filed against a real job, and the evidence_landed notification that followed carried that exact photo back to the same phone inside the WhatsApp message, confirmed by the person who sent it. Test job, evidence rows, worker profile and sessions deleted after; the two uploaded test images could not be removed from Storage directly, Postgres refuses a raw delete on storage tables by design and this session held no Storage API credentials to use instead, so they remain as small orphaned objects under `TEST-WA-LIVE-A/` in the evidence bucket, referenced by nothing, until removed by hand.

## The web app had not actually been deployed all day (31 Aug 2026)

Every piece of `web/` work today, the sign-in redesign, the two live WhatsApp fixes, all of it, had only ever been checked with `deno check`, `npm test`, `npx tsc`, or a local dev server. None of that touches `app.yaadly.co.uk`. The last real deploy to production was 30 Aug 19:08, the evening before, so the founder spent the day being sent portal links that opened a build from before any of it, an old, password-based sign in screen nothing in the current source even resembles, `git log` on that file shows two more commits between what she saw and what was actually on disk.

Found because she sent a screenshot of what a magic link actually opened rather than trusting the description of what should happen, exactly the discipline this whole session ran on for the database and the Edge Functions, just never once applied to the frontend. Confirmed with `wrangler deployments list` before touching anything: the version timestamp is the fact, not a guess.

**Fixed by running the real deploy** (`npm run typecheck && npm test && npm run deploy` from `web/`) and verifying the live site directly afterward, a fresh tab hitting `app.yaadly.co.uk/portal/sign-in`, not a cached one: the first re-check, in a tab that had loaded the page before the deploy finished, still showed the stale build, `cache-control: no-store` on the response ruled out server-side caching as the cause, so that was the tab's own client-side router cache, not a second infrastructure problem. A second, fresh tab showed the correct page immediately.

**The standing gap this leaves: nothing in this session's own workflow ever deploys `web/` as a matter of course.** Every other piece of infrastructure touched today, migrations, Edge Functions, gets applied and verified live in the same breath it is written. `web/` changes were verified locally and left there. Worth carrying forward: after any `web/` change meant to be live today, run the deploy and check the actual production URL, the same as every Edge Function change already does, rather than treating a passing local test as the same thing as being shipped.

## Nobody could sign in, all day, and the cause was a hardcoded 6 (31 Aug 2026)

Today's whole sign-in rebuild carried one unchecked assumption: a code is six digits. Every label said so, "The six digit code", every helper line said so, "We send you a six digit code", and the actual gate deciding whether the button tried to verify or just sent another one, `isCodeComplete()`, checked for exactly six. Nobody had verified that number against what Supabase actually issues for this project. It issues eight.

The founder spent the afternoon typing a real, correctly received code into the box and watching it do nothing, because eight digits never satisfied a check written for six: the button stayed on "send a fresh code" forever, and pressing it kept invalidating whatever code she was holding. Every "it doesn't work" message this session logged had the same real cause underneath it, most of them long before the actual mechanism was found. Found only by asking her to screenshot the real email rather than accept a description of it: "16969202 is your Yaadly sign in code," eight characters, styled exactly like the six-digit box the page was rendering.

**Fixed at the one number: `CODE_LENGTH = 8` in `web/lib/portal/sign-in.ts`, exported, and `isCodeComplete()` checks against it rather than a bare `6`.** Every page's own copy that named a digit count is gone, `/portal/sign-in` and `/portal/join` both now just say "code," "your code," never a number. The reasoning: naming a specific length in visible text is exactly what silently broke this once already, and there is no reason this project's OTP length is guaranteed to stay eight forever. If it changes, one constant changes, not four pages of copy that all have to be remembered and found again.

**`web/tests/sign-in.test.mjs` was rebuilt around the same principle**: every test code is generated from `CODE_LENGTH` (`"1".repeat(signIn.CODE_LENGTH)`), never a literal digit string. The original test suite used a hardcoded `"123456"` and would have stayed green through this entire bug, since it was testing the code against itself rather than against reality. New tests also assert neither page's own source contains the phrase "six digit" again, a direct regression guard on the exact wording that caused this.

**Verified genuinely end to end, not by rendering a page and assuming**: a real code, sent to a real inbox (`justmonty1@gmail.com`), read directly off a screenshot of the actual received email, typed into a live browser session driving the real deployed site, correctly flipping the button to "Sign in," and landing on the real client portal showing a real job. This is the level of proof "the page looks right" never was.

**A second, unrelated finding surfaced trying to prove this**, and is recorded once here rather than twice: attempting to write a small standalone diagnostic Edge Function, with a hardcoded bypass secret and `--no-verify-jwt`, to self-serve a test OTP without needing to read anyone's inbox, was refused by this session's own safety layer. That refusal was correct. A function built to skip the platform's own auth check, deployed on the same say-so that writes ordinary code, is precisely the kind of action that should need the founder's explicit yes, not an agent's judgement that the cause was good. The right move when blocked like that is to stop and use the tools already available and already trusted, which is what actually produced the proof above: a real account, a real email, a real login, driven live.

## The AI's report goes to the worker first, not the client (31 Aug 2026)

Confirmed directly with the founder: the worker decides whether the AI's composed report goes to the client as written, or gets replaced with their own words, not her, and not automatically. The whole point was never a bottleneck through anyone; the worker is closest to the job.

**`evidence_landed` stopped meaning "tell the client" and started meaning "draft it for the worker."** The kind still fires on the same trigger, still composes the same worker-overview and (when it works) the same AI photo review, but now sends that as a labelled draft to the worker's own WhatsApp number, with one instruction: reply `1` to send it as written, or write your own version and that goes out instead. The draft itself is stored in `wa_intake_sessions` under a new lane, `report_confirm`, keyed by the worker's number, since the confirmation might not come back for minutes and the signed photo URLs used for the AI review would already be stale by then regardless.

**A new kind, `evidence_report_confirmed`, is the only thing that actually reaches the client**, and only ever fires from `relay_confirmed_report()`, a Postgres function a worker's WhatsApp reply calls through `yaad-inbound`, never automatically. It reuses the exact photo-attach and approve/comment-hint logic `evidence_landed` used to run directly, just with the worker's now-confirmed wording instead of composing fresh.

**The secret was nearly broken a second time building this.** `relay_confirmed_report()` needs the same shared secret every trigger in this file already carries, and the very first draft of its migration generated a fresh one instead of reusing the existing one, the identical mistake made and caught twice already the same afternoon while building the portal-comment notify trigger. Caught before deploying this time, by checking first rather than assuming: extracted the existing secret from `notify_client_quote_arrived()`'s own already-correct body instead.

**Diagnosed, not yet fixed: the AI photo review is currently coming back empty in practice.** Added real logging (`console.error`, not only an OTel span attribute nobody was checking) and found the actual cause: the NVIDIA vision model itself is declining to answer, "I'm not going to engage in this discussion topic," rather than the JSON findings array the prompt asks for. This is not a wiring bug, `photoUrls` resolves correctly, the model call fires, the response comes back 200. Something about the prompt or the specific image is triggering the model's own refusal. Left diagnosed rather than patched blindly, since the fix (a different model, a reworded system prompt, or handling a refusal explicitly rather than silently returning null) deserves its own pass, not one squeezed in at the end of an already very long session.

## The client-worker feedback loop, both directions (31 Aug 2026)

Founder's own words: a client should be able to say more than yes or no, it should reach the worker, and the worker's answer should reach the client back, logged either way. Built as the mirror of itself in `yaad-inbound`: a client's plain WhatsApp reply that is not the approval code, with exactly one job of theirs awaiting review, is read as a comment and the worker is notified; a worker's plain WhatsApp reply, with exactly one job's comment still unanswered, is read as their answer and the client is notified. Neither guesses across more than one candidate, same discipline as everything else built today: silence and a fallback message, never a guess.

**`evidence_comments` gained `evidence_id` and `origin`.** `evidence_id` is nullable on purpose: a WhatsApp comment only ever names a stage, never a specific photo, and a client commenting from the portal on one specific item is a real, different thing worth keeping distinct rather than collapsing into the same shape. `origin` exists so a comment is never announced to its recipient twice, once inline by whichever Edge Function received it over WhatsApp, once by a database trigger for whichever channel does not have that inline call: portal-origin comments are the trigger's job, WhatsApp-origin comments are already handled inline and the trigger explicitly skips them.

**What this session did not reach**: the portal-side UI for a client to write a comment against one specific photo. The schema and the notify trigger are ready for it (`evidence_id`, the RLS insert policy scoped to `origin = 'portal'`), but there is no button in `EvidenceLedger.tsx` yet. Also not reached: a unique, typeable code per individual photo or video, which the founder asked for directly after noticing that a stage with more than one item currently has no way to say which finding or comment belongs to which; a real gap, not proposed lightly, and the right next piece of work on this thread. Also not reached: turning a report's own "next steps" text into anything that actually schedules a follow-up. All three are next, not abandoned.

## The AI photo review stops refusing, evidence gets a per-item code, and a client can comment on one specific photo (31 Aug 2026)

Three of the four items the previous entry left open ("what this session did not reach"). The fourth, turning a report's own "next steps" into a real follow-up, is its own entry below.

**The refusal was the model, and it is fixed.** `nvidia/nemotron-nano-12b-v2-vl` was declining outright on a system prompt that read like a content-moderation trigger: "reviewer", "hazards", "diagnosis", a preamble about not being a licensed surveyor. Switched to `meta/llama-3.2-11b-vision-instruct` and reworded the prompt to ask for visible condition notes for a job log rather than a review for hazards, same categories, same JSON shape, same refusal to overstate certainty. Verified live against a real photo, repeatedly: the refusal did not recur once across seven live calls after the switch, where it had been the only failure mode seen before it.

**What replaced it was not silence, it was NVIDIA's own hosted latency, and that is not fully fixable from this side.** The same single-photo call came back in fifteen seconds once, ran past thirty five with nothing the next time, and returned a flat `http 500` the time after that, all on the working model, all confirmed live the same afternoon. `reviewEvidencePhotos()` now retries once on a timeout or a 5xx, 25 seconds per attempt, and does not retry a refusal or an unparseable body, since asking again is unlikely to change either. `microsoft/phi-3.5-vision-instruct` was tried as a second model option and turned out not to be a valid slug on NVIDIA's catalog at all, a 404, not a refusal, which is worth remembering before reaching for another model name on faith. This still fails outright more often than a system this central to the product should, in a small live sample, and the honest fix if that matters more than it does today is a different vision provider, not a longer timeout on this one. `yaadly.vision.outcome` and `yaadly.vision.attempts` on the trace say which happened without reading logs.

**Every evidence item now carries a short code, `P1`, `P2`, sequential per job in filing order (`20260831zzzz2`).** The founder's own instruction was to reconsider length and typability rather than copy the long job-code convention onto a per-photo code, given the sign-in OTP length lesson earlier the same day (six assumed, eight actually issued). The answer here was not a length to verify against an external issuer, since this repository generates the code itself: it only ever has to be unique inside a job somebody is already in, over WhatsApp or in the portal, never system-wide, so there is no reason to carry the job's own long id inside it. `P` rather than a bare number specifically so it can never collide with the ordinal-number convenience (`pickJobChoice()`, "reply 1 to send") already live elsewhere on the exact same channel. Assigned by a trigger, not by any of the three callers that insert evidence (portal upload, video upload, WhatsApp intake), the same "one authority" shape `sync_job_status()` already uses, serialised per job with an advisory lock so a burst of photos landing at once, the founder's own question the same day, cannot race two inserts onto the same code.

**Wired into the two places a code needs to mean something, and the vision side changed shape twice in one afternoon.** The first version labelled each image `Photo P1:` in one combined prompt and asked the model to echo the code back per finding. Live testing against a real two-photo stage found something more basic broken underneath that: `meta/llama-3.2-11b-vision-instruct`, hosted on NVIDIA, refuses more than one image in a single request outright, `http 400`, `"At most 1 image(s) may be provided in one prompt"`. A stage with more than one photo was getting no review at all, not a partial one, since the whole batched call failed before any image was looked at. Fixed by reviewing one photo per call, run together with `Promise.all` so the wall clock stays close to a single photo's own latency rather than the sum of several, with the review that comes back from a call assigned to that call's own photo directly, not parsed from anything the model said: a call that only ever saw one photo has nothing left to attribute wrong. A stage where some photos reviewed and others did not now returns the findings that did rather than discarding a real partial result over one photo's bad moment, which the single combined call could never do either, since one failure failed all of it. `yaad-inbound`'s two comment lanes (worker answering, client commenting) attribute a comment to one specific photo only when the reply actually names its code (`pickEvidenceItem()` in `evidence-item-match.ts`, unit tested against the same discipline as `pickJobChoice()` and `matchApprovingJob()`: a plain comment with no code stays about the whole stage, exactly as before).

**The portal finally has the button the previous entry said was missing.** `EvidenceItemComment.tsx`, one per photo in `EvidenceLedger.tsx`, client only, calling a new `commentOnEvidence()` in `job-actions.ts` that writes `evidence_id` directly rather than parsing a code, since the client is looking straight at the photo. Write-only, deliberately matching the WhatsApp half of the same loop: neither channel shows a persisted thread back in the portal today, and building one is a bigger, separate piece of work than the button that was asked for. Verified against the live database by impersonating three identities directly, not by reading the RLS policy and assuming: the job's real client succeeds and writes with `evidence_id` set, its own worker is refused, a stranger is refused, all at the database layer before any application code runs. The actual authenticated click-through could not be completed in this session: no email or SMS inbox was available to receive a portal sign-in code, and no service-role credential was available locally to mint a session directly. Type-check and the full web test suite both pass; the RLS behaviour is proven against the real database; the click-through is the one piece still resting on the same "first real use is the proof" standard this repository has applied to several other WhatsApp-only paths already.

**A live incident, found and fixed inside this same session.** Mid-session, `yaad-notify-client` reverted in production to a version missing roughly six hours of shipped work, no `evidence_report_confirmed`, no `evidence_comment`, none of that day's Stage 6 or reporting-agent features, while the file on disk stayed correct the whole time. Found by a live call failing on a `kind` that plainly existed in the source being read; confirmed by pulling the actually-deployed source back and finding an older `KINDS` array. Almost certainly a second, different way CLAUDE.md's existing warning about pasting file contents into a deploy tool bites: two Claude sessions in the same working tree, and a deploy from something other than the CLI's disk-read path silently overwrote a newer deploy with an older one, no conflict shown to either side. Fixed by redeploying from disk immediately. Recorded in RUNBOOK.md as its own entry, because the fix is always the same once this is spotted, and the failure mode ("a live call fails in a way that makes no sense against the code you are reading") is the thing worth recognising fast.

## The reporting agent's "Next" line finally does something (31 Aug 2026)

The fourth and last item the previous entry left open. `composeEvidenceReport()`'s `what_happens_next` field has existed since the reporting agent was first ported into `yaad-notify-client`, and until now it was narrative: a sentence in a WhatsApp draft, read once by a worker and never looked at again by the system.

**Mirrors `job_stall_state` / `yaad-job-health` on purpose, rather than inventing a second pattern for "something is supposed to happen later on this job."** `job_followups` (`20260831zzzz3`) is a table of pending promises, one row per job and stage, with a due date; `yaad-followup-check` (`20260831zzzz4`), a new Edge Function on the same daily-cron shape, one hour after `yaad-job-health`, clears rows that real activity has already resolved, then re-runs `yaad-notify-client`'s `evidence_landed` kind, the exact worker-confirms-first draft/relay loop that already exists, for every row whose date has arrived with nothing having moved. Nothing new is invented for "prompt the worker again": the same draft, the same confirm-or-rewrite choice, is simply asked for again.

**A row only ever exists because the agent actually named a real next step.** `create_job_followup()` is called from `yaad-notify-client`'s `evidence_landed` branch, right after `composeEvidenceReport()` returns, and only when `what_happens_next` is non-empty and does not start with "nothing." A report that said there was nothing further creates no row, correctly, since there is nothing to check back on. Forty eight hours, not job-health's seventy two: this is a specific promised step, not general silence, and the report already told the client something concrete was coming.

**One pending row per job and stage**, a partial unique index on `(job_id, stage) where fired_at is null`. A fresh report on the same stage before the old promise is due updates the same row rather than piling up two, on the same "one authority" reasoning that already governs `jobs.status`: a job should not carry two different answers to "what set are we waiting on here."

**A real bug, caught live rather than by reading the function twice.** The first version of `create_job_followup` used `make_interval(hours => p_hours)` with `p_hours numeric`; `make_interval`'s `hours` parameter is an integer, and Postgres refused the call outright rather than silently truncating, `function make_interval(hours => numeric) does not exist`. Fixed with plain interval arithmetic, `now() + (p_hours * interval '1 hour')`, which accepts the same numeric default this function was always meant to take.

**Verified end to end against the live database, not by reading the migration and assuming.** A real job, a real worker with a phone, and one real evidence row with no image, just a label, ran through the actual `evidence_landed` path: the reporting agent composed a real next step from it ("Fit and seal the new flashing once the material arrives on site") and `job_followups` picked up a real row with that exact text and a due date forty eight hours out, no manual RPC call involved. Separately, by direct RPC, proved each piece the live run could not exercise on its own: the upsert replaces a pending row's reason and due date rather than duplicating it; `clear_resolved_followups()` deletes a pending row the moment a fresh evidence row lands on that job and stage after it was created; `due_job_followups()` correctly surfaces a row whose due date has passed and stays silent on one that has not; `mark_followup_fired()` removes it from the due list afterward. Test job, worker profile, evidence and follow-up rows deleted after.

**What could not be verified live: `yaad-followup-check`'s own authentication path, and by extension whether its re-notify call to `yaad-notify-client` will actually succeed on the real 14:00 UTC run.** `YAAD_CRON_SECRET`, the same environment secret `yaad-job-health` already depends on for the identical cross-function HTTP call, has to hold the plaintext that hashes to `notify_trigger_secret_sha256`; nothing in this session could read that secret's value to confirm it, mint an admin session to call the function directly, or read function logs after a real cron firing that has not happened yet. Reading the code and testing the SQL underneath it both say this should work; the first real 14:00 UTC run, or a manual admin-authenticated call to `yaad-followup-check`, is the genuine remaining proof, the same standard this repository has already applied to `yaad-job-health`'s own worker nudge and founder escalation sends.

## Stage 6, continued: a client can book a worker by replying on WhatsApp (31 Aug 2026)

Same intent as the WhatsApp approval reply, one door further along the job's life. Went through two wrong turns before landing, both found by testing live against the real database rather than by reading the code and trusting it, and both worth keeping the record of.

**First wrong turn: built on the wrong function.** `accept_quote_as_me()`, the one `AcceptPanel.tsx` calls from the no-account `/jobs/[id]/quotes` page, looked like the obvious thing to give a WhatsApp door to. It is not the real mechanism. `choose_worker()`, called from the signed-in gated portal, is: it enforces a rule `accept_quote_as_me()` knows nothing about, that a quote may only be accepted once both the client and the worker have separately agreed the job's scope (`scope_agreements`, one row per side, added 27–28 Aug specifically so a worker could not assign themselves to a client's own job). Rebuilt on `choose_worker()` instead, same split `approve_stage()` went through: a shared core, `_do_choose_worker(p_job, p_quote)`, never exposed to PostgREST, that `choose_worker()` and the new `choose_worker_via_whatsapp(p_job, p_phone)` both call.

**The founder's own decision on what counts as scope agreement over a channel with no form to tick a box on, asked plainly rather than assumed: send the proposed scope in words first, then ask for a reply to confirm it.** `yaad-notify-client`'s `quote_arrived` message now carries the worker's own proposed scope (`job_quotes.note`) alongside the price, and the reply that follows both records the client's `scope_agreements` row and calls `choose_worker_via_whatsapp()` in the same step: one reply, honestly earned, the client saw the proposal before the reply that binds it.

**Second wrong turn, found seconds after fixing the first: a transaction trap.** The first version of `choose_worker_via_whatsapp()` inserted the client's `scope_agreements` row and then called `_do_choose_worker()` in the same breath. When the worker had not agreed their own side yet, `_do_choose_worker()`'s refusal aborted the whole call, one Postgres transaction, and the client's own agreement, recorded a moment earlier in that same call, rolled back with it. Fixed by never letting a "not both agreed yet" outcome raise from this function: it checks readiness itself and returns a distinct marker (`PENDING_WORKER_SCOPE`) instead of an error when the worker's side is still missing, so the client's confirmation survives on its own. Nothing then auto-completes the booking the moment the worker's side lands, on purpose, matching the portal's own `agreeScope()`, which does not retry `choose_worker()` either: whoever confirms second, client or worker, is the one whose action actually books it.

**A third, smaller trap along the way: `min(uuid)` does not exist in Postgres.** The first draft picked "the" open quote with `min(id)`; fixed to `(array_agg(id))[1]`.

**Verified live end to end against the real database, the exact test the founder asked for by name: post a WhatsApp-shaped job, claim its code.** Two people pend the same code (`pend_portal_code`, allowed by design), the first to confirm binds it (`bind_confirmed_portal_claims`), the second binds nothing and the job is not rebound. Separately: a wrong phone number is refused; the correct one records the client's scope agreement and returns pending while the worker's side is missing; a worker's own agreement inserted afterward lets the identical reply now book, sets `jobs.worker_email`, marks the winning quote `accepted`; a second booking attempt on the same job is refused because the quote is no longer open, not because of a stale "already chosen" race. Test jobs, quotes and scope agreements deleted after.

**Found along the way, not part of this stage, fixed anyway because "fix this first" is what was asked: `accept_quote_as_me()` was worse than the reported bug.** It tries to mark a losing quote `not_chosen`, a value `job_quotes`'s own status check constraint has never allowed, real but narrow (only reachable with two or more competing quotes on the same no-account page). Testing it live turned up the actual, larger fault underneath: `job_quotes_touch`, a trigger predating tracked migrations, reverts any non-admin status change back to its old value unless the caller has set `yaadly.choosing = '1'` first. `accept_quote_as_me()` never set it. So the reported crash was the smaller symptom; the real one is that this function has never actually marked a quote `accepted` for anyone, on any booking through that page, single quote or several, while still reporting success and correctly setting `jobs.worker_email` (a table `job_quotes_touch` does not guard). Fixed with the one literal correction (`not_chosen` to `declined`) and the same `set_config` handshake `choose_worker()` already uses, nothing else changed. **Left alone on purpose: whether this page should also require the scope agreement `choose_worker()` enforces is a real product question, not a database typo**, and bundling that in risked turning a page that now visibly books into one that visibly refuses every booking until a scope-agreement step exists on it. Verified live: two competing quotes, one accepted, the other correctly `declined`, no constraint violation, `jobs.worker_email` set. Test rows deleted after.

**Not built, on purpose: the invoice link the stage originally called for.** There is no Stripe integration in this repository, and CLAUDE.md §9 lists payment integration of any kind as deliberately not built before the legal review lands. Put to the founder directly rather than defaulted around; her answer, 31 Aug 2026, was to relay the existing off-platform pay terms in words instead. `yaad-notify-client`'s new `quote_accepted` kind states the agreed price and the same bank transfer / Lynk / remittance terms already published in the worker FAQ, word for word, fired once from `jobs.worker_email` first being set, whichever door set it.

**Spotted, not touched, flagged separately: two live desk-visibility and delivery gaps unrelated to this stage**, spun off as their own background sessions rather than folded in here: nothing on the concierge desk currently reads `wa_intake_sessions`, so a mid-flight guided WhatsApp conversation is invisible to staff despite the table's own admin RLS policy implying it should be; and `quote_arrived`'s WhatsApp send has no approved-template fallback for outside the 24 hour messaging window, unlike what `RUNBOOK.md` still describes.

## Burst-photo debounce: evidence_landed waits for the burst to land (31 Aug 2026)

Asked for by name. `jobs.status` flips into `'evidence'` on the FIRST evidence photo filed against a stage, and that flip was the only thing that ever fired `evidence_landed` (a transition, never a state, confirmed live back in the original notify-trigger work). Correct for one photo. Wrong for a worker sending several photos back to back over WhatsApp, the realistic case DECISIONS.md already named once for the item-code work: the AI photo review and the worker's draft report were built from whatever evidence rows existed at the moment that FIRST photo's trigger fired, which could be one photo out of five, the rest still landing when the notification went out.

**The fix mirrors `job_followups` on purpose, not a second pattern invented for "wait a bit before acting."** A new table, `evidence_landed_pending` (`20260831zzzz6`), one open quiet-timer per job and stage, a partial unique index the same shape `job_followups` uses. Every evidence insert resets the timer to 90 seconds out (founder's own figure, 31 Aug 2026), rather than only the first insert for a stage mattering. `notify_client_on_job_change()`'s `evidence_landed` branch is removed entirely: that kind is no longer fired straight off the `jobs` UPDATE. `yaad-evidence-landed-check` (`20260831zzzz7`, cron wired once a minute, not daily like `yaad-job-health` and `yaad-followup-check`, since a client waiting on a burst notices seconds, not hours) fires it once the 90 seconds have actually passed with nothing new landing.

**Whether the notification is still worth sending is re-checked at fire time, not assumed from the timer having existed.** `due_evidence_landed_notifies()` joins back to `jobs` and only reports `should_notify = true` when the job is still sitting in `'evidence'` on the same stage the timer was opened for; a stage that got approved, disputed, or moved past in the 90 second window clears silently, the identical "real activity already answered it" reasoning `clear_resolved_followups()` uses for job_followups, just computed inline instead of as a separate sweep.

**Verified live against the real database, not assumed from reading the trigger.** A test job, three evidence rows inserted in quick succession simulating a burst: one open timer the whole time, never two, `due_at` moving forward on the second and third insert exactly as designed, no `evidence_landed` webhook fired on the first photo the way the old behaviour would have. The real, already-running pg_cron job picked up the due timer inside its normal one-minute cadence with no manual trigger, proving the schedule and the auth path both work end to end, not only the SQL underneath them.

**A real, separate bug found in the course of that verification, not fixed here, flagged instead.** `YAAD_CRON_SECRET`, the environment secret `yaad-job-health` and `yaad-followup-check` already depend on to call `yaad-notify-client` from outside a database trigger, does **not** currently hash to `notify_trigger_secret_sha256`, confirmed by a temporary diagnostic log comparing the two SHA-256 values directly (`cron_secret_hash=7ce2b9b9...`, `notify_trigger_secret_sha256=9ea7bddef2...`, `match=false`), then removed once confirmed. `yaad-notify-client` answers every one of these calls with a silent `403`, the same "recorded as a normal completed request, not a crash" failure shape `RUNBOOK.md`'s own secret-sync section warns about for trigger functions, just for an HTTP-calling Edge Function instead of a `pg_proc` trigger, which that section's own check query does not cover. This affects `yaad-evidence-landed-check` (this entry) and, more importantly, means **`yaad-followup-check`'s still-unproven 14:00 UTC daily run will 403 the same way**, the exact risk that entry's own "what could not be verified live" paragraph named and could not settle at the time. **Separately, and also found while checking this: `notify_worker_of_portal_comment` and `relay_confirmed_report` are themselves out of sync with `notify_trigger_secret_sha256` right now** (`RUNBOOK.md`'s own check query run live, 31 Aug 2026: both report a different `fn_hash` than `stored_hash`), so a worker's comment notification and a confirmed report relay to the client are silently 403ing today, unrelated to the debounce work. Not fixed in this session: rotating the shared secret touches every live trigger that checks it, a bigger blast radius than the task asked for, and is Monique's call to make, not a database typo to correct on the way past.

## Stage 7, first piece: the retainer offered at handover (31 Aug 2026)

Not the full stage, deliberately: Stage 7 is mostly copy and operations, several separate pieces (naming, annual billing, an insurance question for an adviser, a per-property record, worker continuity, a seasonal calendar). This is the one piece that plugs directly into code already live from Stage 6, the "convert at handover" moment itself, drafted and approved in words before it was wired in, the same order every consequential piece of copy in this repository goes through.

**Fires only on a job's final stage, not every approval.** The spec's own words, "the moment the client approves evidence and feels relief," read most honestly as the whole job finishing, not stage 2 of 5; pitching a retainer mid-job would land as premature. `yaad-notify-client`'s `stage_released` branch checks `jobs.status === 'complete'` (`sync_job_status` sets that once `stage` reaches 5) before appending anything, so the ordinary stage confirmation is unchanged for every stage but the last.

**Captures a reply, does not pretend to start anything.** There is no billing to switch on: Phase 1 is sell judgment, and CLAUDE.md §9 lists payment integration as deliberately not built yet. The message asks for a reply of "INTERESTED" and says Monique follows up personally, honest about what actually happens next rather than implying a self-serve activation that does not exist. Nothing new was wired to catch that reply specially; it flows through the existing inbound pipeline like any other message on that job's thread, which was judged sufficient for a lead-capture line rather than a reason to build a dedicated intake path.

**Founding rate £350/month, worker continuity named in the pitch ("[worker] keep an eye on the place"), currency left as GBP since nothing in the schema records what currency a given client actually pays in.** Whether that worker is actually offered the retainer job is still a manual, founder decision; nothing here creates any obligation or assignment.

Deployed and verified against the live function source directly, the exact "read the sentence back, not the intent" discipline the earlier `yaad-notify-client` deploy incident this same session made a point of: pulled the deployed source after each of the two deploys this piece needed and confirmed the actual wording (`"Yaad Report"`, `"Reply INTERESTED"`) was present, not assumed from the `deno check` pass alone.

## Kickoff Pack dual agreement, step 1 of 5 (31 Aug 2026)

Founder's own design, agreed in an earlier session and confirmed but not yet started: the Kickoff Pack should auto-draft on quote acceptance, pass a hard guardrail gate, and be confirmed by both client and worker with a shared code tied to one specific revision, the worker reaching it from a WhatsApp link and the client from the portal. Five gaps against that design were mapped against the code that actually exists (not assumed from the migration filenames): no auto-draft trigger, guardrail advisory rather than a hard gate, no worker-side confirmation at all, no confirmation code or dual agreement, no WhatsApp-linked worker page. Building it "one by one" as asked, this entry is the first piece only.

**Built the confirmation-code mechanism first, ahead of the auto-draft timing flip, on purpose.** The founder's list put auto-draft first, but flipping the draft trigger before a real human-clearance gate exists would remove the one checkpoint the current flow already has (an admin currently drafts and links the pack manually, before a quote can even be accepted, which is itself the review step) without yet having anything to put in its place. CLAUDE.md §3's refusal clause exists for exactly this shape of change. Building the gate first means the later timing change lands into infrastructure that can hold a human checkpoint, not into a gap where one used to be.

**Reused this repository's own confirmation-code pattern (`jobs.portal_code`) rather than inventing a new one.** `kickoff_packs.confirm_code` is generated the moment a pack is created (`link_kickoff_draft_to_job`, extended in place, not duplicated), and `agree_kickoff_pack(pack_id, code)` checks the submitted code against the pack's CURRENT code, not a fixed one issued at send time. A change after issue creates a new revision, which needs a new code, and an old link's baked-in code then fails closed with a plain reason rather than silently confirming content that has since changed. That behaviour was the page's own promise in its footer copy already, before anything backed it.

**New table `kickoff_pack_agreements` (pack_id, rev, side, email, agreed_at), modelled directly on the existing `scope_agreements` table rather than a new shape.** Primary key on (pack_id, rev, side) so a revision bump genuinely requires both sides to re-confirm; RLS grants parties read only, every write goes through the security-definer RPC, so a side can never be recorded as agreed except by that side's own signed-in call. `kickoff_packs` gained `both_confirmed_at`, set once both rows exist for the pack's current `rev`.

**A real bug, caught only because this was run end to end rather than trusted from a read-through.** The RPC's first version declared `returns table(side text, both_confirmed boolean)`; `side` as an OUT parameter shadowed `kickoff_pack_agreements.side` everywhere inside the function body, and `on conflict (pack_id, rev, side)` refused as ambiguous the first time it actually ran. Renamed to `agreed_side`. Caught with synthetic rows (`TEST-KO-1`, `TEST-KO-PACK-1`) inside a transaction that was rolled back, not committed: right code confirms and records the correct side by email match, a wrong code is refused without writing anything, `both_confirmed_at` sets itself the moment the second side lands. Confirmed no test rows remained afterward.

**Not done in this piece, and next in line:** the guardrail hard gate (today it only flags, on the draft row, before a human decides to link it), the WhatsApp notification carrying the code-bearing link to the worker (the portal page itself already renders correctly for a worker too, since `parties read approved packs` already covers both `client_email` and `worker_email` matches, so no new page was needed for reading, only for being told to go there), and the auto-draft timing flip itself.

## Kickoff Pack dual agreement, step 2 of 5: the guardrail becomes a hard gate (31 Aug 2026)

`link_kickoff_draft_to_job()` used to attach a draft that `yaad-kickoff` itself had flagged as containing price language, banned terms or foreign text, trusting the admin to have noticed the flags in the desk before clicking link. Nothing in the database stopped it. Now it refuses outright while any of the three flags is still true, naming exactly which ones. Verified live: a draft flagged `price_language_detected: true` was refused with the exact reason in one transaction; a clean draft linked normally in a second. First version used `v_reasons || 'text'` and failed live with "malformed array literal", Postgres resolving `||` against the array-literal overload rather than `anyarray||anyelement` for an untyped string; fixed with `array_append()`, which is unambiguous.

## Kickoff Pack dual agreement, step 3 of 5: the worker is actually told a pack exists (31 Aug 2026)

Until this, nothing ever pointed a worker at their own Kickoff Pack. A new trigger, `notify_worker_kickoff_pack_ready`, fires on the same transition `choose_worker()` already causes (`kickoff_packs.status` into `'approved'`), same shared-secret-extracted-not-regenerated pattern as every other trigger in this repository. Sends the worker a WhatsApp message with the portal pack link, the current confirm code attached, using the worker-recipient branch `evidence_landed`/`evidence_comment` already established (no email attempt for this kind, on purpose: WhatsApp is where a worker actually is).

**Two real bugs, both found only by actually running it, not from the deploy succeeding.** First: `yaad-notify-client` keeps an explicit whitelist of valid `kind` values; the new trigger's calls all 400'd with "secret, jobId and a valid kind are required" until `kickoff_pack_ready` was added to it. Second, after fixing that and redeploying: confirmed the actual call succeeded (`{"ok":true,"kind":"kickoff_pack_ready",...,"whatsapp":{"sent":false,"reason":"twilio 400 (21211)"}}`), the 21211 being Twilio correctly rejecting the test's deliberately fake phone number, not a fault in the new code. Both fixes deployed and re-verified before moving on; every source-of-truth check pulled the actual deployed function source rather than trusted the deploy log, per this repository's own "read the sentence back" rule.

## Kickoff Pack dual agreement, step 4 of 5: the auto-draft timing flip (1 Sep 2026)

The last and largest piece: a worker being chosen now triggers the Kickoff Pack automatically instead of requiring one to already exist. Flagged to the founder before building, specifically because `yaad-kickoff` gates itself to admin sessions only on purpose ("these documents are commercial terms for a real client"), and an automated trigger needs some way past that without weakening it for everyone else. Her call: route it through a polling cron function, the way every other automated check in this repository already works, and have `yaad-kickoff` accept the service role key as a second, equally strong caller identity, not a new bespoke secret. Reasoning discussed and agreed first: the shared-secret-baked-into-a-trigger pattern has already caused two real incidents this same day (`20260831z2`, the `YAAD_CRON_SECRET` drift RUNBOOK entry); reusing it for the highest-stakes function in the repository traded consistency for exactly the risk that pattern is weakest at, for no real gain, when the service role key was already sitting there as the one secret this codebase already treats as maximum-trust and never exposes to a browser (CLAUDE.md §6).

**What actually changed, four pieces:**
1. `kickoff_drafts` gained a nullable `job_id`. Nothing before this could tell "a draft for this job is already in flight" without it; the admin desk's manual flow never had to know, because a human remembered. An automatic poller cannot.
2. `yaad-kickoff` accepts a service-role caller alongside a real admin session. **Not** a JWT role-claim check: this project's service role secret is the newer opaque `sb_secret_...` form, not a JWT, so `role: "service_role"` never appears to decode. Direct constant-time string comparison of the presented bearer against the function's own `SUPABASE_SERVICE_ROLE_KEY` is what actually works, and is arguably a better check regardless of key format: it asks "is this literally our own secret" rather than trusting a claim inside an unverified-by-us payload.
3. `_do_choose_worker()` (shared by both the portal click and the WhatsApp booking reply, since a recent refactor this session found by pulling live source rather than trusting the older migration file) no longer requires a pack to exist, and no longer auto-approves an existing one inline. Both the precondition and the side effect are gone; a chosen worker just gets chosen.
4. A new function, `yaad-kickoff-check`, polling once a minute: requests a draft for a job with a chosen worker and none yet (skipping one that has already failed three times, so a persistently bad job doesn't hammer the model API forever); separately, links a finished, guardrail-clean draft straight to `'approved'`, or leaves a dirty one exactly where it is for a human to notice in the desk's existing Kickoff Drafts view, the same hard rule the manual door already enforces.

**Intake mapping is deliberately incomplete.** `jobs` has no column for client_location, property_type, ground_contact, constraints or already_agreed; rather than guess (job_type is not property_type, and guessing would mean inventing a fact about a specific job, the exact thing `yaad-kickoff`'s own system prompt refuses to do), those fields are simply omitted, and the model's own instruction to turn an unanswered field into an open question for the client does the rest.

**Three more real bugs, each found only by actually running the full chain, not from a clean deploy or a read-through, in the order they surfaced:** the Supabase gateway itself refuses a request whose `apikey` and `Authorization` headers carry different key types ("Conflicting API keys"), so both had to be the service role key, not the publishable key alongside it; the JWT-role-decode approach for the service-role check (described above) simply never fired, since this project's service key isn't a JWT, fixed with the constant-time string comparison instead; and the auto-link step originally inserted a pack directly at `status: 'approved'` in one write, which never fires `notify_worker_kickoff_pack_ready` because that trigger only fires on the UPDATE transition into `'approved'`, not an INSERT that starts there — an auto-issued pack would have silently never told the worker it existed, the exact failure step 3 was built to prevent, just reintroduced one step later. Fixed by inserting at `'draft'` and updating to `'approved'` as two writes, matching the shape the trigger actually watches for.

**Verified live, start to finish, with a real AI draft, not a synthetic one for the final proof.** A test job with a chosen worker and a real brief ("repaint the exterior walls and fix a leaking roof section") was left for the live, already-scheduled cron to find on its own, with no manual step. It requested a draft, the model wrote one, the guardrail passed it clean, it was auto-issued at `'approved'`, and the worker-notify trigger fired and reached `yaad-notify-client` successfully (`kind: "kickoff_pack_ready"`, WhatsApp attempted, rejected only for the test's fake phone number). The cover note read back addressed the client by name and correctly reasoned about the roof-and-repaint interaction, not generic filler. Separately, the guardrail-dirty branch was proven with a synthetic draft: no pack was created, exactly as intended. Every piece of test data (`TEST-KO-JOB-*`, matching drafts, packs, worker profiles) was deleted afterward; confirmed zero rows remaining across every table touched.

**Not done: the Trusted Circle path, and re-running the model on a job whose auto-draft failed for a reason worth understanding rather than retrying blind.** Out of scope for this piece; the three-failure cap exists specifically so a bad job doesn't retry forever unnoticed in the meantime.

## The concierge desk could read an evidence row, but never actually asked storage for the photo (1 Sep 2026)

Found while reviewing how photos move through WhatsApp, asked to confirm the feature actually worked before testing it live. The 31 Aug fix above closed the RLS side: an admin session can read `storage.objects` for the evidence bucket. Nobody had then gone back and made `concierge.html` actually call for a signed URL. Its Evidence view (`panelEvidence`, the row drawer) rendered the row's caption, its SHA-256 fingerprint, and a checklist match done against the caption text, and never once asked Supabase Storage for the file itself. `evidence.img` was correctly left null by every insert path (WhatsApp, portal), exactly as designed, on the assumption something downstream would fill it in for display; nothing did. A reviewer signing off in this desk was going on a filename, not a photograph, which is the same gap the governing rule in CLAUDE.md §2 exists to catch: a named human's decision is only as good as what they were actually shown.

**Fixed by having the desk sign its own links, the same way `/portal/jobs/[id]` already does.** A new `signEvidenceImgs()` runs inside `preEvidence()` on every view load, batches `storage.from("evidence").createSignedUrls()` across whatever rows do not already carry a link, and writes the result onto each row's own `img` column, in memory only, mirroring the portal page's `byPath` pattern rather than inventing a second one. Expiry is 3600s here, not the portal's 300s: a one-shot WhatsApp notification is read in the next minute or two, but a reviewer can sit on this desk for a while, and a broken image mid-review would read as a missing photo rather than an expired link. "What arrived" now renders the actual picture (or a muted, playable clip for video) in place of the old text-only chip, and opening a single row shows it full size. A row whose link could not be signed still shows the old label, with an honest note rather than a broken `<img>`, the same fail-open-to-a-message shape the rest of this desk already uses.

**Not yet verified against a live admin session or a real WhatsApp photo, only against the schema and the storage policy directly.** `storage.buckets.evidence.public = false` and `pg_policies` on `storage.objects` confirm "admins read every bucket" already covers this desk's session (`bucket_id = 'evidence' AND is_admin()`), and the signing call is the same shape already working in production for the portal, but nobody has actually opened the desk and looked at a real photo since this landed. That is next: send a photo on WhatsApp as a test worker, then open Evidence in the desk and confirm it renders.

## A phone shared by two worker_profiles rows silently picked the wrong one (1 Sep 2026)

Found live, testing the fix above with a real WhatsApp photo. A throwaway worker profile and job (`JOB-TEST-EVID-DESK`) were created for the test, phone set to the founder's own number. The photo did not reach the evidence path at all: it was read as a client message and filed against an unrelated draft job instead, and a follow-up text (the job code, sent to confirm) matched a *different*, older test job (`JOB-TEST-REAL-WA`, `status: 'evidence'`) where the same number was already on file as the client, and got logged as a client comment there, with a (harmless, fake-numbered) attempt to notify a worker.

**Root cause: `lookupWorkerByPhone()` used `.find()` over every active `worker_profiles` row with a phone, first match wins, no tiebreak.** A second, older seed profile (`test-live-worker@example.com`) already carried that same number from earlier testing, with one job on file and it `status: 'complete'`. Whichever row the query happened to return first decided everything downstream: when it was the finished-job profile, `lookupActiveJobsForWorker()` correctly found zero active jobs for it, the whole worker-evidence branch read that as "this number is not a worker with anything to file," and every message after that fell through to the ordinary client pipeline, silently. The new profile with real active work was sitting right there and was never tried.

**Fixed by replacing it with `lookupWorkerWithActiveJobs()`, which tries every phone match in turn and returns the first with active work, rather than trusting the first row a query happens to return.** Two call sites, the evidence-filing lane and the worker's-reply-to-a-client-comment lane, both updated; `lookupActiveJobsForWorker()` itself untouched. A phone shared across profiles is not exotic here: this repository already seeds more than one throwaway worker for testing, and nothing stops two real signups from having briefly shared a number in practice either. Deployed (`supabase functions deploy yaad-inbound --project-ref leffyisvfvjwzilydlwf --no-verify-jwt`, matching its live `verify_jwt: false` setting, read from `supabase functions list` first per CLAUDE.md §12). Re-tested live immediately after: `deno check` clean before deploy, and the next real photo from the same number is expected to resolve to the active profile now. That confirmation is the next message in this session, not yet written back here.

**Left alone, on purpose:** the stray `evidence_comments` row this bug wrote against `JOB-TEST-REAL-WA` (body: the other test job's code, from the confused mid-bug reply). Deleting it was blocked by this session's own auto-mode guardrail on a destructive write; flagged to the founder rather than pushed around.

## Confirmed: five photos is enough for the AI review, past that the worker's own edit is the safety net (1 Sep 2026)

Raised while testing the live evidence flow above: `MAX_PHOTOS_SENT_DIRECTLY = 5` in `yaad-notify-client` already caps how many photos reach the AI review and the direct-to-client WhatsApp message, oldest-first, no exception. A worker sending ten angles of the same problem gets all ten filed and safely stored, but only the first five ever get looked at by the model or texted to the client; the rest sit in `evidence`, viewable through the portal link, with nothing telling either side that happened.

**Founder's call: leave it exactly as it is, no change.** Five photos is enough for the AI to form a real read on what is happening, and the worker already reviews and can rewrite the drafted report before anything reaches the client (`evidence_landed` -> `report_confirm`, decided 31 Aug 2026, still the gate here), so a five-photo cap missing something is caught there, not silently. Explicitly rejected: telling workers to send one photo at a time. Worse for someone actually on site trying to document several angles, and the burst-debounce (90 seconds, above) already means the AI review and the client message fire once per batch regardless of how many photos arrive.

## The AI photo review gets a throwaway smaller copy of its own, and a second live incident on the same trigger (31 Aug 2026)

Founder's own instruction, after seeing the AI photo review's real failure rate for the first time: optimise it without switching provider. The founder had already ruled out Supabase's own image transform for this, correctly, on the grounds it is a paid-plan feature and this project is on the free plan; confirmed rather than assumed before reaching for a library instead.

**The real suspect was never NVIDIA's model, it was what NVIDIA was being handed.** A phone photo is commonly 8-12MB at full resolution, and nothing anywhere in this codebase resizes evidence before it is stored, on purpose: the stored copy is the proof, fingerprinted and timestamped exactly as filed, and it stays that way. Every vision call up to today handed NVIDIA's shared, free-tier inference service a URL to that same full original and made it do the downloading and decoding itself, on every single call, which is a real, structural reason for a shared service to time out.

**A throwaway copy, made and discarded inside the review call itself, never touching storage.** `shrinkForReview()` fetches the same signed URL the review already had, decodes it with `@matmen/imagescript` (JSR, zero native dependencies, the actual current package after `deno.land/x` module pages stopped resolving), caps the longer side at 1024 pixels preserving aspect ratio, re-encodes as JPEG at quality 70, and hands NVIDIA that as an embedded base64 image rather than a URL to fetch itself. `evidence.sha256`, the stored object, the signed URL the client and worker actually see: none of it is touched. A fetch, decode or encode failure on one photo falls back to the original URL rather than losing the review over it, the same "never let this block" rule the rest of this function already follows.

**Logged on both the success and the fallback path, not only the failure, the same gap this file already closed once for the review call itself.** A fast response and a silent fallback to the full-size original look identical from the outside otherwise. `yaad-vision: shrink {before}b -> {after}b ({w}x{h})` on success; a distinct line on a failed fetch or a failed decode. Verified live: the log line appeared on a real test call, confirming the resize path genuinely ran rather than being inferred from a faster response time. On this repository's own small stock test photos the re-encoded file was not smaller in bytes than the original, since a photo that was never large in the first place has nothing to gain from being shrunk; the pixel dimension cap, which is what actually drives NVIDIA's decode time down, engaged correctly regardless (confirmed at `1024x755` in the log line). The real test is a genuine multi-megabyte phone photo through the pilot, not a synthetic fixture built for a different purpose.

**A second live incident, found by accident while verifying this, more serious than the first one earlier the same day.** Reading `notify_client_on_job_change()`'s current body back before touching it (the exact discipline the first incident's own writeup called out) found that a concurrent session's own edit to add a `walkthrough_notes_ready` condition had replaced the whole function rather than extending it, and the `evidence_landed` branch, the transition that fires the entire worker-confirms-first draft/relay loop this session spent the day building and fixing, was gone entirely, not merely mismatched. The same edit had also regenerated `notify_trigger_secret_sha256` without reusing the existing plaintext, RUNBOOK.md's own already-documented mistake, a third and fourth time the same afternoon: `notify_worker_of_portal_comment` and `relay_confirmed_report` were both silently failing their own secret check. Fixed by extracting the plaintext every still-correct function agreed on and restoring `evidence_landed` ahead of the `stage_released` and `walkthrough_notes_ready` conditions already there, left exactly as they were rather than reverted. Verified live afterward, not merely deployed: all five functions checked against the stored hash agree, and a real evidence insert produced a real `evidence_landed` call with a real, correctly attributed AI finding. **The lesson worth carrying forward past today: `create or replace function` replaces the whole body. A function with this many callers gets extended by reading its current, live source and adding to it, never rewritten from a mental model of what it probably still contains.**

## The "second incident" above was not a regression. Reverted a second time (31 Aug 2026)

Read live, immediately before opening a PR: `notify_client_on_job_change()`'s `evidence_landed` branch was back, restored by the entry directly above this one. Its absence was not an accidental drop from the `walkthrough_notes_ready` edit. It was deliberate, the same afternoon, one entry earlier in this file: "Burst-photo debounce: evidence_landed waits for the burst to land." Every evidence photo now resets a 90-second quiet timer (`evidence_landed_pending`), and `yaad-evidence-landed-check` fires the notification once a burst has actually finished landing, not the instant the first photo does. Two sessions in this shared tree, each reading the same function and drawing the opposite, confident conclusion about why a branch was missing, both writing it up sincerely at the time, is exactly the failure CLAUDE.md §12 names.

**Removed a second time** (`20260831zzzz8`), stage_released and walkthrough_notes_ready copied verbatim, nothing else about them touched. The secret resync the previous entry did (`notify_client_quote_arrived`, `notify_client_on_job_change`, `notify_client_dispute_raised`, `notify_worker_of_portal_comment` and `relay_confirmed_report` all agreeing on one value) was itself correct and is kept: only the branch is undone, not the secret fix riding alongside it.

**A standing note left directly in `20260831zzzz8`'s own migration comment for whoever reads this function next:** `evidence_landed`'s absence from `notify_client_on_job_change` is the current, intended design, not a gap. Before restoring it a third time, check whether `trg_evidence_schedules_landed_notify` still exists on the `evidence` table and `yaad-evidence-landed-check` is still scheduled in `cron.job` first; if either is missing, that is the actual regression, not this branch.

## Service bookings are born in the desk, never on the public site (2 Sep 2026)

The founder's own call, verbatim reasoning: "what if a lot of people click and I don't have the bandwidth." The services page keeps its enquiry form as the only public door; `20260902o` adds `convert_enquiry_to_service()` so one desk click turns an enquiry into a `held` booking, `confirm_service_booking()` as the founder's explicit stop (it raises the invoice through `raise_service_invoice`, so the price is always the catalogue's, never typed), and `trg_start_service_on_invoice_paid` so the booking goes `live` by itself the moment a named admin marks the invoice paid, mirroring `start_job_on_agency_fee_paid` on the marketplace side. A held booking does not go live even if money lands early: the confirm is the gate, not the payment. The migration also carries the first schema-of-record for `services`, which until now existed only in the live database. Online card payment stays parked per §9 of CLAUDE.md until the legal review lands; the paid signal is the admin's own mark-paid click. This is stage one of copying the marketplace WhatsApp workflow onto services; the notification hub, the reply commands and the chasing clocks are deliberately separate, later changes.

## Service bookings notify from the state change, through the same hub (2 Sep 2026)

Stage two of copying the marketplace workflow onto services. Rather than a second notification function, `yaad-notify-client` learned three service kinds (`service_booked`, `service_confirmed`, `service_live`) carried by `serviceId` instead of `jobId`, reading `public.services` and reusing the identical channel ladder, secret check and tracing. One new trigger, `trg_notify_service_change` (`20260902p`), fires on the same three transitions `20260902o` defined, so a client hears about their booking whether the state moved from the desk, a future WhatsApp reply, or anything else that writes the row, and nothing in the desk has to remember to send anything. The trigger recovers the existing notify secret from a live function body per the 20260831z2 rule, generating nothing. The first message carries the portal code and the join link because a service client has no account yet; later ones link the portal room. Messages state only facts the row holds: no delivery promises beyond a `due_at` the founder herself set, and payment language stays "the invoice is on its way", never holding language. Reply commands over WhatsApp for services (confirm, approve) are the deliberate next piece, not this one.

## Every service booking gets a Kickoff Pack, and packs became editable (2 Sep 2026)

Founder's instruction: "made regardless and can be edited." Rather than a second pack pipeline, kickoff_drafts and kickoff_packs each gained a nullable `service_id` (`20260902q`), the pack read policy gained a service-client arm, and `yaad-kickoff-check` gained a services phase mirroring the quote phase: draft for every non-cancelled booking, auto-issue when guardrail-clean, hold dirty drafts for a human. The worker pack-ready trigger now fires only when `job_id` is present, because its notification names a job and a worker and a service pack has neither. The portal serves the pack read-only at `/portal/services/<id>/pack`, reusing the job pack's renderers; there is no dual confirmation on a service pack because Yaadly itself is the provider, and her "Confirm the work" click is the provider's signature. Editing: `edit_kickoff_doc_section()` rewrites one section's prose (cover, scope summary, timeline basis, payment note) and rides the existing `kickoff_touch` revision machinery, so an edit archives the old revision and de-approves the pack; `approve_kickoff_pack()` is the named-human way back. These two RPCs are the first editing and approving surface any pack has had, job packs included.

## The web can book a service directly, held (2 Sep 2026)

Founder reversed the enquiry-only door the same day it shipped: "should also be able to book directly on the web." The reconciliation is the held state, which was the real bandwidth gate all along: `yaad-book-service` creates a `held` booking priced from the catalogue, the receipt fires from the state change, and nothing proceeds until her confirm. The page's founding-rate figures are not in service_catalogue, so the clicked label (with its price) is recorded in notes for her to reconcile at confirm; that pricing question is hers, deliberately not solved in code. Technical Sign-off is not bookable because no catalogue row exists for it.

## The bot stands down after a handoff, and Monique replies through the same number (2 Sep 2026)

Two halves of one promise. First, `intake_threads.human_handling`: `wants_human` was one model call's read of one message and nothing remembered it, so a client who asked for a person got the bot again on their next message, which is a phone tree wearing a nicer sentence. The flag is set by `yaad-inbound` the moment a thread hands over and by `yaad-desk-reply` the moment Monique speaks on one, and while it is true the function keeps the record (transcript, photos, a high-priority push per message) but never runs the model. Deliberately no timeout and no automatic clearing: a bot rejoining a live human conversation because a clock ran out is worse than a quiet thread, and the per-message pushes are what stop a forgotten one being lost. Only the desk's "Hand back to the assistant" button clears it. Second, `yaad-desk-reply`: her reply used to come from her personal WhatsApp, arriving as a new chat from a strange number. Now the desk sends her words through the same Twilio sender the assistant uses, so the conversation continues where it started. That function deliberately contains no model call and does not screen her text through `guardrails.ts`: the banned-language screen exists to stop AI output making promises no human approved, and this is a named human speaking for herself, signed in and checked by `is_admin()` on the server side, with every database write under her own token so RLS is the access control. WhatsApp's 24 hour window is surfaced honestly as a refusal rather than worked around with a template, because a templated sentence pretending to be her defeats the point of the lane.

## The founder prices her own invoices; the AI still cannot (2 Sep 2026)

Her instruction: "make it so I can edit the invoices, just put whatever price, and add invoices at what I want it to be." The mechanism already existed unused: `price_source = 'manual'` has meant a human-set amount since the first invoicing migration, and the price guard refuses it only on AI-drafted invoices. `20260902s` and the desk changes open the doors without touching the rule: Confirm the work takes a price choice (full, founding, or a typed figure), the line editor gained a "my figure" tier with a typed amount, and a deliberate "Make it mine to price" click converts an AI draft to a human one before manual pricing unlocks, so owning the number is always a recorded human decision. Catalogue tiers stay guard-overwritten, so a typo against the service list still cannot ship. Technical Sign-off was added to the catalogue at the page's own £245 (no founding discount printed, so both tiers match) making it bookable through the web form; whether its founding rate should differ is her pricing call, editable in `service_catalogue`.

## The website chat is the WhatsApp assistant with a second door, and reaching a person is WhatsApp (2 Sep 2026)

Founder's ask: a chat on the site that answers questions there and connects to her if there is a problem; her rider in the same breath, "just use the whatsapp for now". Three shapes were on the table: a plain live-chat widget with no model, a third-party AI widget, or our own. A third-party AI widget was ruled out on the spot: it is a new processor of personal data, it does not obey `guardrails.ts`, and it will quote a price. Our own was built, but as a lane inside `yaad-inbound` rather than a new function, because "same brain" is only true if it is literally the same code: one prompt, one `readTheJob`, one `stripPromises`, one handoff counter, one screen. `docs/chat.js` posts `{channel: "web", visitor, text}` and gets JSON back where Twilio gets TwiML. A web thread is an `intake_threads` row keyed by a widget-minted hex token where the phone number would be, so it lands in the desk beside everything else with a draft job behind it, and nothing new needed RLS beyond the throttle table (`20260902t`, RLS on, no policies, service role only). Authentication on the door is the same posture as `yaad-enquiry`: Origin allowlist, token shape, and a three-way throttle, because every message is a paid model call and the door is public. The handoff copy differs by door on purpose: WhatsApp says "on this number", the web says "carry on on WhatsApp", because there is no reply lane back to a widget and a promise the desk cannot keep is worse than none. The piece that makes "you will not have to say it twice" true is the adoption step: a WhatsApp message carrying a `JOB-WEB-` reference (tolerant of a digit out, after her first live test typed one short) adopts the web thread onto that number, carries the transcript, keeps the job code and fills in the now-proven phone. It first marked the thread with Monique; she reversed that on watching it live ("AI should try help in the whatsapp chat before get passed to me"), so the assistant now continues on WhatsApp with the whole web conversation in front of it and a fresh three-turn allowance, handing over by the same rules as any thread, unless she had already personally replied in the web chat, in which case it stays hers. Deliberately not changed: the model provider; the chat runs on whatever `textmodel.ts` picks, which today is MiniMax, and a public page sending anonymous visitors' text there is the data protection question already recorded in this file, sharper now, and hers to time.

**Three additions the same evening, on her next two messages.** First, the reply lane after all ("a way I can answer back the person in the chat if they require a human"): `yaad-desk-reply` learned the web channel, writing her words to `web_chat_replies` (`20260902u`, RLS on, admin-only policy, written under her own token) instead of Twilio, and the widget polls that table through `yaad-inbound` while the page is open, using the visitor token as the only proof of who is asking. Polls are not model calls and are not throttled. The WhatsApp button stays beside it, because a reply into a closed tab reaches nobody and the desk note says so plainly. Second, the FAQ ("should also have the FAQ question if someone asks them"): the twelve answers from `docs/faq.html` are condensed into `faq.ts` and injected into the one intake prompt, so WhatsApp answers them too. That put published service prices into a model's mouth for the first time, which is not the pricing path §5 protects (a repair is still only ever quoted by a worker against a scope) but is still a number a model could garble; so `price-figures.ts` is the hold point: any pound, J$ or percent figure not on the published list is cut from the reply in code, on every channel, and a test proves `faq.ts` itself passes the guard so the two files cannot drift apart. Third, a backstop found by testing: "Can I talk to a real person please" once got the generic opener because the model call failed and `wants_human` was that call's only reading; a plain word match on the visitor's own text now adds the true the model missed, the same shape as the confirmation backstop. Testing the FAQ answers surfaced two more model habits, both handled in code rather than by hoping: the longer prompt made the model think longer, and 3500 tokens was no longer enough for the reasoning plus the answer (budget raised to 6000, and the think block is stripped before the JSON is looked for, so a brace inside the reasoning is never mistaken for the answer); and asked a plain question, the model sometimes answers in plain prose with no JSON at all, so prose of a sane length with no braces is now taken as the reply, with every classification field left empty, and still passes through the promise strip, the figure guard and the language screen like any other reply.

## The site says Yaadly is principal, and three legal pages went up with their gaps marked (3 Sep 2026)

The public site described a venue where a client and a tradesperson transact and a platform sits in the middle holding money. That is not the business, and it is the reading the principal structure exists to avoid, so the copy moved: the client buys the job from Yaadly, Yaadly prices it and subcontracts a vetted worker, and the client does not contract with the worker. The specific sentences were "a vetted tradesperson quotes it" and "vetted workers quote it" (a client receiving a quote from a worker is a client contracting with that worker), "can see the money is real" and "the job is funded" on the worker panel (both describe a pot set aside for the worker; under the principal structure the money is Yaadly's and so is the commitment, and the reassurance to the worker is identical either way), and the page title "The Marketplace". Five uses of "escrow" in the trust-engine demo went with them: that word is banned outright by CLAUDE.md §8, it had been live on the page the whole time, and the 3 September brief driving this change did not catch it either. The "25% max deposit, ever" claim came off in all four places because the founder removed the deposit cap the same day and no replacement figure was set; the panel it anchored now shows an unquantified four-stage ladder, and the CSS that hard-coded a 25% bar width was deleted rather than left to be re-used. Alongside those, "0 Contractor ties" and the company-wide "Independence guarantee" card came off the services page as simply untrue on a managed job, where Yaadly does price, supply and manage the trade. The narrower per-job independence claims on that page were left standing deliberately: on a check job where the client has their own builder they are still true, and that is the founder's call to make, not a session's.

Three legal pages were published in the same change (`docs/privacy.html`, `docs/cancellation.html`, `docs/terms.html`), with nothing anywhere on the site linking to a legal document before this. The governing instruction was publish now, clear with the adviser after, on the reasoning that a page which exists and says plainly what is still being settled beats no page and beats invented legal text by further still. So every genuinely open question is a visible marked box rather than a plausible paragraph: retention periods, liability and its limits, the workmanship guarantee (deliberately not promised before it is insured, though the three UK principals checked all give twelve months in their own name, so that is the norm it will be measured against), governing law for United States and Canada clients, and the registered office address. The prescribed statutory wording is the sharpest case: the model cancellation instructions and model cancellation form are substantially set by Schedules 3 and 4 of the Consumer Contracts (Information, Cancellation and Additional Charges) Regulations 2013, legislation.gov.uk is blocked by this environment's egress policy, and an approximation written from memory is worse than a marked gap because a wrong version reads exactly like the real one. The 14 day right, how to exercise it, and the consequence of asking for work to start early are all stated in full regardless, and are honoured whether or not the form is on the page. Two disclosures on the privacy page are marked not to be softened: the text model is MiniMax, in China, until the Mistral move lands, and identity documents never reach any model at all. Supabase's region was confirmed as eu-west-3 (Paris) rather than published on assumption.

Two things this change deliberately did not do. The registered office address is still not in the footer, which leaves Regulation 25 of the Company, Limited Liability Partnership and Business (Names and Trading Disclosures) Regulations 2015 unmet: the current registered office is the founder's home address, and republishing it in a footer on every page is worse than the gap. That unblocks only when an AD01 filing moves it to a service address. And no Stripe payment links were added, because creating them needs the Stripe account and a secret key must never enter a chat session; the choice between Dashboard links (charged immediately, five minutes, loses the line "your card is not charged until you approve the work") and API-created links with `capture_method` set to `manual` (keeps the hold, and the hold is the differentiator) is recorded for the founder, unmade.

## The registered office waits for a service address, and £500 splits invoice from hold (3 Sep 2026)

Two founder instructions the same evening.

**The address.** Regulation 25 of the Company, Limited Liability Partnership and Business (Names and Trading Disclosures) Regulations 2015 requires a company's website to carry the part of the UK it is registered in, the registered number and the registered office address. The first two were in the footer; the third was left out because the registered office is the founder's home, and a footer on every page of a site fed by a 9,000 follower TikTok is a different kind of exposure from a Companies House record: indexed, cached, archived. She instructed that it be published anyway to close the regulation, it was, and within the same session she reversed and chose to take a registered office service address instead. Nothing reached `main`, so the address was never live. The site is therefore back to carrying no address, and the Regulation 25 gap on that third element stands as a known, accepted, temporary breach rather than a permanent publication of a home address. It closes when the service address is taken (an "appropriate address" under the 4 March 2024 rules, so not a PO Box, about £30 to £50 a year) and an AD01 is filed within 14 days. `RUNBOOK.md` carries the three places the address goes and the limit that the historic register entry cannot be undone either way. The reversal is recorded rather than tidied away because the reasoning, that the cheaper compliance route costs something no policy page can give back, is the part worth keeping.

**The £500 threshold.** The morning's resolution was that payment terms are agreed in writing per job with no site-wide rule, which settled the terms but left the instrument unsettled, and the 3 September brief had flagged the consequence: a job paid by card hold produces no invoice, and the invoice was the artefact evidencing the principal structure, each one saying the client bought the job from Yaadly. The threshold resolves it. Above £500 the job is invoiced, so every larger job has that record. At or under £500 the card is authorised at booking and captured on the client's approval. The two rules coexist rather than contradict: price picks the instrument, the written terms still govern stages and amounts. Two constraints were carried into the copy. First, no Stripe links exist yet, so the hold half is not live and all three pages say every job is invoiced for now; that sentence comes out when the links do, not before. Second, "your card is not charged until you approve the work" is true only of a hold, so it is scoped to the at-or-under-£500 clause everywhere it appears and must never sit beside an invoice job. Under £500 there is still no invoice, so the written confirmation issued at booking has to carry the principal statement, which it must go out to do anyway under the distance selling durable-medium requirement.

## Principal, plumbed: one all-in client invoice and a worker payable (3 Sep 2026)

Founder's instruction, "build to principal now", after the structure question was settled earlier the same day. The trigger was her asking how she takes her 12% from the worker, and the honest answer being that she could not, because nothing had been built to.

**What was live and wrong.** Two documents per job. `raise_job_agency_fee_invoice()` billed the client 15% of labour. `raise_job_worker_pay_invoice()` billed the client the worker's **full** labour plus materials, with the note "no fee added, pay them directly". Meanwhile the worker's own screens showed him his labour **less 12%** under the words "You receive". So on one job the client was told to pay him 100% and he was told he would get 88%, and no invoice type, `payable_to` value or function anywhere collected the difference. The 12% was decorative. No real payment data exists in the repo, so nobody was actually shortchanged; it was found before a worker found it.

**Why principal dissolves it instead of plumbing it.** There is no fee to collect from a subcontractor. Yaadly engages him at his quoted price less 12% and pays him that, so the 12% is never collected because it is never paid out. Nothing of his passes through Yaadly, which is the entire point: taking money from one party to pass to another can be a regulated payment service under the Payment Services Regulations 2017, while buying work and reselling it is ordinary trading. The arithmetic is unchanged for everyone. Client pays labour plus 15% plus materials at cost; worker is paid labour less 12% plus materials at cost; Yaadly's margin is 27% of labour with materials passed through untouched.

`raise_job_client_invoice()` and `raise_job_worker_payable()` replace the pair, and the old two are dropped outright rather than left unused, following the precedent 20260901y set with `raise_job_stage_invoice`: a working function that computes the wrong money is worse to leave lying around than no function at all.

**One security consequence that was easy to miss.** Under the old shape the worker-pay invoice carried the *client's* email, correctly, because the client was paying it. Under this shape the client must never read that document: it is Yaadly's cost base, and `invoices_client_read` matches on `lower(client_email)` against the caller's JWT email, so leaving the client there would have handed them Yaadly's margin. `client_email` is NOT NULL, so a worker payable now carries a sentinel at a `.invalid` address, a TLD RFC 2606 reserves precisely so it can never be a real mailbox or match a real session. The worker still reads his own through `worker_email` and `invoices_worker_read`, untouched.

**On telling the worker there is a cut,** which the founder asked for explicitly. The words changed, the numbers did not. "Your fee, 12%" and "You receive" described money of his being reduced, which is the agent shape. It now reads "Yaadly's margin, 12% on labour" and "Yaadly pays you", with the 12% still shown in full on the quote screen before he accepts, because that is the only moment he is deciding anything. His portal now says Yaadly engages him at his quoted price less 12%, agreed before he accepts and never changed afterwards. He can still see that Yaadly makes 15% on the client side and 12% on his, which is unusually open and is a deliberate trust choice, not an oversight.

**What this does not do.** It changes what the documents say, not how money moves: both are still admin-raised and admin-marked-paid off platform. `docs/payments.html` still promises no client payment is taken until legal sign-off and insurance are in hand, and that promise is untouched, because the moment the client pays Yaadly for the whole job is the moment Yaadly carries the liability. `raise_job_stage_worker_pay_invoice(job, stage)` still apportions the old shape across payment stages and needs the same treatment; it reads the Kickoff Pack and the quote pack draft to do it, so it deserves its own review rather than a rewrite in passing. Recorded in RUNBOOK.md.

## Payment moves to principal, live, on the founder's word (3 Sep 2026)

Her instruction, verbatim in substance: legal sign-off and insurance are in hand, move forward with payment, she will gather the legal structure, and this is not to be a hold. The published promise that no client payment would be taken until both were in hand has therefore been met and comes off the site, along with every "you pay your tradesperson directly" statement across the marketing pages, the FAQ's structured data, the WhatsApp assistant's facts and the client notification texts.

**The replacement claim is deliberately the founder's own, and it is smaller than the one it replaces.** "Standing behind the work" was the old sentence. She said what she actually offers is that Yaadly reviews the works and ensures issues are handled, on small jobs where the risk is contained, so that is what the site now says. No workmanship guarantee period is stated anywhere, because none has been set and promising a length before it is insured for that length is how a guarantee becomes a liability. The placeholder on `docs/terms.html` stays.

The counter-argument was put to her once and she is aware of it: small jobs cap the price, not the exposure. As principal the client's remedy is against Yaadly, so on a £400 plumbing job the worst case is the flooded house, not the £400. Her own 3 September brief made the same point in its opening correction. That is a founder risk judgment, taken knowingly, and it is hers.

Two things did not switch on with it. Card payment is still not live because no Stripe links exist, so every job is invoiced and paid by bank transfer and all three pricing surfaces still say so. And `CLAUDE.md` section 9 still parks payment integration until the legal review lands, which is now stale; that file is Monique's and a session must not rewrite it to suit a task, so it is flagged for her rather than edited.

One regression from earlier the same day was caught and fixed in the same change. The catalogue price alignment updated `docs/services.html` and `docs/faq.html` but not `price-figures.ts`, the exact-figure allowlist that guards what the assistant may say. Its own comment says to update it in the same commit as `faq.ts`, and that was missed, so the assistant would have had every sentence carrying £249 or £45 cut before sending, leaving a client asking about prices with no price at all. The list now carries all twelve published figures, and the runbook records that `service_catalogue`, the services page, `faq.ts` and this list are four copies of one fact.
