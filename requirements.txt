# Needs Python 3.10 or newer.
#
# opentelemetry-sdk below is pinned at a version whose own metadata says
# Requires-Python >=3.10, so on anything older `pip install -r requirements.txt`
# fails with "No matching distribution found" and never says why. macOS ships
# Python 3.9 as /usr/bin/python3 (Command Line Tools), which is what a fresh Mac
# will use unless told otherwise, and Python 3.9 reached end of life in October
# 2025. CI runs 3.11 and 3.12.
#
# If that install fails, check `python3 -VV` before anything else.

openai>=1.40
pytest>=8.0

# Observability (OpenTelemetry). The engine no-ops without these, but they
# are first-class dependencies: spans, counter and duration histogram, and a
# bounded audit event for every money and guardrail decision, live in
# yaad/telemetry.py. openai-v2 auto-instrumentation covers the live path and
# is activated in run_demo.main via telemetry.init(). Pinned exactly rather
# than a floor, so a fresh install always gets the versions this was tested
# against.
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-openai-v2==2.4b0
